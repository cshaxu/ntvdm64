# mvdm-host

Original-relative MVDM host mirror. M0 T311 re-rooted original support
carriers (`dirs`, `makefil0`, `inc`, `oemuni`, `suballoc`) here, beside their
original MVDM host consumers. Standalone tools and selected firmware inputs
remain in their own mirror components. This root has no locally authored
provider replacement.

## Provenance

- [S1 canonical union ledger](../../docs/etc/operations/m0-t274-s1-non-guest-mvdm-union-ledger.tsv)
  selects one source for every host path.
- [S5 topology manifest](../../docs/etc/operations/m0-t274-s5-mvdm-host-topology-manifest.tsv)
  records all 1,689 host rows, including imported/deferred disposition, source
  path and SHA-256.
- [S5 closure evidence](../../docs/etc/operations/m0-t274-s5-mvdm-host-topology-closure-001.md)
  records the topology/body boundary.
- [S6 host-input manifest](../../docs/etc/operations/m0-t274-s6-mvdm-host-input-manifest.tsv)
  records the 639 S5-deferred selections and their copied-file hashes.
- [S6 closure evidence](../../docs/etc/operations/m0-t274-s6-mvdm-host-package-supply-closure-001.md)
  records full selected-host coverage and verification.

The root carries the current host-owned selection described by the topology
manifests. T310/S4 reclassified executable `softpc.new/base/bios` and
`softpc.new/base/keymouse` packages from the firmware-input component back to
this host mirror: their 24 source/makefile/manifest files are byte-exact
selected-OpenNT mirrors; their 23 historical `obj/` products have no selected
source-root counterpart and are retained as byte-identical project evidence.
Reset, BIOS services and controller algorithms are original SoftPC runtime
code. This physical layout is not by itself a runtime-enablement claim.

T301 S1 P21 additionally preserves missing byte-exact historical build
products from `opennt-src-2` under their original-relative host paths.  These
files are source/product evidence only: they are not host build, link or
runtime-discovery inputs.  The complete per-file provenance is in
[`m0-t301-s1-opennt-src-2-mvdm-extra-import-manifest.tsv`](../../docs/etc/operations/m0-t301-s1-opennt-src-2-mvdm-extra-import-manifest.tsv).

## Divergence register

### Re-rooted MVDM support declarations

| ID | Original purpose | Reason | Implementation | Files |
| --- | --- | --- | --- | --- |
| MVDM-SUPPORT-DIV-001 | Model `DEMEXTERR.ExtendedErrorPointer` as a 32-bit field in the packed DOS data record. | The original x86 `PUCHAR` happens to be 32 bits; on x64 it widens and changes the guest ABI. | Preserve the source record's nine-byte packed layout by storing the guest pointer numerically as `ULONG`. | `inc/dossvc.h` |
| MVDM-SUPPORT-DIV-002 | Model the packed `DOS_CALL_NAMED_PIPE_STRUCT` 16:16 pointer fields as native pointer typedefs. | The original x86 typedefs happen to be 32 bits; on x64 they widen and move every subsequent guest field. | Preserve the original four-byte physical 16:16 values as `DWORD`; unchanged `READ_FAR_POINTER` source decoding retains the original name, parameter and result semantics through the bounded Redirector pointer scope. | `inc/vrnmpipe.h` |
| MVDM-SUPPORT-DIV-003 | Model the packed `DosWriteMailslotStruct` buffer location as a native pointer. | The original x86 pointer is four guest bytes and would widen on x64. | Keep a four-byte numeric 16:16 location, decoded only by the original `READ_FAR_POINTER` flow inside a bounded Redirector pointer scope. | `inc/apistruc.h` |
| MVDM-SUPPORT-DIV-004 | Model packed `DOS_ASYNC_NAMED_PIPE_STRUCT` far-address members as native pointer typedefs. | The original 16:16 request is a fixed guest ABI; x64 pointer widening changes offsets before original Redirector code can inspect it. | Keep each location as its original four-byte numeric value. The original source decodes it only under a bounded Redirector pointer scope; cross-call completion still requires a separately admitted safe completion binding. | `inc/vrnmpipe.h` |
| MVDM-SUPPORT-DIV-005 | Declare the internal MVDM hardware/software/fault interrupt registration callbacks. | The original public declarations carried function pointers as `PVOID`, relying on an invalid function/data-pointer conversion that has no safe C ABI meaning. | Retain the original registration entrypoint names and callback parameters/results, but publish three named `int`-returning function-pointer types. | `inc/intapi.h`; `softpc.new/host/inc/nt_inthk.h`; `softpc.new/host/src/nt_inthk.c` |
| MVDM-SUPPORT-DIV-006 | Select the x86 direct fetch/store macros in `mvdm.h`. | The original `i386` condition otherwise chooses a MIPS/RISC fallback when a host build does not define `i386`. | Retain the original x86 macro bodies for the x86 guest contract. | `inc/mvdm.h` |
| MVDM-SUPPORT-DIV-007 | Select the x86 4-KiB suballocation commit granularity. | The original `i386` condition otherwise chooses its historical 64-KiB MIPS/RISC branch. | Retain the original 4-KiB x86 guest commitment contract. | `inc/suballoc.h` |
| MVDM-SUPPORT-DIV-008 | Include the original VDM_TIB declaration through an absolute NT source-tree path. | The historical path cannot resolve outside the OpenNT build tree. | Resolve the selected identical `vdm.h` declaration through the mirrored OpenNT ABI include root; no type, field or ABI is recreated. | `inc/vdmtib.h` |

### Existing MVDM host divergences

| ID | Original purpose | Reason | Implementation | Files |
| MVDM-HOST-DIV-171 | Write the original local computer/user result directly through `LPSTR_FROM_WORDS(ES:DI)`. | The historical x86 host alias has no explicit lifetime and cannot safely cross the current session boundary; the selected public NetAPI carrier returns the user name as Unicode without the historical global `UNICODE` compile setting. | Preserve the original public calls, capacity gates and register/status order; replace only the two bounded copies with the existing session lease through the Redirector helper, with OEM conversion before the username copy. Lease refusal returns `ERROR_INVALID_ADDRESS` without writing guest memory. | `vdmredir/vrnetapi.c`; `../adapter-mvdm-host-out/redir/include/mvdm_redirector_guest_copy.h`; `../adapter-mvdm-host-out/redir/mvdm_redirector_guest_copy.c` |
| MVDM-HOST-DIV-172 | Use `VrRemoteApi`'s locally synthesized computer name as the server name for the original RAP branch. | The original source explicitly copied the Unicode `NetWkstaGetInfo` result with `strcpy`, which is not a valid modern C string conversion. | Preserve the fixed local OEM temporary, `NetApiBufferFree`, and original later RAP branch; convert only the public Unicode result through `WideCharToMultiByte(CP_OEMCP)`. Conversion failure releases the original local allocation and returns `ERROR_INVALID_DATA`. | `vdmredir/vrremote.c` |
| MVDM-HOST-DIV-176 | Write the original `I_CDNames` trio of guest far-pointer destinations after obtaining public workstation/user values. | The original treats the packed guest structure and all three targets as durable flat aliases. That crosses the current session boundary without a lease and is invalid on x64. | Keep original NetAPI calls, ANSI conversion, upper-casing and no-return-code behavior. The Redirector adapter reads the 12-byte guest record, clears each non-null target, and writes each converted result through bounded leases. | `vdmredir/vrnetapi.c`; `../adapter-mvdm-host-out/redir/{include/mvdm_redirector_guest_copy.h,mvdm_redirector_guest_copy.c}` |
| MVDM-HOST-DIV-177 | Diagnose the original `demOpen` canonical pathname and scalar file result during fixed-container permanent-COMMAND bootstrap recovery. | T353 proves the original command open fails but not whether NTDOS reached the default or `cmdconf`-generated shell pathname. Direct inspection must not retain `GetVDMAddr` as a host alias. | Retain original `demOpen` order, path, CreateFile, handle, CF and AX behavior. A default-off adapter helper receives only DS:SI numbers, independently makes a bounded read lease, records copied text/scalars, and releases before the original call continues. | `dos/dem/demfile.c`; `../adapter-mvdm-host-out/softpc/{include/mvdm_softpc_termination.h,mvdm_softpc_termination.c}` |
| --- | --- | --- | --- | --- |
| MVDM-HOST-DIV-001 | Store a native x86 `PVOID` find-list reference in the DOS DTA/FCB reserved field. | The original field is 32 bits by process-pointer coincidence; on x64 `PVOID` widens and corrupts the original DOS layout. | Retain the original 32-bit field with `ULONG`; its value is an opaque host-resource surrogate, not a pointer. | `dos/dem/dosdef.h` |
| MVDM-HOST-DIV-002 | Persist, compare and release a `PFFINDLIST` pointer across DOS FindFirst/FindNext calls. | Native pointer persistence is width-unsafe and forbidden across the MVDM/guest boundary. | The original source flow publishes, resolves and releases an opaque identity through the S15 adapter-mvdm-host-out/softpc facade; `FFindId` stays original numeric data and source resource-failure branches are retained. | `dos/dem/demsrch.c` |
| MVDM-HOST-DIV-003 | Use 32-bit host pointer casts for DEM directory-handle sentinels, pathname/device offsets and directory-buffer traversal. | Those values are host-local native-width pointers/HANDLEs on x64; their original arithmetic truncates them even though no value belongs in guest state. | Retain original algorithms, data flow and failure branches while using native-width HANDLE sentinel and byte-pointer/`uintptr_t` arithmetic. | `dos/dem/demsrch.c` |
| MVDM-HOST-DIV-004 | Retain and write the DOS extended-error location installed by `demSetDTALocation`. | A native `GetVDMAddr` pointer cannot cross a callback or an x64 ABI boundary. | Preserve DS:CX as an `adapter-mvdm-host-out/softpc` numeric guest descriptor; `demRead` takes a fresh nine-byte bounded lease, writes the original fields in order, and commits/releases before it continues. | `dos/dem/dem.h`, `dos/dem/demdata.c`, `dos/dem/demgset.c`, `dos/dem/demhndl.c` |
| MVDM-HOST-DIV-005 | Retain the NTDOS DTA far-address cell and use its current target for DEM search/FCB I/O. | The original `pulDTALocation` is a persistent native pointer, and DTA bytes must not outlive a guest-memory lease. | Preserve DS:AX and its four-byte far-address cell numerically; use fresh bounded DTA leases for FindFirst, FindNext and FCB I/O, committing only guest-write paths. Lease failure follows existing DEM failure flow with `ERROR_INVALID_ADDRESS`. | `dos/dem/dem.h`, `dos/dem/demdata.c`, `dos/dem/demgset.c`, `dos/dem/demsrch.c`, `dos/dem/demfcb.c` |
| MVDM-HOST-DIV-006 | Retain the current DOS PDB scalar for DEM file/find and SoftPC disk/floppy ownership. | The original `pusCurrentPDB` is a persistent native pointer; its two-byte guest value must not survive a callback as host memory. | Preserve DS:DX as a numeric descriptor and take a fresh two-byte read lease at each reached consumer. Existing false/zero/negative error directions are retained when the scalar cannot be read. | `dos/dem/dem.h`, `dos/dem/demdata.c`, `dos/dem/demgset.c`, `dos/dem/demsrch.c`, `dos/dem/demfile.c`, `softpc.new/host/src/nt_fdisk.c`, `softpc.new/host/src/nt_rflop.c` |
| MVDM-HOST-DIV-007 | Retain the DEM SFT chain and implement VDD SFT/JFT handle operations. | The original persists `pSFTHead` and returns direct guest aliases that WOW writes after the call; its 32-bit `SFT_NTHandle` also truncates x64 handles. | Keep the SFT head as numeric DS:offset.  Active VDD wrappers use the adapter-mvdm-host-out/softpc session-owned shadow/commit contract; reached WOW caller writes have explicit final commit/discard points. | `dos/dem/dem.h`, `dos/dem/demdata.c`, `dos/dem/demgset.c`, `dos/dem/demfile.c`, `wow32/wkfileio.c` |
| MVDM-HOST-DIV-008 | Resolve dynamically loaded WOW and installable-BOP entrypoints through the historical `FARPROC`/`MYFARPROC` variables, including the original ordinal-name threshold. | The historical x86 compiler accepted incompatible implicit function-pointer assignments and compared host `PCHAR` values after narrowing them to `ULONG`; modern x64 rejects or truncates those representations even though `GetProcAddress` supplies the same raw export address. | Explicit function-pointer casts retain the original selected pointer representation and call order; `UINT_PTR` retains the original ordinal threshold for private host pointers. They do not enable WOW or installable BOP providers; their distinct calling contracts remain owned by those later packages. | `softpc.new/host/src/nt_bop.c` |
| MVDM-HOST-DIV-009 | Write `cmdGetCurrentDir`'s `DS:SI` result directly through an unbounded `GetVDMAddr` pointer. | A guest pointer cannot be retained as host memory on x64 or across the modern component boundary. | Retain the original drive/environment order and CF/AX outcomes; stage the at-most-67-byte OEM result in a local buffer, then use the session-owned adapter-mvdm-host-out/softpc numeric descriptor and exact synchronous guest write. | `dos/command/cmdmisc.c` |
| MVDM-HOST-DIV-010 | Persist `pHimemA20State` as a native `GetVDMAddr` pointer for later A20 state-byte writes. | The pointer is not width-safe and cannot survive beyond a scoped guest-memory lease. | Preserve the AX:BX numeric location and A20 write ordering; the matching private `mvdm-host-overlay` records only that location and uses a fresh one-byte session lease for each write. | `xms.486/xmsa20.c`, `xms.486/xmsumb.c`, `xms.486/xms.h`; `../mvdm-host-overlay/xms.486/xms_a20_state.[ch]` |
| MVDM-HOST-DIV-012 | Return a DPMI extended-memory allocation record by splitting the native `XMEM_BLOCK *` into `SI:DI`. | The original x86 process-pointer coincidence truncates a native pointer on x64 and persists it across BOP calls. | Retain the original `SI:DI` 32-bit handle ABI, allocation/failure ordering and source list; publish, resolve and release an opaque `host_resource` surrogate through the existing same-shaped SoftPC facade. | `dpmi32/xmem.c` |
| MVDM-HOST-DIV-018 | Return the monitor `VdmTib.PmStackInfo` address in the original `CX:DX` x86 register pair. | A process pointer is not a valid guest value on x64 and must not cross the host/guest boundary. | Publish the address through the existing per-session host-resource mapping manager, return its stable opaque 32-bit identity in `CX:DX`, and return zero on mapping failure. The later monitor projection owner, not this mirror, decides whether guest code receives a descriptor-backed view. | `dpmi32/dpmiint.c` |
| MVDM-HOST-DIV-139 | Define the x86 `LDT_ENTRY` descriptor layout for the non-kernel-VDM DPMI source branch. | Modern public `winnt.h` supplies that same declaration before the historical source-local fallback, causing a duplicate C type definition. | Preserve the original fallback verbatim only when the public declaration carrier is absent; no selector algorithm or descriptor field is changed. | `dpmi32/dpmidata.h` |
| MVDM-HOST-DIV-140 | Preserve original DPMI32 `IntelBase`/`FlatAddress` host-flat-address arithmetic on either host width. | NT4/x86 stored private host pointers in `ULONG`; this truncates selected SoftPC backing addresses on x64 before original descriptor, buffer, IVT and flat/segmented conversion logic can run. | Use `ULONG_PTR` only for private host-address storage and arithmetic; retain every guest address, descriptor field, register result and 16:16 narrowing rule. No pointer is published through a guest ABI or mapping token. | `inc/softpc.h`, `dpmi32/data.c`, `dpmi32/dpmidata.h`, `dpmi32/dpmi32p.h`, `dpmi32/dpmi32.c`, `dpmi32/buffer.c`, `dpmi32/int21map.c`, `dpmi32/dpmiint.c`, `dpmi32/xlathlp.h`, `dpmi32/dpmiselr.c` |
| MVDM-HOST-DIV-020 | Carry a `PREDIRCOMPLETE_INFO` and standard handles through the COMMAND AX:BX/BX:CX 32-bit register ABI. | The original x86 implementation exposes host pointers and native handles by splitting them into 16-bit words; neither value is valid on x64 or outside the host/session boundary. | Resolve/publish only opaque session host-resource identities at the original record/handle crossings, retain the original switch, register result order and cleanup point, then retire the record identity before its original free. | `dos/command/cmdredir.c`; `../adapter-mvdm-host-out/softpc/mvdm_command_redirection.[ch]` |
| MVDM-HOST-DIV-146 | Deliver a keyboard-controller output-port reset pulse to the selected CCPU40 executor. | The original non-CPU30 branch writes CCPU's private `cpu_interrupt_map` directly and calls the historical host wake hook. This composition intentionally keeps that private bitmap inside `c_main.c`. | Use the original public `cpu_interrupt(CPU_HW_RESET, 0)` entrypoint; it sets the same CCPU40 reset event and clears quick mode for the selected executor. | `softpc.new/base/keymouse/keyba.c` |
| MVDM-HOST-DIV-147 | Provide the historical shared SoftPC support definitions. | `xt.c`'s CCPU/non-CPU30 declaration block is an obsolete monitor-owned duplicate of CCPU40 register, descriptor and machine-status state; selected CCPU40 owns that state privately in `c_main.c`. | Retain the source file and every non-state support definition, but exclude only the obsolete CPU-state declaration block when `CPU_40_STYLE` is selected. | `softpc.new/base/support/xt.c` |
| MVDM-HOST-DIV-155 | Provide the historical non-CCPU parity lookup table. | In the selected CPU40 CCPU product, `c_main.c` owns `pf_table`; `xt.c`'s original monitor-path copy is a duplicate provider and has no separate CPU40 consumer. | Retain the original table for all other profiles but exclude only this duplicate table under `CPU_40_STYLE`. | `softpc.new/base/support/xt.c` |
| MVDM-HOST-DIV-177 | Translate a SIM32 protected-mode selector:offset into a numeric guest-linear address. | The original SIM32 body uses the CPU30 `DESCR/read_descriptor` carrier, while CPU40 keeps compatible descriptor decoding inside its execution core. | Keep real-mode arithmetic and the `-1` invalid-selector result. In protected mode, the adapter first uses the loaded CCPU segment, then the original CPU40 `selector_outside_GDT_LDT/read_descriptor_linear` descriptor path; an invalid selector still fails and no CPU30 monitor state is recreated. | `softpc.new/host/src/sim32.c`; `../adapter-mvdm-host-out/softpc/mvdm_softpc_effective_address.[ch]`; `softpc.new/base/ccpu386/c_bsic.c` |
| MVDM-HOST-DIV-178 | Let the original non-GUI WOW `CallBack16` body use its temporary caller/callback frame aliases. | Original `GETFRAMEPTR`/`FLUSHVDMPTR` expose raw FlatAddress process pointers that cannot be retained across the current adapter/session boundary or CCPU40 recursive simulation. | Preserve the original frame fields, task/stack order, flush-before-simulate point and return extraction. Replace only the fixed `VDMFRAME`/`CBVDMFRAME` aliases with bounded `adapter-mvdm-host-out/wow` leases that resolve through `adapter-mvdm-host-out/softpc`, commit before `host_simulate`, and reacquire after return. | `wow32/wcall16.c`; `../adapter-mvdm-host-out/wow/{include/wow_callback_frame_lease.h,wow_callback_frame_lease.c}`; `../adapter-mvdm-host-out/softpc/mvdm_softpc_guest_memory.c` |
| MVDM-HOST-DIV-179 | Include the original WOW32 precompiled-declaration cohort. | The original `base/mvdm` tree placed `winuserp.h` and `vdm.h` on a private include root; the split product deliberately keeps those declaration-only files in `opennt-abi` instead. | Replace only the two include spellings with the same-named byte-identical ABI carriers. This adds no USER/GDI, CSR, VDM-control or provider behavior. | `wow32/precomp.h`; `../opennt-abi/source/public/internal/{windows/inc/winuserp.h,base/inc/vdm.h}` |
| MVDM-HOST-DIV-180 | Keep the original WOW32 exports as the selected source provider. | The modern SDK's `wownt32.h` declares later public WOW32 exports with incompatible historical type modifiers before original source can define its matching NT4 bodies. | Suppress only that modern declaration carrier for this original WOW32 compilation cohort; no runtime API is removed or substituted. | `wow32/precomp.h` |
| MVDM-HOST-DIV-181 | Select original CCPU40 simulator stack access in WOW32. | Modern x86 headers set `_X86_` for host architecture, but original WOW32 uses that marker to select a native-VDM `pIntelRegisters` alias unavailable in the selected CCPU40 machine. | Undefine the host marker only before `wow32.h`, retaining the original `getSS`/`setSS` CCPU simulator stack contract on both x86 and x64. | `wow32/precomp.h`; `wow32/wow32.h`; `softpc.new/host/inc/host_cpu.h` |
| MVDM-HOST-DIV-150 | Select the original CPU40 recursive-emulation vector. | In the selected CPU40 CCPU profile, original `host_cpu.h` maps `host_simulate()` to `host_simulate_func`, while this historical host source also defines a direct function of that macro name. | Retain the direct body unchanged for non-CCPU builds and omit it only for CCPU, whose original initialization selects `c_cpu_simulate` through the vector. | `softpc.new/host/src/nt_cpu.c`; `softpc.new/host/src/nt_cprgs.c`; `softpc.new/host/inc/host_cpu.h` |
| MVDM-HOST-DIV-151 | Select the original generated-CCPU access-vector initializer for CPU40. | `nt_cprgs.c` historically gates that CCPU initializer on `CPU_30_STYLE`; without the retired macro, CPU40 falls into a different legacy register-state implementation that requires CPU30 globals. | Select the original CCPU initializer for either historical CPU30 or selected CPU40. CPU30 remains undefined and unlinked. | `softpc.new/host/src/nt_cprgs.c` |
| MVDM-HOST-DIV-182 | Install the selected generated-CCPU access vector at original CPU startup. | The historical CPU30 product shell supplied the post-CPU vector-installation step; the CPU40-only product deliberately excludes that shell, leaving the original `host_simulate_func` null even though DIV-151 selects its CCPU40 provider. | Immediately after the original `cpu_init()`, call the existing original `load_sw_cpu_access_functions()` only for selected CCPU40. This retains the original vector table, register accessors and `c_cpu_simulate` recursive entry; it adds no executor, adapter or guest behavior. | `softpc.new/base/support/main.c`; `softpc.new/host/src/nt_cprgs.c` |
| MVDM-HOST-DIV-152 | Publish the actual CPU40 CCPU return widths for control-state vectors. | Old declarations represent `c_getTS`, `c_getEM`, `c_getMP`, `c_getPE`, and `c_getIOPL` as unprototyped `word` providers, while selected CCPU bodies return `ISM32`. | Keep vector storage, names, and calls; give the five selected CCPU entries their exact no-argument `ISM32` contracts. | `softpc.new/host/src/nt_cprgs.c`; `softpc.new/host/inc/host_cpu.h`; `softpc.new/base/ccpu386/c_reg.c` |
| MVDM-HOST-DIV-153 | Preserve original BIOS BOP FE unsimulate entry. | CPU40 CCPU owns the actual `c_cpu_unsimulate` execution unwind, while original BIOS retains the historic `host_unsimulate` public spelling. | Publish only the original-name bridge to the selected CCPU provider; no alternate CPU loop or termination policy is added. | `softpc.new/host/src/nt_cpu.c`; `softpc.new/base/ccpu386/c_main.c`; `softpc.new/base/bios/bios.c` |
| MVDM-HOST-DIV-021 | Allocate, resolve and release original Redirector mailslot WORD handles through its private bitmap. | The guest-visible WORD must not encode a native pointer/HANDLE, and a second private handle-mapping implementation conflicts with the session mapping-manager rule. | Preserve the original WORD ABI, list ownership, errors and cleanup order while publishing the private mailslot record through the current session's host-resource mapping. The original private helper names remain; the adapter rejects zero and identifiers not representable as WORD. | `vdmredir/vrmslot.c`; `../adapter-mvdm-host-out/redir/include/mvdm_redirector_mailslot.h`; `../adapter-mvdm-host-out/redir/mvdm_redirector_mailslot.c` |
| MVDM-HOST-DIV-022 | Store and retrieve `PERRORDIALOGINFO` through the original NT4 dialog `DWL_USER` long slot. | The pointer is host-only but its original 32-bit cast truncates it on x64; no guest/component ABI receives this value. | Preserve the original dialog initialization and command-result order through a same-shaped adapter-win32 pointer-width dialog-context carrier. It intentionally does not use the mapping manager because the pointer never becomes an opaque external identity. | `softpc.new/host/src/nt_error.c`; `../adapter-mvdm-host-out/win32/include/dialog_context.h`; `../adapter-mvdm-host-out/win32/source/dialog_context.c` |
| MVDM-HOST-DIV-023 | Start the original `ErrorDialogBoxThread` through the public `CreateThread` call shape. | The selected NT4 source declares the worker as cdecl, but current x86 Win32 requires `LPTHREAD_START_ROUTINE`/`WINAPI`; the source is rejected before the original dialog code can be composed. | Preserve its parameter, return, lifetime and call ordering; apply only the two `WINAPI` declaration/definition annotations supplied by the registered NTVDMx64 `callconv.patch`. | `softpc.new/host/src/nt_error.c`; `../mvdm-softpc-patch/patches/minnt/callconv.patch` |
| MVDM-HOST-DIV-024 | Include the monitor `vdm.h` carrier required by original SoftPC idle-detection state. | The original relative include assumes the unsplit NT4 private-tree depth, which no longer exists after the owner-rooted mirror move. | Replace only that path with the same selected `vdm.h` declaration supplied through the declared ABI include path; source declarations and algorithm remain unchanged. | `softpc.new/base/system/idetect.c` |
| MVDM-HOST-DIV-025 | Represent a temporary native SoftPC address in `IHPE`, documented by the original source as being the same size as `IHP`. | The original `unsigned int` implementation is only 32 bits while `IHP` is a native pointer on x64; `nt_mem.c` truncates its own allocation/header addresses during original SAS initialization. | Preserve the original type spelling and all algorithms, but bind it to public Win32 `UINT_PTR`, which is pointer-sized on both x86 and x64. This is internal SoftPC storage, not a guest or component ABI identity; no mapping-manager token is appropriate. | `softpc.new/host/inc/insignia.h` |
| MVDM-HOST-DIV-069 | Retain the original aligned fixed-disk I/O scratch-buffer lifetime. | `nt_fdisk.c` stores a private `malloc` pointer in `DWORD`; x64 truncates it before the alignment calculation. | Keep the same lazy allocation and alignment arithmetic with `PBYTE` backing and `uintptr_t` calculation. This private host allocation never crosses a guest or component boundary, so it is not a mapping-manager identity. | `softpc.new/host/src/nt_fdisk.c` |
| MVDM-HOST-DIV-070 | Retain the original host CRT `fwrite` buffer mutation path. | Its private native buffer was unnecessarily round-tripped through `DWORD`, truncating it on x64. | Preserve the original direct buffer cast and write order without the fixed-width integer conversion. It remains a private native CRT buffer, not a guest or external identity. | `softpc.new/host/src/fprt.c` |
| MVDM-HOST-DIV-026 | Keep the CPU-40 SAS memory-span global consistent with its original `sas.h` declaration. | On x86, the implementation's `PHY_ADDR` and header's `IHPE` were both 32-bit by coincidence; after the required x64-safe `IHPE` correction they conflict. | Retain the original global spelling, usages and numeric meaning, but define it as the declared `IHPE`; its one floating-point consumer declaration is kept congruent. No guest or component ABI changes. | `softpc.new/base/ccpu386/ccpusas4.c`, `softpc.new/base/ccpu386/zfrsrvd.c` |
| MVDM-HOST-DIV-027 | Call the existing SoftPC `host_sas_init(sys_addr)` allocator during CCPU SAS setup. | The original x86 compiler accepted the missing function prototype and therefore happened to preserve the 32-bit pointer return; modern x64 C treats it as `int` and truncates the allocated backing address. | Add only the exact existing `UTINY *` declaration; implementation, parameter, return value and source control flow are unchanged. | `softpc.new/base/ccpu386/ccpusas4.c` |
| MVDM-HOST-DIV-028 | Compute page ranges covering native SoftPC allocation-header addresses. | The original 32-bit `IU32` page counters truncate the high bits of host virtual addresses on x64 before reconstructing the `VirtualAlloc` commit address. | Keep the original shift/range/commit algorithm and guest-size result, but retain temporary native page identities in the existing pointer-sized `IHPE` type. This is not a guest address or mapping-manager identity. | `softpc.new/host/src/nt_mem.c` |
| MVDM-HOST-DIV-029 | Make `check_malloc` call the existing host `malloc` contract for SoftPC allocations such as the CCPU FPU stack. | The historical `<memory.h>` incidentally declared `malloc`; current MSVC declares it in `<stdlib.h>`, otherwise C treats the pointer result as `int` and truncates it on x64. | Add the modern declaration header only; `host_malloc` spelling, allocator, failure loop and all callers remain original. | `softpc.new/base/inc/ckmalloc.h` |
| MVDM-HOST-DIV-030 | Provide the original `host_start_cpu`/`host_simulate` source in a CCPU static link. | `nt_cpu.c` carries two host memory globals that duplicate CCPU SAS definitions; they are not used in its CCPU branch and make the original host wrapper un-linkable beside `ccpusas4.c`. `host_delays` is independent controller timing state and remains required by the original FDC/WD2010 callers. | Retain `Start_of_M_area` and `Length_of_M_area` for every non-CCPU build; exclude only those duplicate definitions under `CCPU`, while preserving original `host_delays` in every profile. The original wrapper names, parameters and bodies are unchanged. | `softpc.new/host/src/nt_cpu.c` |
| MVDM-HOST-DIV-031 | Link the historical i386 SoftPC product aggregate with `monitor.lib`. | The selected `i386 + CCPU` profile must physically exclude the NT4 kernel-V86 monitor; retaining it in the product manifest would permit an accidental second executor. | Remove only the `monitor.lib` line; retain all other original library order and record the byte-exact monitor package under `docs/etc/legacy_code`. | `softpc.new/obj.vdm/sources`; `../../docs/etc/legacy_code/mvdm-host/v86/monitor/` |
| MVDM-HOST-DIV-032 | Declare the original `host_time` seconds interface through `time_t`. | The NT4 source build made `time_t` a 32-bit `long`; modern x64 widens it, while the unchanged implementation and reached callers retain the historical 32-bit seconds ABI. | Preserve the original spelling, parameter count and seconds result, but declare the actual historical `long` ABI explicitly. | `softpc.new/base/inc/timeval.h` |
| MVDM-HOST-DIV-033 | Read CRT system-error text from `_sys_errlist`/`_sys_nerr`. | Modern UCRT removed those exported storage symbols. | Preserve `host_get_system_error`'s original `char *` text contract with public `strerror`; only the unavailable CRT-storage access changes. | `softpc.new/host/src/nt_unix.c` |
| MVDM-HOST-DIV-034 | Convert between the original `ULONG` Intel-linear allocation result and `nt_mem.c`'s private native backing allocation. | The original x86 source truncates `intelMem` before local address subtraction/reconstruction; that corrupts the internal allocation-header lookup on x64. Page-span temporaries derived from native addresses also need an explicit return to the original fixed-width allocation-size ABI. | Retain the original public `ULONG` Intel address ABI and allocation ordering, but use the pre-existing pointer-sized `IHPE` only for local base arithmetic. Assert the source invariant that a page span lies in the original `IU32 MaxIntelMemorySize` allocation before explicitly returning it as `IU32`. No guest value, component ABI, mapping-manager identity, or external host pointer is changed. | `softpc.new/host/src/nt_mem.c` |
| MVDM-HOST-DIV-035 | Add/remove a host-backed external page range with `VdmAddVirtualMemory` / `VdmRemoveVirtualMemory`. | The original `ULONG HostAddress` is a native pointer on NT4 and truncates on x64; the current product must use the same stable 32-bit host-object identity on x86 and x64. | Preserve the original public function name, argument widths, allocation/cache/bind/remove ordering and status direction. Replace only the host-pointer alignment and physical-binding argument with the current session guest-memory mapping-manager surrogate; adapter failure returns the original `NTSTATUS` form before guest state changes. | `softpc.new/host/src/nt_mem.c`; `../adapter-mvdm-host-out/softpc/{include/mvdm_softpc_physical_mapping.h,mvdm_softpc_physical_mapping.c}` |
| MVDM-HOST-DIV-036 | Resolve an Intel physical byte to CCPU backing memory. | The original CCPU assumes every physical address is an offset in one native `Start_of_M_area` block; an admitted external host-page span must instead remain live through its session-owned mapping identity. | Add one selector-blind adapter probe before the unchanged original RAM calculation. The adapter returns a native pointer only to this synchronous CCPU access path after validating the owning session, mapping identity, span and lifetime; otherwise the original RAM path runs unchanged. | `softpc.new/base/ccpu386/ccpusas4.c`; `../adapter-mvdm-host-out/softpc/{include/mvdm_softpc_physical_mapping.h,mvdm_softpc_physical_mapping.c}` |
| MVDM-HOST-DIV-037 | Locate original SoftPC ROM/CMOS resources through `host_find_file`. | The original body hardcodes the NT system directory, which is neither the project firmware package nor a portable product installation boundary. | Preserve the original function name, ANSI parameters and `host_error` failure order; replace only its system-directory lookup with the session-owned configured firmware-root adapter. | `softpc.new/host/src/nt_unix.c`; `../adapter-mvdm-host-out/softpc/mvdm_softpc_firmware.c`; `../session/session.[ch]` |
| MVDM-HOST-DIV-038 | Load the original `ntio.sys` image in `InitialiseDosEmulation`. | The original body hardcodes the NT system directory. The current product stages immutable MVDM system media beside its executable rather than requiring an installed NT4 system file. | Preserve original file validation, `CreateFile`/`ReadFile`, SAS write, virtual-interrupt restore and `CS:IP` entry ordering; replace only directory discovery with the session-owned MVDM-system adapter. A controlled termination that returns now returns `-1`, matching the original terminal failure direction. | `softpc.new/host/src/nt_msscs.c`; `../adapter-mvdm-host-out/softpc/mvdm_softpc_firmware.c`; `../app/package_layout.[ch]`; `../session/session.[ch]` |
| MVDM-HOST-DIV-039 | Initialize the fixed virtual-interrupt state for the selected x86 guest profile. | Historical `#ifndef i386` selected an MIPS VDM branch. MIPS is outside the product profile; a host architecture macro must not decide guest-machine state. | Reduce the mirror body to its original x86 bit-clearing transition on both supported hosts. No product-wide build defines `i386`; any future x86-only source exception must be target-local and registered. | `softpc.new/host/src/nt_msscs.c`; `../../tools/build/New-T310OriginalSoftpcNinja.ps1` |
| MVDM-HOST-DIV-040 | Terminate the original SoftPC heartbeat at its alertable wait. | NT4 `NtAlertThread` wakes the wait with `STATUS_ALERTED`; the public modern same-shaped adapter uses an APC, which produces `STATUS_USER_APC`. | Preserve the original termination block and additionally accept `STATUS_USER_APC` as the same terminal heartbeat signal; no timer, PIC, RTC or session policy moves into the mirror. | `softpc.new/host/src/nt_timer.c`; `../adapter-mvdm-host-out/win32/source/nt_thread_alert_compat.c` |
| MVDM-HOST-DIV-041 | Carry C-video generated rule operands through the original `IUH` host-word slots. | The original generated `SINIT*`/`SEVID*` C rule bodies use `IUH` for host-word values, including private native SoftPC pointers; its historical `unsigned int` spelling truncates those values on x64. | Only the generated rule translation units select `CVIDC_RULE_WORD`, making `IUH` the original pointer-sized host-word intent (`UINT_PTR`) on both x86 and x64. Other SoftPC/CCPU units retain the original 32-bit spelling. This is private controller state, not a guest or component ABI identity. | `softpc.new/host/inc/insignia.h`; `../../tools/build/New-T310OriginalSoftpcNinja.ps1` |
| MVDM-HOST-DIV-043 | Publish the original EGA BIOS chain-write callback globals. | `cga.c` used old-style unprototyped global function pointers while the same original interface is already explicitly two-`ULONG` in `video.h`; x64 then loses the checked callback contract when EGA routines are assigned. | Preserve names, storage and call order, changing only the two global declarations to the existing `video.h` `IPT2` shape. | `softpc.new/base/video/cga.c`; original declaration evidence `softpc.new/base/inc/video.h` |
| MVDM-HOST-DIV-044 | Preserve the original NTVDM host-code selection without implicitly selecting a second machine profile. | The original common host header defines `CPU_30_STYLE`, which selects the separate NT4 kernel-VDM V86-monitor contract. The product admits only the original CCPU40 software-executor profile, so a shared header must not re-enable the retired route. | Retain `NTVDM` and the original remaining host declarations; remove only the `CPU_30_STYLE` definition and document the exclusion at its former insertion point. `CPU_40_STYLE` remains the sole build-manifest selection. | `softpc.new/host/inc/host_def.h`; `../../tools/build/New-T313CcpuLifecycleNinja.ps1`; `../../tools/build/New-T310OriginalSoftpcNinja.ps1` |
| MVDM-HOST-DIV-146 | Let the historical ICA header publish its non-CCPU interrupt-map bit macros. | The original combined CCPU40 build also defined `CPU_30_STYLE`, using it incidentally to suppress `CPU_HW_INT` before CCPU's distinct event enum. The product no longer defines the retired CPU30 marker. | Preserve the original non-CCPU branch and prevent the ICA macro only when the original `CCPU` executor is selected; CCPU's original event enum remains authoritative. | `softpc.new/base/inc/ica.h` |
| MVDM-HOST-DIV-055 | Store the selected SAS physical-memory capacity. | The original CPU_40 declaration used the host-sized `IHPE` alias, although the value is produced from a physical-address input and returned through the fixed-width `TYPE_sas_memory_size` interface. On x64 that silently widens a guest capacity and narrows it on return. | Preserve all source control flow while declaring the capacity as `PHY_ADDR`; it remains a guest physical extent and never a host pointer or mapping token. | `softpc.new/base/inc/sas.h`, `softpc.new/base/ccpu386/ccpusas4.c` |
| MVDM-HOST-DIV-056 | Select the original x86 guest memory-size calculation. | The historical `#ifdef i386` branches on a host compiler macro; its other branch belongs to the NT RISC VDM product, not a distinct guest architecture. | Retain the original x86 calculation unconditionally for both supported host targets.  No build defines `i386` globally, and host ABI cannot select guest-machine semantics. | `softpc.new/host/src/config.c` |
| MVDM-HOST-DIV-057 | Declare the original `host_simulate` controller re-entry form. | Non-MONITOR sources select `cpu4.h`, which omits the declaration although original keyboard, mouse, BIOS and storage code call the same `void` entrypoint.  Modern C otherwise assumes an `int` result and loses the call ABI. | The shared original host declaration carrier publishes only `void host_simulate(void)`; selected SoftPC/CCPU implementation and all call order remain unchanged. | `softpc.new/host/inc/host_def.h` |
| MVDM-HOST-DIV-058 | Carry mouse range endpoints from the original INT 33h state to the host range notifier. | The caller's `MOUSE_SCALAR *` endpoints and the host declarations' `word *` parameters had equal NT4 width but incompatible signed scalar pointer types; modern MSVC correctly rejects the call shape. | Preserve the four-pointer API, call order and implementation.  Declare both caller and host definitions with the actual `MOUSE_SCALAR *` endpoint type. | `softpc.new/base/keymouse/mouse_io.c`, `softpc.new/host/src/nt_mouse.c` |
| MVDM-HOST-DIV-059 | Read the NTIO keyboard vector table and stream-buffer cells through original SoftPC SAS backing memory. | The original x86 build treated fixed-width `effective_addr` results as host pointers; x64 truncates those values before the cast. | Retain the original table order and all `effective_addr` calculations, but combine each physical number with the existing native-width `Start_of_M_area` backing pointer.  No new mapping manager or device shim is created. | `softpc.new/base/keymouse/keybd_io.c` |
| MVDM-HOST-DIV-060 | Retain the unprovided `Sas_overwrite_memory` member in the original generated CCPU SAS vector. | The source-era generic K&R null-function-pointer cast is incompatible with the selected generated two-argument slot on modern x86/x64 C compilers. | Keep the original unavailable null slot and vector order, but spell it as the existing generated `TYPE_sas_overwrite_memory *` null type.  No provider, wrapper, mapping or behavior is added. | `softpc.new/base/ccpu386/sasCdef.c` |
| MVDM-HOST-DIV-061 | Preserve the original EMM host-storage ID contract on x86 and x64. | The NT4 implementation stored private `calloc`/`realloc` pointers in `long`; that truncates on x64.  The source already models IDs as host-dependent and requires access only through `USEBLOCK`/`FORGETBLOCK`. | Use the existing native-width `IHP` for the private storage-ID declarations, functions and table; retain all allocation, mapping and lock/unlock calls.  Add the standard allocator declaration where NT4 selected it only for MONITOR.  This is neither a guest address nor an external identity, so it deliberately does not use the session mapping manager. | `softpc.new/base/dos/emm_mngr.c`, `softpc.new/base/inc/emm.h`, `softpc.new/host/inc/host_emm.h`, `softpc.new/host/src/nt_emm.c`, `softpc.new/host/src/x86_emm.c` |
| MVDM-HOST-DIV-062 | Preserve the selected C-video callback and generated-global contracts. | NT4 represented callbacks as generic data pointers and assigned generated `VGAGLOBALSETTINGS` to the non-generated `VGA_GLOBALS` view, which current C cannot validate. | Derive the four callback shapes from the original `ga_mark.h`/C-video tables, publish the original BIOS chain-write `ULONG` ABI, and select `VGAGLOBALSETTINGS` only for the original `C_VID` profile. The x86 product compiles the original generated table sources directly; non-C-video source keeps its original `VGA_GLOBALS` view. | `softpc.new/base/cvidc/evidgen.h`, `softpc.new/base/cvidc/ev_glue.c`, `softpc.new/base/inc/egacpu.h` |
| MVDM-HOST-DIV-075 | Publish the original generated C-video runtime-error call contract. | All selected generated `SEVID*.c` bodies call `CrulesRuntimeError`, whose existing original definition is in `ev_glue.c`; the historical generated support header omitted a declaration, producing implicit-`int` calls. | Add only the exact `void (char *)` declaration to the shared original generated support header. No generated body, diagnostic behavior, adapter or mapping-manager route is changed. | `softpc.new/base/cvidc/j_c_lang.h` |
| MVDM-HOST-DIV-076 | Retain exact native C variadic call contracts at selected original SoftPC call sites. | The NT4 compiler accepted implicit `printf`/`sprintf` declarations. On x64, a variadic call must see its real declaration; the original host already supplies the selected `printf` provider and `sprintf` remains a CRT API. | Add the standard C declaration header at each reached original call site only. No custom provider, adapter, format change, mapping-manager use or warning suppression is added. | `softpc.new/base/ccpu386/fpu.c`, `softpc.new/base/ccpu386/popf.c`, `softpc.new/base/ccpu386/ntstubs.c`, `softpc.new/base/cvidc/j_c_lang.c`, `softpc.new/base/cvidc/ev_glue.c`, `softpc.new/host/src/nt_mem.c` |
| MVDM-HOST-DIV-063 | Preserve the original VDD/UMB `PVOID` public argument shape while consuming it as a guest physical address. | NT4 carried the VDD linear address through `PVOID` because guest and host address widths coincided; direct `DWORD` casts truncate a host-width value on x64. Kernel VDM also supplied a host virtual fault pointer, a route which this user-mode SoftPC product physically excludes. | Reuse the existing private UMB numeric codec at every page-alignment, allocation and callback boundary. The selected user-mode page-fault contract now carries an Intel linear address directly; no host pointer is recovered or passed to the VDD callback. | `softpc.new/host/src/nt_msscs.c`; `../mvdm-host-overlay/softpc.new/host/src/mvdm_umb_address.[ch]` |
| MVDM-HOST-DIV-064 | Align the original four-byte SoftPC fill engine through a native host address. | NT4 used the same 32-bit `unsigned int` carrier for the fill datum and for private backing-pointer alignment; on x64 the latter truncates before alignment. | Keep the original 32-bit fill datum and branch order. Use `uintptr_t` only for private address alignment and `size_t` only for the locally derived count; retain the original in-source word-fill loop instead of introducing a provider or mapping identity. | `softpc.new/host/src/copy_fnc.c` |
| MVDM-HOST-DIV-140 | Publish the original `host_getenv` native pointer result. | The portable SoftPC host header aliases `host_getenv` to the C runtime `getenv`, but the selected CCPU source has no standard declaration in scope. Modern C then assumes an `int` result, truncating the returned host pointer on x64. | Add only the standard C runtime declaration in the original host portability header. The alias, call sites and environment-variable semantics remain unchanged; this private host pointer is not a mapping-manager identity. | `softpc.new/host/inc/host_def.h` |
| MVDM-HOST-DIV-071 | Publish the original CCPU EFLAGS accessor. | `c_bsic.c` defines `c_getEFLAGS`, but its own selected internal header omits the declaration. Callers otherwise use an implicit host `int` return, which is not the source's fixed 32-bit EFLAGS contract. | Add only the exact `IU32 c_getEFLAGS(void)` declaration to the same CCPU header; no caller ordering or flag semantics change. | `softpc.new/base/ccpu386/c_bsic.h` |
| MVDM-HOST-DIV-072 | Publish selected same-package CCPU call contracts. | The original CCPU executor relied on implicit declarations for its existing address, segment, PIC, timer, debug and NPX bodies. Modern C defaults those calls to a host `int`, which is unsafe for fixed-width results and void call contracts. | Include the existing owner headers and declare only the two ownerless same-package `void` functions. The called bodies, call order, CPU state and device semantics are unchanged. | `softpc.new/base/ccpu386/c_main.c` |
| MVDM-HOST-DIV-126 | Publish CCPU `note_486_instruction` contract at the reached `POPFD` caller. | The original same-package function is defined in `c_main.c`, but `popf.c` relied on implicit-int declaration. Modern C would infer a host `int` result despite the original diagnostic being `void`. | Declare the original `void(char *)` signature at the reached caller; it neither changes the 486 diagnostic policy nor changes CPU state. | `softpc.new/base/ccpu386/popf.c` |
| MVDM-HOST-DIV-073 | Pass the CCPU TLS jump frames with the original frame identity. | The helper functions return `jmp_buf *`, but historical C accepted that pointer where modern MSVC requires the referred-to `jmp_buf` array. The old form has an incompatible pointer level on both targets. | Dereference only at the three `setjmp` calls. The same TLS frame, exception path, simulation nesting and long-jump target remain unchanged. | `softpc.new/base/ccpu386/c_main.c` |
| MVDM-HOST-DIV-074 | Declare the selected EVID word-fill glue callback in the original EGA write handler table. | The original declaration says `IU8` while both `fill_word_ev_glue` and the selected `MEM_HANDLERS` slot use `IU16`; historical unprototyped table initialization masked the mismatch, but modern x64 correctly diagnoses an unsafe function-pointer ABI. | Change only that forward declaration to `IU16`, matching the existing original glue body and handler-slot contract. The function, table order and video behavior remain unchanged. | `softpc.new/base/video/ega_writ.c` |
| MVDM-HOST-DIV-141 | Read original `config_inquire` scalar options used by firmware, controllers and display initialization. | The original `void *` result carries either a real object pointer or a small configuration enum; direct casts from that carrier to 16/32-bit scalars truncate a native pointer representation on x64. | Retain `config_inquire` and all original option/result destinations; only scalar consumers first convert through `ULONG_PTR`, then perform their original narrow enum conversion. These values remain private host configuration, not guest or external identities. | `softpc.new/base/{bios/emm_fncs.c,system/rom.c,system/cmos.c,system/cmosnt.c,keymouse/keyba.c,video/video.c}`; `softpc.new/host/src/nt_graph.c` |
| MVDM-HOST-DIV-142 | Size and advance the original DOS ROM scratch-pad message. | `strlen` returns native `size_t`; original assignment to `IU32` can truncate before the existing single-page/scratch-pad guard on x64. | Retain the original guard, input truncation, byte write, `$`/NUL termination and final 32-bit `cur_loc`; use native-width temporary length/end values, and convert only after the guard has bounded the result to the ROM scratch range. | `softpc.new/base/system/rom.c` |
| MVDM-HOST-DIV-077 | Process the original XMS move descriptor and forward-copy its guest byte range. | The original 32-bit process model obtains `SS:BP` and the source/destination linear values as raw `PBYTE` aliases, then truncates those aliases through `ULONG`/`DWORD`. Neither architecture may use a host pointer as an Intel address. | Retain the original descriptor order (`destination`, `source`, word count), overflow rule, forward copy and AX=1 success result. A same-shaped SoftPC adapter obtains bounded session guest-memory leases for the numeric locations. It requests a controlled session cancellation for an unrepresentable descriptor/range instead of inventing a guest XMS error code. | `xms.486/xmsblock.c`; `../adapter-mvdm-host-out/softpc/include/mvdm_xms_memory.h`; `../adapter-mvdm-host-out/softpc/mvdm_xms_memory.c` |
| MVDM-HOST-DIV-078 | Convert original XMS UMB allocation/release `PVOID` values into paragraph segments. | The selected user-mode `ReserveUMB`/release contract preserves the historical `PVOID` spelling while carrying an Intel physical number. Direct `(DWORD)Address` treats that number as a host pointer and truncates on x64. | Reuse the existing UMB numeric-address codec before the original paragraph conversion. The linked-list order, ownership flags, merge behavior and original public API stay unchanged; an invalid encoded number returns without creating a host pointer. | `xms.486/xmsumb.c`; `../mvdm-host-overlay/softpc.new/host/src/mvdm_umb_address.[ch]` |
| MVDM-HOST-DIV-079 | Select the original CPU_40 C-video EVID string route instead of an unselected legacy `WRT_POINTERS` route. | The historical non-C-video WRT tables carry native string callback pointers through `ULONG`; on x64 those table initializers truncate pointers. The selected `CPU_40_STYLE` profile uses the original EVID tables instead. | Keep the original direct BIOS byte/word callbacks and table storage, but compile the uninstalled direct string move/copy helpers only outside `CPU_40_STYLE`. The selected EVID source and its table contract remain unchanged. | `softpc.new/base/video/egawrtm0.c` |
| MVDM-HOST-DIV-080 | The selected CCPU host facade and exception source rely on existing CCPU/Yoda providers whose exact declarations are hidden by historical profile macros or absent from the include list; the exception macro also redeclares the already-mapped CRT environment query with a mismatched parameter qualifier. | Implicit declarations infer an incorrect integer result on x86/x64, while the duplicate `getenv` declaration conflicts with the native pointer contract. | Declare the original `c_main.c` / `ccpusas4.c` contracts in the host facade, include the original Yoda diagnostic interface, and use the existing `host_def.h` CRT environment declaration as the sole contract. Every original call and provider remains. | `softpc.new/base/ccpu386/ntstubs.c`; `softpc.new/base/ccpu386/c_xcptn.c` |
| MVDM-HOST-DIV-143 | Select the original CPU_40 C-video EVID mode-1/mode-2 string route and retain native alignment values at pointer width. | Historical mode-1/mode-2 WRT string callbacks are uninstalled under `CPU_40_STYLE` yet carry pointers through `ULONG`; `fill_both_bytes` also used `ULONG` merely to test and adjust a private native buffer pointer. | Compile the uninstalled WRT string helpers only outside `CPU_40_STYLE`; preserve selected EVID bodies and direct byte/word callbacks. Use `IHPE` only for the private alignment test and byte adjustment, without introducing a guest-address mapping. | `softpc.new/base/video/egwrtm12.c` |
| MVDM-HOST-DIV-081 | Publish the original `RtlFillMemoryUlong` NTDLL call contract. | The selected modern header subset does not declare the historical routine, so modern C assumes an `int` result and cannot validate its native pointer argument on either host architecture. | Add only the exact `NTAPI` declaration at the original use site's include boundary. The original private backing-buffer call, byte-count calculation and fill behavior remain unchanged; no guest address or resource identity crosses this contract. | `softpc.new/host/src/copy_fnc.c` |
| MVDM-HOST-DIV-082 | Publish original CCPU FPU/address/debug and CRT call contracts. | The selected source package has existing `c_addr.h`, `fpu.c`, `yoda.h` and CRT declarations, but three translation units omit the declaration/import selected by their original calls. Modern C then compiles those calls as unchecked implicit forms. | Include only the original owning headers, retain the exact original `FLDENV` declaration shape, and include the standard CRT declaration. `yoda.h` retains the original `PROD` no-op `check_D` choice. No CPU/FPU/memory logic, mapping identity or provider is added. | `softpc.new/base/ccpu386/{zfrsrvd.c,c_page.c,c_reg.c}` |
| MVDM-HOST-DIV-083 | The original SoftPC support startup sequence calls selected `nt_msscs`, `nt_cpu`, and mouse providers without a common declaration carrier; the `CPU_40_STYLE` guard hid one historical declaration. | The selected CCPU40 support call site otherwise lacks the exact provider declarations. | Publish the exact existing `void` / `int` contracts at the original support call boundary. `nt_msscs.c` retains its original implicit-result spelling. Startup order, provider bodies, machine state, return values, and guest behavior remain unchanged. | `softpc.new/base/support/main.c`; `softpc.new/host/src/nt_msscs.c` |
| MVDM-HOST-DIV-084 | The original `nt_msscs` initialization path invokes selected PIF and EMS/DOS providers without including the existing PIF declaration or having a public LIM page-frame declaration carrier. | Their implicit result declarations are invalid on x86/x64. | Include the original `nt_pif.h` provider declaration and publish the exact original `emm_mngr.c` boolean/LIM configuration contract at the machine-initialization use site. No PIF/EMS logic, mapping, or startup order changes. | `softpc.new/host/src/nt_msscs.c` |
| MVDM-HOST-DIV-092 | Publish the selected CPU_40 direct SAS overwrite call. | The original `sas.h` moves to a generated function-vector contract under `CPU_40_STYLE`, but `sas_overwrite_memory` remains a direct CCPU physical-range invalidation function and was omitted from that generated declaration set. Modern C consequently inferred an `int` result at original host call sites. | Add the exact existing `void (PHY_ADDR, PHY_ADDR)` declaration adjacent to the selected generated SAS declarations. The CCPU provider, physical-range semantics and call order remain unchanged. | `softpc.new/base/inc/sas.h` |
| MVDM-HOST-DIV-093 | Format the original private host allocation pointer safely. | The original `nt_mem` diagnostic prints native `IHP` storage with a 32-bit `%lx` format, which is invalid on x64. | Retain the failure path and all diagnostic values, but pass `commitAddr` as a native `%p` pointer. No allocation, guest address, mapping-manager or memory semantics change. | `softpc.new/host/src/nt_mem.c` |
| MVDM-HOST-DIV-095 | Format the retained `sas_manage_xms` stub diagnostic using its actual argument widths. | The original stub reports a native start pointer with `%lx`; x64 cannot pass that pointer through a 32-bit variadic conversion. | Retain the diagnostic and stub result; use `%p` for the private pointer and scalar conversions for its `ULONG` and `INT` arguments. No XMS/SAS behavior or mapping route changes. | `softpc.new/host/src/stubs.c` |
| MVDM-HOST-DIV-096 | Invoke the original EMS page-frame mapping services with a declared status/argument ABI. | `VdmMapDosMemory` and `VdmUnmapDosMemory` were NT4 kernel-VDM services; their user-mode source calls otherwise compile as implicit `int` on both host architectures. | Retain original page-number calculations, calls, status test, and failure result. Add only the source-shaped adapter declaration/import; the adapter implements a session-owned physical-page alias without host pointers. | `softpc.new/host/src/nt_emm.c`; `../adapter-mvdm-host-out/softpc/include/mvdm_softpc_physical_mapping.h` |
| MVDM-HOST-DIV-097 | Resolve the original EMS page-frame physical alias before normal CCPU/SAS backing lookup. | NT4 kernel VDM performed this physical remap outside CCPU; without it a selected SoftPC build sees the frame address rather than the EMS backing page. | Preserve normal/external SAS lookup. Add one numeric, selector-blind translation probe owned by the same adapter; it neither exposes a host pointer nor changes CCPU instruction semantics. | `softpc.new/base/ccpu386/ccpusas4.c`; `../adapter-mvdm-host-out/softpc/{include/mvdm_softpc_physical_mapping.h,mvdm_softpc_physical_mapping.c}` |
| MVDM-HOST-DIV-085 | Selected BIOS tape and keyboard controller paths invoke the original XMS A20 providers but their inherited SAS conditional declaration is not effective in the selected compilation units. | Implicit host-int calls are invalid x86/x64 contracts. | Include the original XMS provider header at the two original call boundaries; retain the same A20 call ordering and original `xmsa20.c` implementation. | `softpc.new/base/keymouse/keyba.c`; `softpc.new/base/bios/tape_io.c` |
| MVDM-HOST-DIV-068 | Carry original serial/parallel controller ioctl arguments and worker context. | NT4 used `long` and direct `void*` casts for either scalar device values or host-local output pointers; those representations diverge on x64. | Retain the original request values, COM/LPT configuration behavior, worker order and result writes. Use `intptr_t` for the internal ioctl transport, `ULONG_PTR` only to read the original pointer-encoded scalar configuration values, and a native scalar conversion for the worker adapter index. The transport is never guest-visible and uses no mapping manager. | `softpc.new/base/inc/host_com.h`, `softpc.new/base/comms/com.c`, `softpc.new/host/src/nt_com.c`, `softpc.new/host/src/nt_lpt.c` |
| MVDM-HOST-DIV-065 | Preserve the original per-thread direct-access error suppression mask. | NT4 transported its category bitset through a TLS `LPVOID` by narrowing it to `DWORD`; that truncates the native TLS slot representation on x64. | Keep the original category bits, duplicate-prompt test and dialog sequence, but carry the private mask as `ULONG_PTR` and form its bit with `ULONG_PTR`. This is private host state, not a guest identity, so it does not use the mapping manager. | `softpc.new/host/src/nt_error.c` |
| MVDM-HOST-DIV-066 | Preserve original error-dialog worker sequencing under current Win32 callback contracts. | SoftPC headers redefine `BOOL` as `UINT` and original NT4 code moved `HWND`, dialog data and `DialogBoxParam` results through `DWORD`/`LONG`; those contracts are not pointer-width safe and do not match current public callback typedefs. | Retain the original worker, enumeration and dialog order, while using native `HWND`, `LPARAM`, `INT_PTR` and `LRESULT`; spell the `EnumWindows` callback with the public signed `BOOL` representation (`int`) because the local SoftPC `BOOL` macro is incompatible. | `softpc.new/host/src/nt_error.c` |
| MVDM-HOST-DIV-046 | Declare the selected CCPU flag-read and segment-load callbacks consistently across the generated CCPU declaration carrier and `nt_cprgs` selection table. | Original NT4 C builds accepted unprototyped function-pointer assignments; the generated header incorrectly uses `IBOOL`/`IUH` where `c_reg.c` defines `ISM32`. Modern MSVC correctly diagnoses the mismatch. | Keep the original CCPU function names, parameters, results and initialization order. Correct only the selected generated declarations and CCPU table slots to their actual `ISM32` contracts; alternate CPU selections retain their own original declarations. | `softpc.new/base/ccpu386/cpu4gen.h`, `softpc.new/host/src/nt_cprgs.c` |
| MVDM-HOST-DIV-047 | Bind every selected CCPU register/flag setter through the original SoftPC access table. | The original generic table uses old-style empty parameter lists, so NT4 C accepted setter assignments without proving the passed value width. | Preserve original callbacks and initialization order; give only the selected CCPU slots their existing generated `IU8`, `IU16` or `IBOOL` parameter shapes. Non-CCPU profiles retain their own original declarations. | `softpc.new/host/src/nt_cprgs.c` |
| MVDM-HOST-DIV-048 | Store generated CCPU/CVIDC GDP fields in a fixed 32-bit offset byte area, then expose selected fields to the host debug/control carrier through hard-coded offsets. | The selected generated GDP contains native pointers beside 32-bit Intel values. Widening its base word overlaps later fixed offsets; retaining it truncates native pointers on x64. The original non-PROD `GdpOffsetFromName` assertion depends on an undeclared NT4 packed-GDP debug helper and cannot validate a native-width carrier. | The exact selected GDP macro spellings, types and source ordering are generated into a private overlay with per-field native-width storage. The three reached mirror consumers include that overlay; `nt_cpu.c` uses corresponding source-named fields and omits only the invalid packed-offset assertion. GDP remains private SoftPC state and does not use the session mapping manager. | `softpc.new/base/ccpu386/cpu4gen.h`, `softpc.new/base/ccpu386/vglob.c`, `softpc.new/base/cvidc/ev_glue.c`, `softpc.new/host/src/nt_cpu.c`; `../mvdm-host-overlay/softpc.new/base/cvidc/mvdm_gdp_state.[ch]`; `../../tools/build/Generate-T310GdpSlots.mjs` |
| MVDM-HOST-DIV-051 | Store six selected CCPU generated video-rule entries as 32-bit `IU32*` values. | They are private host-code addresses; NT4's 32-bit representation truncates them on x64 and conflicts with the unchanged original `IUH*` CCPU accessor ABI. | Change only these storage fields to native-width `IUH*`; `vglob.c` and `egacpu.h` keep their original names, signatures, ordering and access semantics. They are not guest identities and do not use the session mapping manager. | `softpc.new/base/ccpu386/evid_c.h` |
| MVDM-HOST-DIV-049 | Carry UMB physical address values through the original `PVOID` Reserve/Release/VDD API spelling. | The selected non-MONITOR SoftPC path uses these values as 32-bit guest physical numbers; x64 casts falsely treat them as truncatable native pointers. | The matching overlay encodes/decodes only representable `DWORD` numeric values, preserving the original API shape and error direction.  It does not map, allocate or expose a host pointer; SAS remains the owner of physical backing. | `softpc.new/host/src/nt_umb.c`; `../mvdm-host-overlay/softpc.new/host/src/mvdm_umb_address.[ch]` |
| MVDM-HOST-DIV-050 | Keep original timer and Redirector-to-PIC public forms while entering their selected typed NT/SoftPC contracts. | NT4 C accepted `IS32*` as `PULONG` and `int*` as `LONG*`; modern builds correctly reject the mismatches even though both carriers were historically 32-bit. | The timer uses a local `ULONG` remainder then preserves the bounded microsecond result in `IS32`; an overlay bridges the exported `SoftPcEoi(int,int*)` form to `ica_eoi(ULONG,LONG*,int)` without changing PIC ordering. | `softpc.new/host/src/nt_timer.c`, `softpc.new/host/src/nt_eoi.c`; `../mvdm-host-overlay/softpc.new/host/src/mvdm_ica_eoi_bridge.[ch]` |
| MVDM-HOST-DIV-147 | Preserve selected VDM terminal paths without ending the application process. | NT4 owned a dedicated `ntvdm.exe`, so reset/argument and CCPU40 SAS-allocation failures could call `ExitProcess` after their historical product-shell handling; that process exit also destroyed live SoftPC workers. The app owns the current process and must not die with one session. | Retain each original caller's cleanup/order and failure return where applicable, replacing only process exit with the same exit-code transfer to a bound session escape. In `nt_reset.c`, the retained `host_applClose` cohort is once-per-original-host-start and is entered before any selected direct `host_terminate` escape, so its original heartbeat/event/device close order remains intact. `nt_event.c` has been restored to its original console-control form and is not covered by this divergence. | `softpc.new/host/src/{nt_reset.c,sim32.c}`; `../adapter-mvdm-host-out/softpc/mvdm_softpc_termination.c` |
| MVDM-HOST-DIV-148 | Compile the original LIM byte/word/string writeback bodies needed when CCPU40 encounters `SAS_MM_LIM`. | `host_def.h` selects `LIM`, so the selected original CCPU calls `LIM_b_write`/`LIM_w_write`; the matching original bodies in `emm_mngr.c` are nevertheless excluded by its historical `#ifndef NTVDM` gate. | Retain the entire original writeback block unchanged, expanding only its outer compilation condition for the explicit selected CCPU40 recovery profile. No adapter, substitute write algorithm, or alternate EMS state is introduced. | `softpc.new/base/dos/emm_mngr.c`; selected build flag in `tools/build/New-T310OriginalSoftpcNinja.ps1` |
| MVDM-HOST-DIV-149 | Start a 32-bit COMMAND child with the original selected standard-stream endpoints. | NT4 temporarily changed its dedicated `ntvdm.exe` process-wide standard handles before `CreateProcess`; the current app owns those streams independently even in the admitted single-session profile. | Keep the original `SetStdHandle → CreateProcess → restore` order and parameters. A translation-unit-local Win32 adapter records those selected handles per worker and passes them only in a copied `STARTUPINFO` at the public child-creation boundary. | `dos/command/cmdexec.c`; `../adapter-mvdm-host-out/win32/{include/command_process_compat.h,source/command_process_compat.c}` |
| MVDM-HOST-DIV-067 | Call original same-package video selector and EGA helpers. | NT4 C accepted absent declarations as an implicit `int` contract, which prevents modern compilers from validating the real parameter and return ABI on either host architecture. | Add only exact declarations for the existing `setMarkPointers`, `Glue_set_vid_wrt_ptrs`, index-state, C_VID selector and `ega_read_dot` bodies. Their definitions, selection order and behavior are unchanged. | `softpc.new/base/video/ega_prts.c`, `vga_prts.c`, `cga.c`, `ega_read.c`, `video.c` |
| MVDM-HOST-DIV-087 | Invoke the original VM-instance cleanup and host stream-display update providers. | The split selected source layout carries neither historical declaration carrier, so NT4-era implicit `int` calls cannot validate their `void`/pointer-width ABI on x86 or x64. | Publish only the exact existing provider declarations at the two original call sites; preserve the patch `ClearInstanceDataMarking` provider, `nt_graph.c` display provider, and all caller ordering. | `softpc.new/base/bios/virtual.c`, `softpc.new/base/video/gfx_updt.c` |
| MVDM-HOST-DIV-088 | Publish the selected C-video EVID handler-table and selector contracts. | The original generic `MEM_HANDLERS` table erases the concrete EVID callback signatures, leaving its move-slot initializer incompatible; the selected writers/markers also call original selectors without declarations. | Under the existing `CPU_40_STYLE`/`C_VID` selection only, preserve the six callback shapes from `ev_glue.c`, publish exact selector contracts, and omit the unselected generic A2/debug table whose callback forms are not installed by this profile. Other historical machine selections retain the generic table. | `softpc.new/base/inc/gmi.h`, `softpc.new/base/video/{ega_writ.c,gfx_updt.c}` |
| MVDM-HOST-DIV-089 | Bind selected C-video read glue through the original read-handler table. | The historic `ULONG` table fields and implicit glue imports only agree with EVID's `IU32` forms by 32-bit typedef coincidence, so modern compilers cannot validate the selected call ABI. | Under the existing `CPU_40_STYLE`/`C_VID` selection, give `READ_POINTERS` and its three imports the exact `ev_glue.c` forms. Other historical selections retain their original carrier. | `softpc.new/base/inc/cpu_vid.h`, `softpc.new/base/video/ega_read.c` |
| MVDM-HOST-DIV-090 | Fill selected EGA private plane words through the original host `memset4` helper. | The base declaration used `ULONG *`, while the selected `copy_fnc.c` provider takes `unsigned int *`; their historical 32-bit width coincidence leaves a mismatched pointer contract on current MSVC. | Publish the provider's exact `unsigned int` data/pointer/count forms and use the matching private plane-word cast. The fill algorithm and private video-memory ownership are unchanged. | `softpc.new/base/inc/host.h`, `softpc.new/base/video/ega_vide.c` |
| MVDM-HOST-DIV-091 | Scan original COMMAND current-directory MULTI_SZ entries for per-drive `=X:` variables. | The original code round-trips a private native `CHAR *` through `ULONG` merely to advance by two/four bytes; this truncates the host pointer on x64. | Preserve the byte offsets, matching condition and environment update order with direct same-buffer pointer arithmetic; this host-local pointer never crosses a guest/component ABI and does not use the mapping manager. | `dos/command/cmdmisc.c` |
| MVDM-HOST-DIV-098 | Call the original SoftPC ICA lock around speaker state and device-handle transitions. | The historical product aggregate supplied the two `nt_eoi.h` contracts transitively; its owner-rooted source mirror no longer does, so modern C assumes an incompatible `int` return. Full `nt_eoi.h` also selects an unrelated monitor-only `VDMVIRTUALICA` global. | Add only the two original `void` declarations required by this source. The original sound/ICA bodies, lock ordering and failure behavior remain unchanged. | `softpc.new/host/src/nt_sound.c`, original declaration evidence `softpc.new/host/inc/nt_eoi.h` |
| MVDM-HOST-DIV-099 | Expose the original event-manager `PointerAttachedWindowed` external. | The original aggregate let C infer `int`; its provider actually defines the shared SoftPC `BOOL` state. | Publish only the existing `BOOL` declaration. No event, mouse or console behavior changes. | `softpc.new/host/inc/nt_event.h`, `softpc.new/host/src/nt_event.c` |
| MVDM-HOST-DIV-100 | Notify the selected CCPU executor when an original SoftPC application thread starts or exits. | The original `ntthread.h` provider carrier was omitted by the owner-rooted host source, causing implicit `int` calls on both architectures; its unrelated `jmp_buf` declarations require executor-private state not used here. | Add only the two existing original `void` declarations; keep original calls, thread lifetime and failure flow. | `softpc.new/host/src/nt_thred.c`, original declaration evidence `softpc.new/base/ccpu386/ntthread.h` |
| MVDM-HOST-DIV-102 | Publish selected CCPU common register-vector contracts. | NT4 common host_cpu.h, nt_cprgs.c storage and direct nt_aorc.c wrappers use empty parameter lists/default int, while selected c_reg.c providers use precise register, flag, segment and descriptor-result contracts. Those unchecked calls are not a stable x86/x64 ABI. | Preserve all source-named callbacks, operations and dispatch order; make only selected CCPU IU8/IU16/IU32/IBOOL and ISM32 declarations explicit. Guest-width narrowing warnings remain visible and unchanged. | softpc.new/host/inc/host_cpu.h; softpc.new/host/src/nt_cprgs.c; softpc.new/host/src/nt_aorc.c |
| MVDM-HOST-DIV-107 | Represent the DOS Drive Parameter Block next link in its guest-defined 16:16 form. | The original `struct A_DPB *` is a 32-bit x86 process-pointer coincidence; on x64 it widens the packet and corrupts the following DOS fields. | Keep the original field order and writes, but store `Next` as the guest's four-byte far value; terminal and link values remain `FFFFFFFFh` and `segment:offset`. | `dos/dem/demdasd.h`, `dos/dem/demgset.c` |
| MVDM-HOST-DIV-108 | Carry COMMAND redirection records and standard stream handles across their original DWORD/register-pair ABI. | NT4 x86 placed native host pointers and HANDLEs directly in `CMDINFO`, `STD_HANDLES`, and `BX:CX`; those fields cannot carry x64 native values. | Preserve the original record allocation, service order, BOP fields and free point. Publish/resolve only opaque 32-bit IDs through the existing session host-resource mapping manager; zero retains the original no-redirection case and an invalid nonzero ID follows the original error direction. | `dos/command/{cmdmisc.c,cmdexec.c}`, `../adapter-mvdm-host-out/softpc/include/mvdm_command_redirection.h`, `../adapter-mvdm-host-out/softpc/mvdm_command_redirection.c` |
| MVDM-HOST-DIV-109 | Start the original COMMAND child-execution worker under the correct x86/x64 callback and session contract. | `cmdCreateProcess` is a `void`, cdecl no-argument worker but historical source casts it to `LPTHREAD_START_ROUTINE`; x64 masks the mismatch and the raw worker has no session binding for mapped handles. | Retain the original worker body and `CreateThread` call ordering; a translation-unit-local adapter binding invokes a same-shaped void/cdecl bridge that establishes the creator session only for worker execution, carries a bounded source spelling for a default-off disposal diagnostic, and returns `DWORD 0` to Win32. | `dos/command/cmdexec.c`; `../adapter-mvdm-host-out/win32/{include/thread_start_compat.h,source/thread_start_compat.c}` |
| MVDM-HOST-DIV-110 | Compare the original COMMAND boot-file handle with its native invalid value. | An x86 `0FFFFFFFFh` integer cast to `HANDLE` becomes a zero-extended non-sentinel on x64. | Use the public `INVALID_HANDLE_VALUE` spelling at the unchanged original failure test; no file I/O or guest ABI behavior changes. | `dos/command/cmdconf.c` |
| MVDM-HOST-DIV-111 | Retain COMMAND's SCSINFO, `SCS_ToSync`, `SCS_Is_Dos_Binary`, and `SCS_FDACCESS` source field addresses. | NT4 retained `GetVDMAddr` process aliases and derived real-mode values by truncating those host pointers; neither representation is safe across current x86/x64 session boundaries. | Preserve the packed source structure, field offsets, byte/word updates, and unsigned-word wrap. The mirror records each original 16:16 location once; the adapter takes a bounded session guest-memory lease only while a COMMAND, DEM, disk, or floppy consumer operates. A missing/stale location follows that caller's existing error or termination direction. | `dos/command/{cmd.h,cmddata.c,cmdexec.c,cmdmisc.c,cmdpif.c}`, `dos/dem/demmisc.c`, `softpc.new/host/{inc/nt_fdisk.h,src/nt_fdisk.c,src/nt_rflop.c}`, `../adapter-mvdm-host-out/softpc/{include/mvdm_command_guest_state.h,mvdm_command_guest_state.c}` |
| MVDM-HOST-DIV-112 | State the original COMMAND binary/registry calls with their public Win32 parameter types and retain native host-local string length. | NT4 x86 accepted `char[]` as `LPBYTE`, `LPLONG` as `LPDWORD`, and `size_t` as `ULONG` by width coincidence; the latter can narrow on x64. | Preserve all original commands, registry bytes, classification values and 128-byte guest-tail admission rule. Use the actual Win32 `LPBYTE`/`DWORD*` contracts and retain `strlen` in `size_t` until that unchanged bounded admission check. | `dos/command/cmdexec.c` |
| MVDM-HOST-DIV-113 | Pass original COMMAND temporary-file byte counts to `WriteFileAssert`. | The historical `DWORD` parameter was fed `size_t` results from native `strlen`; x64 can narrow an arbitrary host string count before the Win32 write boundary validates it. | Retain the source temp-file and controlled-termination sequence. The mirror passes native byte counts unchanged to a tiny matching overlay, which checks the sole `size_t → DWORD` boundary before the original `WriteFile` call. | `dos/command/cmdconf.c`, `../mvdm-host-overlay/dos/command/mvdm_command_write_length.[ch]` |
| MVDM-HOST-DIV-114 | Read keyboard-layout registry values and return the original `KEYB.COM` command-tail packet. | NT4 C accepted `CHAR[]` where `RegQueryValueEx` requires `LPBYTE`, and narrowed host `strlen` values to `INT` then a guest byte. x64 makes the first conversion diagnostic; the final guest byte must never silently wrap on either host width. | Keep source buffer sizes and layout order. State the public `LPBYTE` arguments, retain local fixed-buffer lengths as `size_t`, and follow the existing `NoInstallkb16` failure direction if the guest one-byte packet cannot represent the result. | `dos/command/cmdkeyb.c` |
| MVDM-HOST-DIV-115 | Build original COMMAND console-title and command-tail strings. | NT4 x86 assigned native `strlen` values to a 32-bit `ULONG`; these are host-local buffer counts and can narrow on x64 before the established guest boundary is reached. | Retain the local values as `size_t`; the fixed title buffer supplies the public `DWORD` console count, and the existing source assertion remains responsible for the 127-byte guest tail. | `dos/command/cmdmisc.c` |
| MVDM-HOST-DIV-160 | Receive the current host directory returned through original `VDMINFO`. | The original `cmdGetNextCmd` advertises `MAX_PATH + 1` bytes but allocates only the guest-visible 64-byte DOS-directory limit, which corrupts the stack when an installed package root is longer. | Retain the original BaseSrv request and host `SetCurrentDirectory` behavior; enlarge only the host-local receiving buffer to exactly the declared `MAX_PATH + 1` capacity. | `dos/command/cmdmisc.c` |
| MVDM-HOST-DIV-116 | Report an original CCPU simulation-buffer allocation failure. | The original `%d` diagnostic receives `sizeof(ThreadSimBuf)`, which is `size_t` and can be wider than `int` on x64. | Preserve the original allocation, failure branch and diagnostic fields; use C's matching `%zu` conversion on both host architectures. | `softpc.new/base/ccpu386/ntthread.c` |
| MVDM-HOST-DIV-128 | Return a typed failure value from the original CCPU exception-frame accessor. | The original `jmp_buf *ccpu386ThrdExptnPtr()` used a bare `return` on its invalid TLS and missing-TLS paths, producing an undefined pointer result. | Preserve diagnostics, failure branches, TLS lookup and normal frame selection; return the same typed null frame used by sibling `ccpu386SimulatePtr()` failure paths. | `softpc.new/base/ccpu386/ntthread.c` |
| MVDM-HOST-DIV-144 | Retain the original local `DbgBreakPoint` replacement used by the process-level SoftPC entry. | The selected modern NTDLL declaration is `NTAPI`, while the source definition omitted that historical calling-convention spelling; x86 would otherwise expose two incompatible declarations. | Add only the matching `NTAPI` declaration modifier. The original debugger-port check, free-build no-op and `DebugBreak` behavior remain unchanged. | `softpc.new/obj.vdm/ntvdm.c`; declaration carrier `../adapter-mvdm-host-out/win32/include/nt.h` |
| MVDM-HOST-DIV-145 | Compare the original windowed-graphics resize switch against its fixed option text. | The original stores `strlen`'s native `size_t` result in `int`; current compilers diagnose that host-width narrowing before the unchanged bounded comparison. | Retain the count in `size_t` until the original `strncmp` and exact-length test; the value stays host-local and does not alter a guest, configuration, or component ABI. | `softpc.new/host/src/nt_reset.c` |
| MVDM-HOST-DIV-129 | Publish the original alternate-CPU interrupt dispatch result ABI. | `a3_cpu_interrupt` has no result path but used an implicit `int` declaration, which modern C rejects and can misdescribe its call result. | Retain switch cases, interrupt delivery and default diagnostic; state the existing `VOID(int, IU16)` callable form. | `softpc.new/base/ccpu386/ntstubs.c` |
| MVDM-HOST-DIV-130 | Preserve bounded FPU stack-index arithmetic across host widths. | Subtracting two native pointers produces a host-width `ptrdiff_t`, while the original FPU register index is fixed-width `IU32`. | Preserve the original eight-entry FPU stack and `IU32` register computation; state that the bounded private stack offset is converted before addition and is neither a guest address nor host identity. | `softpc.new/base/ccpu386/fpu.c` |
| MVDM-HOST-DIV-131 | Publish the original reserved-NPX dispatch input ABI. | `ZFRSRVD` used a K&R declaration that hides its fixed-width instruction argument from modern C call checking. | Retain the original `IU32` parameter, instruction table and prologue order; express the same signature with a modern prototype. | `softpc.new/base/ccpu386/zfrsrvd.c` |
| MVDM-HOST-DIV-132 | Select XMS's original virtual-memory allocator path without the historical host-architecture proxy. | `i386` selected an implementation that converted Intel XMS values into process pointers. That is neither safe nor semantically consistent for the CPU40/x86 product. | Preserve the original `VdmAllocateVirtualMemory`/`SAInitialize` branch, callback order, allocator and failure path by leaving historical `i386` undefined in the formal product. The direct-pointer `i386/xmsmem86.c` remains mirror evidence only; `xmsMoveMemory` remains the separately registered mapping-manager transfer seam. | `xms.486/xms.c`; `softpc.new/host/src/nt_mem.c`; `../adapter-mvdm-host-out/softpc/{include/mvdm_xms_memory.h,mvdm_xms_memory.c}` |
| MVDM-HOST-DIV-133 | Keep the original resource-write retry inside the app-selected firmware root. | After `host_find_file` had resolved an existing resource, the NT4 retry used only the bare name and could create/write a same-named file in the process current directory. The current product's app/session contract owns resource roots and must not silently escape them. | Preserve the original lookup, first open, retry/create attempt and `host_error` ordering; retry using the same resolved `hff_ret` path rather than bare `name`. No resource bytes, guest state or component ABI changes. | `softpc.new/host/src/nt_rez.c` |
| MVDM-HOST-DIV-134 | Create zero-length `C:\\MSDOS.SYS` and `C:\\IO.SYS` placeholders during a first session for a historical Brief installer probe. | These are installation-only faux boot markers. The selected product may project real host drives but must never mutate a real host boot root merely to satisfy this probe. | Retain the original `scs_init → AddSystemFiles` order and void contract. A selected-profile macro transfers only this side effect to the same-shaped adapter disposition; the complete original body remains in the mirror under the alternate compile branch. | `softpc.new/host/src/nt_msscs.c`; `../adapter-mvdm-host-out/softpc/{include/mvdm_softpc_firmware.h,mvdm_softpc_firmware.c}` |
| MVDM-HOST-DIV-118 | Carry original COMMAND host environment lengths to fixed DOS/VDM fields. | NT4 assigned native `strlen`/wide-character differences directly to `USHORT`, `WORD`, and `DWORD`; x64 widens `size_t` and can silently truncate an environment length before the original fixed-width interface observes it. | Keep the original environment collection, filtering, ordering, allocation, and return flow. A private overlay validates each host length only at the existing `UNICODE_STRING`, COMMAND/DOS, or `VDMENVBLK` width boundary; an unrepresentable value follows the source function's existing failure direction instead of wrapping. | `dos/command/cmdenv.c`; `../mvdm-host-overlay/dos/command/mvdm_command_length.[ch]` |
| MVDM-HOST-DIV-119 | Retain the packed original SCS/standard-handle guest ABI while storing COMMAND pipe, thread, critical-section and redirection records as native host allocations. | The original `#pragma pack(1)` remained active past the guest records. x86 tolerated unaligned host fields; x64 reports each address passed to a Win32 synchronization API and the layout is not a guest-visible contract. | Close the packing scope immediately after `STD_HANDLES`; preserve the byte-exact guest structures and leave the later host-only structs naturally aligned. No source algorithm, BOP field, mapping identity, or handle route changes. | `dos/command/cmd.h` |
| MVDM-HOST-DIV-120 | Start original COMMAND pipe workers through the modern `CreateThread` callback contract. | `cmdPipeInThread` and `cmdPipeOutThread` are cdecl `void(LPVOID)` bodies, but the NT4 source force-casts them to `LPTHREAD_START_ROUTINE`; this is ABI-unsafe on x86 and loses the creator session binding. | Keep both original worker bodies and call ordering. A same-shaped adapter captures the current session, calls the cdecl worker through a WINAPI thunk, carries a bounded source spelling only for default-off disposal attribution, then returns `DWORD 0` only at the Win32 boundary. | `dos/command/cmdredir.c`; `../adapter-mvdm-host-out/win32/{include/thread_start_compat.h,source/thread_start_compat.c}` |
| MVDM-HOST-DIV-121 | Declare the selected generated C-video mark rules with their real native host-word ABI. | `evidfunc.h` declared 17 generated `S_*Mark*` definitions with implicit `int` returns and obsolete table-facing argument lists. The generated rule definitions actually take four `IUH` words and return `IUH`; this is a call ABI error even in the selected x86 profile. | Retain the original generated rule names, tables and bodies. Correct only the mirror declarations to the exact existing four-word `IUH` contract, so the original `evidfunc.c` table definitions compile directly. | `softpc.new/base/cvidc/evidfunc.h` |
| MVDM-HOST-DIV-122 | Format generated C-video native host words with their native variadic shape. | The selected C-video rule profile widens `IUH` to `UINT_PTR`, but five diagnostic-only `printf` calls retained `%d`, which is a varargs ABI mismatch on x64. | Use the standard `PRIuPTR` format with an explicit `uintptr_t` view only at those diagnostic calls. Rule arithmetic, table selection, video state, and guest values remain unchanged. | `softpc.new/base/cvidc/{ev_glue.c,j_c_lang.c}` |
| MVDM-HOST-DIV-123 | Preserve generated C-video native-word bit-test width. | Two generated rule paths use an `IUH` bitfield and an `8*sizeof(IUH)` guard, but leave the literal `1` as a 32-bit `int`; x64 then performs a 32-bit shift before the native-word bit test. | Keep the original rule IDs, guard, fallthrough and result paths. Type only the literal as `IUH`, so the generated bit test follows its existing native-word carrier on x86 and x64. | `softpc.new/base/cvidc/{SEVID019.c,SEVID020.c}` |
| MVDM-HOST-DIV-124 | Report the original `RegainRegenMemory` null pseudo-handle failure through `DisplayErrorTerm`. | The original only reaches this branch when `NtCurrentProcess()` is null, so its historical `(DWORD)processHandle` value is exactly zero; spelling the cast directly truncates `HANDLE` on x64. | Pass the same scalar zero directly. The selected non-MONITOR profile does not enable the fullscreen regeneration transition; this change preserves the original branch's observable error argument without asserting that transition is runtime-closed. | `softpc.new/host/src/nt_sec.c` |
| MVDM-HOST-DIV-125 | Retain the CCPU private host decode-cursor delta until it joins Intel EIP arithmetic. | The original macro assigned a native pointer difference to `int` and then mixed it with `IU32` EIP. This is an implicit width narrowing on x64 even though the two pointers describe one bounded code-page instruction span. | Name the existing delta as an `IU32` instruction-byte count and use it at the three original EIP update forms. No guest address, host pointer, mapping token, decode order or EIP wrapping rule changes. | `softpc.new/base/ccpu386/c_main.c` |
| MVDM-HOST-DIV-127 | Publish the original CCPU extended fast-BOP fallback call contract. | The selected CCPU source calls `EDL_fast_bop(ULONG)` but no selected OpenNT definition exists, so modern C infers an invalid `int` result. | Declare the exact `void(ULONG)` form at the original caller. The separately registered NTVDMx64 selective patch preserves only the original default debugger-break disposition; no BOP semantics or adapter route is introduced. | `softpc.new/base/ccpu386/c_main.c`; `../mvdm-softpc-patch/patches/common/fmstubs.c` |
| MVDM-HOST-DIV-135 | State the selected original C-VID mark callback result contract. | The historic `UPDATE_ALG` table declared `boolean` results although all selected CGA/EGA mark providers are `VOID` and every reached caller discards that result. The old casts obscure the actual CCPU40 callback contract. | State the five callback forms as `void`, make the local simple marker `VOID`, and retain the existing side effect and table order. No display endpoint, guest address form, or controller state algorithm changes. | `softpc.new/base/inc/gfx_upd.h`; `softpc.new/base/video/gfx_updt.c` |
| MVDM-HOST-DIV-136 | Publish original EGA write-provider parameter contracts. | The original writer tables used empty K&R declarations at local and imported provider boundaries, so modern MSVC could not check the selected value/offset/count forms. | Keep bodies and table order unchanged; replace only those declarations with the exact original later-definition `IPT2`/`IPT3`/`IPT4` signatures. | `softpc.new/base/video/{ega_writ.c,egawrtm0.c,egwrtm12.c}` |
| MVDM-HOST-DIV-137 | Provide selected CCPU shared Gdp/Cpu/Video state without a duplicate SAS vector. | Historical `localfm.c` declares all four globals, but selected `ccpusas4.c` already owns the original `Sas` vector. Linking both would make two SAS states. | A narrow overlay preserves the original Gdp, Cpu and Video declaration spellings and omits only duplicate `Sas`; no algorithm, guest value or host identity is added. | `../mvdm-host-overlay/softpc.new/base/ccpu386/localfm.c`; original reference `softpc.new/base/ccpu386/localfm.c` |
| MVDM-HOST-DIV-156 | Recover the omitted generated CCPU-to-C-VID vector binder. | The retained CCPU40 manifests contain original slot metadata but omit the historical generated assembly unit; generic include order also selected an incompatible CCPU vector layout for the shared carrier. | The overlay expands original `c2cpusad.h` into typed C-VID assignments at the original setup point. Slots without an original selected-profile provider remain explicitly unavailable, not shimmed. | `softpc.new/base/cvidc/ev_glue.c`; `../mvdm-host-overlay/softpc.new/base/{ccpu386/localfm.c,cvidc/mvdm_cvidc_vector_binding.c}`; `../../tools/build/GenerateCvidcCpuBinding.mjs` |
| MVDM-HOST-DIV-103 | Carry DEM native Win32 file HANDLEs in original AX:BP/AX:SI register pairs. | The NT4 x86 encoding passes a native 32-bit HANDLE directly through guest register words; a modern x64 HANDLE cannot be represented there. | Preserve the exact two-word register ABI but publish, resolve and release its opaque 32-bit identity through the existing session host-resource mapping manager. Invalid nonzero identities become INVALID_HANDLE_VALUE; mapping-publication failure closes the just-created native handle and follows the existing error route. | dos/dem/dem.h, dos/dem/demfile.c, dos/dem/demfcb.c, dos/dem/demhndl.c, adapter-mvdm-host-out/softpc/include/mvdm_host_identity.h, adapter-mvdm-host-out/softpc/mvdm_host_identity.c |
| MVDM-HOST-DIV-104 | Form the original temporary FOT pathname by copying the prefix preceding the extension. | The original code subtracts two native pointers after truncating both to ULONG; that loses x64 address bits even though the difference is bounded by the local pathname buffer. | Retain direct pointer subtraction and convert only its bounded final count to ULONG; it is private host arithmetic, not a mapping-manager identity. | dos/dem/demfile.c |
| MVDM-HOST-DIV-105 | Pass the original file-position high offset into the Win32 SetFilePointer call. | NT4 accepted an unsigned ULONG pointer where the public contract requires signed LONG; same-width coincidence hid the incompatible call ABI. | Retain the zero high-offset value and original seek ordering, but declare its local carrier as LONG at both calls. | dos/dem/demhndl.c |
| MVDM-HOST-DIV-106 | Load NTDOS.SYS through the original DEM host loader. | The native invalid-handle sentinel and private loader cursor were round-tripped through 32-bit integer casts, truncating on x64. | Use INVALID_HANDLE_VALUE and direct PBYTE cursor arithmetic; neither value crosses guest or component ABI. | dos/dem/demmisc.c |
| MVDM-HOST-DIV-154 | Initialize original DEM's DOS directory carrier. | NT4 obtains it from the installed system directory; the product selects one immutable MVDM system root per session. | Retain DEM's caller-owned directory buffer and all subsequent path/file logic, but fill it through the existing session-system adapter rather than `GetSystemDirectory`. | dos/dem/dem.c; ../adapter-mvdm-host-out/softpc/{include/mvdm_softpc_firmware.h,mvdm_softpc_firmware.c} |
| MVDM-HOST-DIV-157 | Select original `config.nt` and `autoexec.nt` when a PIF does not provide an override. | NT4 derives the default from its installed system directory. Current Windows does not provide those historical files, while the product selects immutable MVDM system media per session. | Preserve original PIF override expansion, caller buffer, return direction and downstream `cmdconf.c` fatal path; bind only the no-PIF default through the existing system-media lookup adapter. | softpc.new/host/src/nt_pif.c; ../adapter-mvdm-host-out/softpc/{include/mvdm_softpc_firmware.h,mvdm_softpc_firmware.c}; ../../tools/build/Stage-OriginalSoftpcRuntime.mjs |
| MVDM-HOST-DIV-158 | Expand original CONFIG.NT/AUTOEXEC.NT `%SystemRoot%` paths and write their synthesized COUNTRY/SHELL lines. | NT4 uses its installed Windows directory; the product must instead use the app-selected immutable MVDM system root and must not depend on host installation files. | Preserve original short-path normalization, PIF/config parsing, generated line order, temporary-file ownership and failure direction. Bind only the root acquisition through the existing system-media adapter; stage selected original system-directory-shaped media below `mvdm/system32`. | dos/command/cmdconf.c; ../adapter-mvdm-host-out/softpc/{include/mvdm_softpc_firmware.h,mvdm_softpc_firmware.c}; ../../tools/build/Stage-OriginalSoftpcRuntime.mjs |
| MVDM-HOST-DIV-148 | Select an empty installable-VDD configuration when the retired NT4 product registry key is absent. | Modern Windows no longer installs `HKLM\\SYSTEM\\CurrentControlSet\\Control\\VirtualDeviceDrivers`; treating that absence as a generic error opens the original modal dialog before DOS initialization. | Preserve the original registry enumeration, `SafeLoadLibrary` calls, order and error dialog. A narrow adapter distinguishes only an absent key and returns without installing a VDD; other open failures retain the original dialog path. | softpc.new/host/src/nt_msscs.c; ../adapter-mvdm-host-out/softpc/{include/mvdm_softpc_vdd_configuration.h,mvdm_softpc_vdd_configuration.c} |
| MVDM-HOST-DIV-159 | Terminate the dedicated NT4 VDM after its top-level unhandled-exception filter receives an exception. | The fixed modern product container deliberately forbids debugger attachment and registry dump configuration, while its post-termination WER record loses the original exception address and native return context. | Preserve the original filter's `UnhandledExceptionFilter`, timer, return and termination order; add one adapter call before them that writes only copied exception code/address, native IP/SP/FP/CS, access parameters and a bounded readable stack window to standard error and, only when the fixed observer supplies `MVDM_EXCEPTION_REPORT_PATH`, to that exception-only report. | softpc.new/host/src/nt_timer.c; ../adapter-mvdm-host-out/softpc/{include/mvdm_softpc_termination.h,mvdm_softpc_termination.c}; ../../tests/observation/console_startup_observer.c |
| MVDM-HOST-DIV-162 | Bind the selected CCPU40 C-VID video function-vector before SoftPC configuration begins. | The retained source packages omit the historical generated pre-configuration binder. `config()` reaches EGA setup before the original later `setup_vga_globals()` hook, leaving the C-VID `Video` vector null. | Preserve the original `gfi_init → config` order; insert one declaration-only overlay seam and one idempotent video-only binder call between them. The full CCPU vector is still bound only at the original post-CPU initialization location. | softpc.new/base/support/main.c; ../../mvdm-host-overlay/softpc.new/base/cvidc/{mvdm_cvidc_vector_binding.h,mvdm_cvidc_vector_binding.c} |
| MVDM-HOST-DIV-163 | Provide the CCPU quick-event calibration/restart slots referenced by the selected C-VID CPU table. | The retained C-VID generated `accessfn.c` contains the public forwarding shims but the paired generated timing provider is absent. Binding those slots back to the shims recurses indefinitely. | The C-VID overlay provides only `GetJumpCalibrateVal`, `GetJumpInitialVal`, and `SetJumpInitialVal`, with the original `qevnt.c` initial restart value (100). The generated table binds exactly those slots to the provider; all other retained table entries keep their original mapping. | ../../tools/build/GenerateCvidcCpuBinding.mjs; ../../mvdm-host-overlay/softpc.new/base/cvidc/mvdm_cvidc_vector_binding.c |
| MVDM-HOST-DIV-164 | Observe an already decoded original DEM or COMMAND BOP at its original ingress and, for COMMAND, its already-completed host return. | The non-debug product has no usable debugger or historical trace shell; the BOP adapter must distinguish a reached original ingress from an invented dispatcher, and the `54:05` fixed observation cannot otherwise distinguish a non-returning COMMAND provider from a later CPU/guest boundary. | The adapter emits one ingress record only after original `MS_bop_0` or `MS_bop_4` has decoded its service byte and before original `DemDispatch` or `CmdDispatch`. For the existing fatal DEM `50:3D` record only, it additionally copies the already-live 16-bit `CS:IP` and `DS:DX` words. For original COMMAND only, it emits a second record immediately after the unchanged `CmdDispatch` and original `setIP(+1)` sequence; that record copies already-live `CS:IP`, `AX`, `CF`, and `IF`. It is default-off: only the fixed observer's child-only report path receives it, so the guest-facing Console is never used for project diagnostics. It does not read guest memory, route a selector, alter the provider result, IP ordering, CPU, guest, session, selector, service, return or failure state. | softpc.new/host/src/nt_bop.c; ../adapter-mvdm-host-out/softpc/{include/mvdm_softpc_termination.h,mvdm_softpc_termination.c} |
| MVDM-HOST-DIV-214 | Preserve CPU40 host-thread event notifications while CPU40 consumes another event. | CPU40's original shared event map uses ordinary read/modify/write operations although timer, reset, host-I/O and PIC notification can arrive on host threads concurrently. One clear can therefore erase an unrelated new bit; the hardware IRQ race was only the first observed instance. | Treat every existing bit as an atomic notification latch: use atomic snapshot, raise and compare/exchange consume operations while preserving the original event masks, their original consumers, PIC acknowledge order, vectors, guest flags and transfers. The ICA remains the vector/priority authority. | softpc.new/base/ccpu386/c_main.c |
| MVDM-HOST-DIV-215 | Identify the original guest keyboard-read boundary for a fixed external Console observation. | COMMAND `54:0E` initializes keyboard/Console state but can return while startup code still has interrupts masked; it is not proof that guest `INT 16h` is awaiting a key. The formal `COMMAND.COM` child reaches the NTVDM-specific `AH=1` status-poll edge rather than `AH=2` waitio. | At original `keyboard_io` cases `AH=0`, `AH=1` and `AH=2`, immediately before their unchanged idle calls, emit one default-off child-only marker. The `AH=0`/`AH=1` report is latched once because source polling can repeat. The observer then uses ordinary `WriteConsoleInput`; it does not inject guest data, route a BOP, alter CPU/PIC/BIOS state or change idling. | `softpc.new/base/keymouse/keybd_io.c`; `../adapter-mvdm-host-out/softpc/{include/mvdm_softpc_termination.h,mvdm_softpc_termination.c}` |
| MVDM-HOST-DIV-165 | Make the selected SoftPC BIOS/video ROM inputs resident before recursive CPU40 firmware calls. | Historical NTVDM+X86GFX assumes a retired kernel-VDM product shell has already installed ROM bytes, so `rom_init()` skips its own source-defined resource/SAS loader. The modern non-kernel product has no such shell. | Select the existing original generic `rom_init()` body only for `rom.c`; it preserves original resource lookup, read, RAM-copy and ROM-protection ordering for the staged firmware. Its temporary SAS scratch pointer is private to the synchronous machine call and never becomes a guest/MVDM ABI identity, so no mapping-manager token is appropriate. | softpc.new/base/system/rom.c; ../adapter-mvdm-host-out/softpc/mvdm_softpc_firmware.c; ../../tools/build/New-T310OriginalSoftpcNinja.ps1 |
| MVDM-HOST-DIV-166 | Attribute an already-selected original top-level VDM return in the fixed non-debug observation container. | After ROM recovery, an unchanged terminal status no longer reached the existing known-thread exception report. Without distinguishing `main` return from direct process/thread termination, the next machine repair would be a guess. | Immediately before the original `main` returns, call a diagnostic-only adapter that writes its already-selected `int` result only when the fixed observer supplies a child-only report path. It does not alter SEH, return, guest state, process lifetime or normal product behavior. | softpc.new/obj.vdm/ntvdm.c; ../adapter-mvdm-host-out/softpc/{include/mvdm_softpc_termination.h,mvdm_softpc_termination.c}; ../../tests/observation/console_startup_observer.c |
| MVDM-HOST-DIV-167 | Retain the original async named-pipe request's guest buffer and result locations until host I/O completes. | NT4 stored durable flat `GetVDMAddr` aliases and passed its guest buffer to an asynchronous worker. A session guest lease cannot cross the BOP or worker lifetime. | Preserve the original packed 16:16 values, request queue, completion order and SoftPC ICA route. The Redirector adapter snapshots writes or allocates read staging at submission, then takes fresh leases only to write completion words and read bytes. Stale session memory is discarded, never published as a synthetic completion. | `inc/vrnmpipe.h`, `vdmredir/vrnmpipe.c`; `../adapter-mvdm-host-out/redir/{include/mvdm_redirector_async.h,mvdm_redirector_async.c}`; existing `../adapter-mvdm-host-out/{softpc/include/mvdm_guest_location.h,win32/include/thread_start_compat.h}` |
| MVDM-HOST-DIV-168 | Calculate original local named-pipe canonicalization and record-name lengths. | The original casts host pointers and `strlen` results to `DWORD` by x86 coincidence; these are private host-string measurements and truncate on x64. | Retain the exact path transformation and fixed original `NameLength` record. Keep pointer difference and temporary lengths as `size_t`, reject an unrepresentable record length through the existing false/last-error direction, then narrow only at the original host-local record field. | `vdmredir/vrnmpipe.c` |
| MVDM-HOST-DIV-169 | Terminate local asynchronous named-pipe work when its original owning DOS process terminates. | The retained source declares `VrTerminateNamedPipes` as the lifecycle owner but its body contains no cleanup; keeping a session-bound worker alive prevents safe session teardown. | Preserve the original worker/queue/ICA owners. The Redirector adapter supplies only an atomic stop state; the original termination entry signals, joins, then cancels and releases its own queued I/O records without guest completion. | `vdmredir/vrnmpipe.c`; `../adapter-mvdm-host-out/redir/{include/mvdm_redirector_async.h,mvdm_redirector_async.c}` |
| MVDM-HOST-DIV-170 | Keep original local-mailslot name lengths and invalid-handle tests correct on x86/x64. | The original uses a `DWORD` for a native `strlen` result and casts `0xffffffff` to `HANDLE`; both rely on x86 width coincidence. | Use `size_t` until the original bounded record/allocation call and fail through its existing path if unrepresentable; use the public `INVALID_HANDLE_VALUE` sentinel for the same original failure checks. Mailslot IDs still use the existing session host-resource mapping. | `vdmredir/vrmslot.c`; existing `../adapter-mvdm-host-out/redir/mvdm_redirector_mailslot.c` |
| MVDM-HOST-DIV-173 | Start `VrpAsyncNmPipeThread` through the original `CreateThread` call. | The original cdecl `DWORD(LPVOID)` callback is incompatible with the WINAPI callback ABI on x86; x64 compatibility is incidental. | Keep the original worker body, call expression, arguments and failure branch. The existing same-shaped Win32 thread facade makes only the cdecl-to-WINAPI transition and binds the creator session during the worker lifetime. | `vdmredir/vrnmpipe.c`; `../adapter-mvdm-host-out/win32/{include/thread_start_compat.h,source/thread_start_compat.c}` |
| MVDM-HOST-DIV-174 | Locate `VDM_LOAD_INFO.VrInitialized` for the original deferred Redirector load notification. | The original forms a pointer from a zero base then narrows it to `DWORD`; it is semantically a guest record field offset, not a native address. | Use `FIELD_OFFSET` as the exact numeric field offset before the existing CS:offset conversion. The guest write, source ordering and failure behavior are unchanged. | `vdmredir/vrinit.c` |
| MVDM-HOST-DIV-175 | Start the original DLC receive-event worker through its original `CreateThread` call. | Its historical cdecl `DWORD(LPVOID)` entry is incompatible with the modern x86 WINAPI callback ABI. | Retain the worker body, original call expression, arguments and error branch; the existing translation-unit-local thread facade performs only the cdecl-to-WINAPI boundary and binds the creator session for the worker lifetime. | `vdmredir/vrdlcpst.c`; `../adapter-mvdm-host-out/win32/{include/thread_start_compat.h,source/thread_start_compat.c}` |
| MVDM-HOST-DIV-176 | Bind original SoftPC graphics DIB output without the NT4 Console Server graphics-buffer protocol. | `graphicsResize` requests `CONSOLE_GRAPHICS_BUFFER`, whose private server implementation supplies `lpBitMap` and a mutex. Modern public Console APIs cannot do this. | Retain the original DIB allocation, palette, resize, CGA/EGA/VGA pixel writers, invalidation calls and handle-close order. Replace only the private allocation call with a same-shaped adapter binding that supplies a bounded session-owned DIB byte plane and duplicate output handle; app sees snapshots only. | `softpc.new/host/src/nt_graph.c`; `../adapter-mvdm-host-out/win32/{include/conapi.h,source/console_compat.c}`; `../session/session.[ch]` |
| MVDM-HOST-DIV-177 | Attribute the complete already-reached original COMMAND table-call cohort. | A durable post-`MS_bop_4` return record is absent, while the final console screen cannot identify whether original `CmdDispatch`, its `PFNSVC` table call, `cmdSetInfo`, or the scalar registration is the first non-returning edge. | Add fixed-width diagnostic records immediately before/after the original table call and before/after original `cmdSetInfo` scalar registration. For the original `SVC_GETINITENVIRONMENT` retry, the existing record also captures SS:SP with ES/BX/AX/CF so a BOP service cannot be blamed for an unrelated stack change. Records append to the existing child-only observer report when supplied; they do not read guest memory, alter the table/callee, AX/CF/IP, BOP result, guest, CPU or session state. | `dos/command/{cmddisp.c,cmdmisc.c}`; `../adapter-mvdm-host-out/softpc/{include/mvdm_softpc_termination.h,mvdm_softpc_termination.c}` |
| MVDM-HOST-DIV-202 | Determine whether the source-owned resident `LodCom_Entry` jump-table binding, HMA/A20 precondition, and post-environment-return execution window remain valid. | `EndInit` transfers through `LodCom_Trap -> LodCom_Entry`; scalar BOP records alone cannot distinguish an invalid resident far pointer, an HMA flag/A20 mismatch, or a later control failure. | After the unchanged `SVC_GETINITENVIRONMENT` table call, a default-off helper reads only `TrnLodCom1_Entry`, `LodCom_Entry`, `MsgRetrv_Entry`, `ComInHMA`, the source-defined `PDB_Version` word at resident PSP offset `0040h`, and eight bytes at the source-defined `LodCom` HMA target through short session mapping leases; it also queries the existing CPU40 A20-wrap scalar. At each already completed `54:0F` BOP return it copies a 12-byte window from that exact post-service CS:IP. The PDB word is startup-state evidence, not an assertion that the observer has proven the `INT 21h/AH=30h` return. It cannot retain a guest alias, write guest memory, alter CPU/BOP state, or route COMMAND. | `dos/command/cmddisp.c`; `softpc.new/host/src/nt_bop.c`; `../adapter-mvdm-host-out/softpc/{include/mvdm_softpc_termination.h,mvdm_softpc_termination.c,mvdm_guest_location.c}` |
| MVDM-HOST-DIV-203 | Observe the original CCPU `BOP FE` CPU-unwind boundary. | Fixed-container evidence can show that `host_main` is inside original cleanup but cannot otherwise identify the already-decoded guest return site that selected the original `c_cpu_unsimulate` transition. | Immediately before the unchanged CPU40 `c_cpu_unsimulate()` call, copy only the already-live post-decode CS:IP to the existing child-only BOP report when configured. The hook neither routes, resumes, stops, changes registers, writes guest memory, retains a guest alias nor changes the original unwind. | `softpc.new/base/ccpu386/c_main.c`; `../adapter-mvdm-host-out/softpc/{include/mvdm_softpc_termination.h,mvdm_softpc_termination.c}` |
| MVDM-HOST-DIV-183 | Observe the source-defined end of NTDOS CONFIG processing without changing its notification semantics. | The non-debug fixed container cannot otherwise distinguish a skipped `multi_pass`/`trys` write from a later `commnd` loss before permanent COMMAND loading. | After unchanged `UMBNotify(0)` and `demDasdInit()` in original `MS_bop_E`, a default-off adapter records only selected-map `multi_pass_id` and bounded `commnd` offsets under the already-live original CS. It acquires and releases synchronous read leases before the original BOP returns; it does not retain a guest pointer, route a selector, modify CONFIG/UMB/DEM, or affect CPU/guest state. | `softpc.new/host/src/nt_bop.c`; `../adapter-mvdm-host-out/softpc/{include/mvdm_softpc_termination.h,mvdm_softpc_termination.c,mvdm_guest_location.c}` |
| MVDM-HOST-DIV-184 | Observe a bounded source-owned CPU40 SAS write span, including byte/word/dword stores. | Selected-image recovery must distinguish an absent original control-word write or transient copy from a later in-guest control transfer, without adding instruction tracing or reading guest memory. | After each unchanged `c_sas_store`, `c_sas_storew` or `c_sas_storedw` completes, a default-off helper matches one explicitly configured linear span and records only write address, width and scalar value. It cannot alter CPU state, guest memory, selectors, routing, or mapping lifetime. | `softpc.new/base/ccpu386/ccpusas4.c`; `../adapter-mvdm-host-out/softpc/{include/mvdm_softpc_termination.h,mvdm_softpc_termination.c}` |
| MVDM-HOST-DIV-185 | Observe the original DEM file-read scalar contract needed by NTDOS `doconf`. | S7 proves `trys` was not reached; the original `doconf` branch requires a complete `AX == CX` read before it can enter `getcom`. | Default-off calls at original `demRead` entry and terminal success/failure copy only numeric DS:DX, request, file-offset, completed count, AX and carry state. They do not inspect the guest buffer, alter the host read, retain a pointer or alter a DEM/BOP result. | `dos/dem/demhndl.c`; `../adapter-mvdm-host-out/softpc/{include/mvdm_softpc_termination.h,mvdm_softpc_termination.c}` |
| MVDM-HOST-DIV-186 | Observe the original CONFIG EOF-seek scalar contract before NTDOS `doconf` reads its temporary file. | T355 S8 proves a temporary-file open but no `demRead`; source proof identifies `AH=42h/AL=2` as the only intervening host-service call. | Default-off calls at original `demChgFilePtr` entry and terminal success/failure copy only DOS offset/origin/result/AX/carry scalars. They retain no host handle or guest pointer and do not alter file position or a DEM result. | `dos/dem/demhndl.c`; `../adapter-mvdm-host-out/softpc/{include/mvdm_softpc_termination.h,mvdm_softpc_termination.c}` |
| MVDM-HOST-DIV-188 | Recover `SVC_DEMFASTREAD` when the original NT4 x86 kernel VDM interceptor is absent. | Original `demdisp.c` deliberately exposes a CF-clear no-op because `base/ntos/vdm/x86/rdwr.c::NTFastDOSIO` handles the normal-file BOP in kernel. The selected modern user-mode product cannot import that kernel/trap-frame/VDM-TIB implementation. | Bind the original table entry to one mirror-relative overlay. It accepts only a mapped regular disk file, seeks only when the original ZF contract requires it, then takes one bounded session guest-memory write lease and performs the synchronous read. It returns AX byte count/CF=0 on success (including EOF); standard handles, pipes/devices, invalid handles, unsafe leases and every fast-I/O failure return only CF=1, so unchanged DOS `$READ` falls back to original `SVC_DEMREAD` (`50:16`). | `dos/dem/demdisp.c`; `dos/dem/dem.h`; `../mvdm-host-overlay/dos/dem/demfastio.c`; `../../adapter-mvdm-host-out/softpc/{include/mvdm_host_identity.h,mvdm_host_identity.c,include/mvdm_softpc_guest_memory.h,mvdm_softpc_guest_memory.c}` |
| MVDM-HOST-DIV-189 | Supply the reached C-VID `Sas.Sas_overwrite_memory` vector member. | The original generated CCPU `sascdef.c` carrier leaves this member null, but the selected CPU40 `ccpusas4.c` supplies an exact same-shaped provider whose original behavior is a deliberate no-op: CCPU has no compiled-code cache to invalidate. | At the existing C-VID vector composition point, assign only `c_sas_overwrite_memory`; all other generated unavailable slots remain unchanged. | `../mvdm-host-overlay/softpc.new/base/cvidc/mvdm_cvidc_vector_binding.c`; `softpc.new/base/{ccpu386/ccpusas4.c,cvidc/{sas4gen.h,accessfn.c}}` |
| MVDM-HOST-DIV-190 | Attribute a reached CCPU40 direct-RAM physical-read failure without changing the original SAS result. | S17 proves the selected startup does not publish an external physical-page binding, but the `phyR` access violation alone cannot distinguish a bad Intel address from an unavailable M-area backing span. | At the already reached `c_GetPhyAdd` normal-RAM fallback, call one default-off adapter recorder after alias/resolver misses. It copies only requested/direct physical numbers, wrap mask, M-area length and private base scalar; exception reporting emits the last record only when the observer configured a child-only path. | `softpc.new/base/ccpu386/ccpusas4.c`; `../adapter-mvdm-host-out/softpc/{include/mvdm_softpc_termination.h,mvdm_softpc_termination.c}` |
| MVDM-HOST-DIV-191 | Distinguish original COMMAND `SVC_CMDGETNEXTCMD` first delivery, repeat/reacquisition and return behavior in a fixed container. | The existing COMMAND observation records only service/AX/CF, so two reached `54:01` calls cannot prove whether `/C EXIT` returned to a parent COMMAND or started a different lifecycle.  Direct record inspection would retain a Base VDM pointer. | Retain the original table dispatch and `cmdGetNextCmd` body.  At its already-existing table boundary, copy only CS:IP, AX, BX, CF, `IsFirstCall`, `IsRepeatCall` and a locked read-only bounded Base VDM record-state value to a separate default-off report.  No command bytes, record pointer, guest/native address, BOP result or session state is changed. | `dos/command/cmddisp.c`; `../adapter-mvdm-host-out/{basesrv/include/base_vdm_local.h,basesrv/source/base_vdm_local.c,softpc/include/mvdm_softpc_termination.h,softpc/mvdm_softpc_termination.c}` |
| MVDM-HOST-DIV-192 | Read original permanent-COMMAND `DS:DX` COMSPEC text at `SVC_CMDCOMSPEC`. | NT4 treated the real-mode source as a process-address alias. That alias is invalid outside the session guest-memory owner even on x86. | Keep original `COMSPEC=` construction, `cbComSpec` calculation and `AL` disposition. Copy at most `MAX_PATH` bytes through one synchronous read lease; unavailable or unterminated source returns `CF=1/ERROR_INVALID_ADDRESS` without retaining a guest pointer. | `dos/command/cmdmisc.c`; `../adapter-mvdm-host-out/softpc/{include/mvdm_guest_location.h,mvdm_guest_location.c}` |
| MVDM-HOST-DIV-193 | Write original permanent-COMMAND initial environment at `SVC_GETINITENVIRONMENT` `ES:0`. | NT4 wrote the selected environment through a durable low-address alias. | Retain original environment construction, paragraph sizing and `BX` insufficient-buffer result. After that original capacity gate, write the unchanged `COMSPEC` and environment byte order through one synchronous session lease; an unavailable destination returns `CF=1/ERROR_INVALID_ADDRESS`. | `dos/command/cmdenv.c`; `../adapter-mvdm-host-out/softpc/{include/mvdm_guest_location.h,mvdm_guest_location.c}` |
| MVDM-HOST-DIV-194 | Exchange original COMMAND `CMDINFO`, executable path, command-tail and transformed-environment guest records at `SVC_CMDGETNEXTCMD`. | NT4 passed these 16:16 records as direct host aliases, including across the Base VDM client call. | Snapshot the fixed `CMDINFO` before the host call; keep Base VDM fields in local caller-owned buffers; lease executable-path, command and environment writes only while copying; then commit the original `CMDINFO` fields through a fresh synchronous lease. The executable path is committed after the original PIF/uppercase transformations, matching its original direct-alias visibility. No guest/native pointer survives the BOP. | `dos/command/cmdmisc.c`; `../adapter-mvdm-host-out/softpc/{include/mvdm_guest_location.h,mvdm_guest_location.c}` |
| MVDM-HOST-DIV-195 | Supply the original low-dependency DOS `.COM` `DEMOPEN` pathname and `DEMREAD` buffer without retaining a `GetVDMAddr` host alias. | The selected non-`i386` SIM32 form provides a native pointer, but a pathname/read buffer must not outlive the session-owned guest-memory boundary. | Keep original DEM pathname canonicalization, named-pipe branch, file/open/seek/error ordering, opaque `AX:BP` handle identity and CF/AX results. `demOpen` copies only a bounded `MAX_PATH` string; `demRead` takes one writable lease for the synchronous host call, commits only on its original success path, and discards it on every failure path. Lease refusal follows original DEM error direction with `ERROR_INVALID_ADDRESS`. | `dos/dem/{demfile.c,demhndl.c}`; `../adapter-mvdm-host-out/softpc/{include/mvdm_guest_location.h,mvdm_guest_location.c}` |
| MVDM-HOST-DIV-196 | Let original `cmdExec32` and its detached `cmdCreateProcess` worker retain the command, environment and standard-handle inputs needed by the original process lifecycle. | NT4 kept durable `GetVDMAddr` process aliases in globals until the detached worker consumed them. Those aliases are invalid across the session guest-memory boundary. | Before worker creation, copy the original bounded command tail, double-NUL environment and three 32-bit standard-handle identities into session-owned adapter state. The original COMMAND worker retains conversion, process, wait, exit-code and re-entry order; no guest pointer crosses the asynchronous boundary. | `dos/command/cmdexec.c`; `../adapter-mvdm-host-out/softpc/{include/mvdm_command_native_child.h,mvdm_command_native_child.c}` |
| MVDM-HOST-DIV-197 | Preserve original `cmdExec32` re-entry ordering while its newly created worker has not yet incremented the Base VDM re-entry count. | Historical BaseSrv holds `RETURN_ON_NO_COMMAND` during the narrow `CreateThread`-to-`INCREMENT_REENTER_COUNT` interval. A local one-session Base VDM provider otherwise reports immediate no-command and exposes a stale exit status. | Mark only the source-shaped pending launch interval in the Base VDM adapter. `RETURN_ON_NO_COMMAND` waits for the worker's original increment/decrement or another published record; it never synthesizes a command or changes the COMMAND `VDMINFO` sequence. | `dos/command/cmdexec.c`; `../adapter-mvdm-host-out/basesrv/{include/base_vdm_local.h,source/base_vdm_local.c}` |
| MVDM-HOST-DIV-198 | Correct the imported XMS move-callback declaration to the original `suballoc` contract. | The original `xms.h` prototype reverses source/destination relative to `suballoc.h::PSAMEMORYMOVEROUTINE` and both original XMS definitions. Function-pointer conversion would therefore conceal a real callback ABI mismatch. | Retain the original XMS callback name, width and move semantics, but declare its parameters as `Destination, Source, Count`, exactly as the original `suballoc` contract and original direct backends require. | `xms.486/xms.h`; `suballoc/suballoc.c`; `../adapter-mvdm-host-out/softpc/mvdm_xms_memory.c` |
| MVDM-HOST-DIV-199 | Observe the reached original COMMAND `SVC_GETINITENVIRONMENT` `ES:0`/`BX` and immediately-live `EndInit` arena scalar contract before and after its table call. | The current product must distinguish a BOP/CPU address mismatch from an original `SETBLOCK`/environment-layout limit without retaining a guest alias or changing the immutable guest retry. | At the existing original `CmdDispatch` table boundary, copy only `ES`, `BX`, `AX`, carry, `DS` and `SS:SP`; after the call, copy `DS:EnvSiz` (`203Ch`), original `ResSize` (`0592h`), and MCB size at `DS-1:0003h` through short read leases, plus CPU40's existing real-mode `MSW`/`DS` base/limit scalars. The adapter uses the app-captured report path that is removed before `cmdenv.c` reads inherited variables; no guest bytes, pointer, provider state or BOP result is changed. | `dos/command/cmddisp.c`; `../adapter-mvdm-host-out/softpc/{include/mvdm_softpc_termination.h,mvdm_softpc_termination.c,mvdm_guest_location.c}` |
| MVDM-HOST-DIV-203 | Distinguish a returned original Base VDM record from a post-command DOS error before selecting the next owner repair. | A second `54:01` proves only that immutable COMMAND returned to its acquisition loop; without the already-live `CMDINFO.ReturnCode`, `VDMINFO.ErrorCode` and `VDMState` scalars, it cannot distinguish normal command completion from a DOS/device failure. | At the original `cmdGetNextCmd -> GetNextVDMCommand` call boundary, append default-off copies of only the 16-bit guest return code, `ErrorCode`, `VDMState`, first/repeat flags and success/failure.  No `CMDINFO`/`VDMINFO` pointer, command bytes, guest address, session state or return value is changed. | `dos/command/cmdmisc.c`; `../adapter-mvdm-host-out/softpc/{include/mvdm_softpc_termination.h,mvdm_softpc_termination.c}` |
| MVDM-HOST-DIV-204 | Dispatch the historical normal-file `SVC_DEMFASTWRITE` selector. | On NT4, kernel `NTFastDOSIO` owns the fast-write trap. The former user-mode table reused `demNotYetImplemented`, which falsely cleared CF and made unchanged NTDOS report a successful write without invoking its normal write service. | One overlay handler returns only CF=1. The unchanged `$WRITE` caller then takes its original `SVC_DEMWRITE` fallback; no user-mode fast-write algorithm is invented or enabled. | `dos/dem/{dem.h,demdisp.c}`; `../mvdm-host-overlay/dos/dem/demfastio.c` |
| MVDM-HOST-DIV-205 | Observe original DEM create/write only while validating immutable COMMAND redirection. | NT4's direct `GetVDMAddr` aliases cannot be retained for host diagnostics in the current session boundary. | Default-off hooks copy one bounded create pathname or write scalar contract synchronously; they retain no handle, buffer, guest pointer or payload and do not modify DEM results. | `dos/dem/{demfile.c,demhndl.c}`; `../adapter-mvdm-host-out/softpc/{include/mvdm_softpc_termination.h,mvdm_softpc_termination.c}` |
| MVDM-HOST-DIV-206 | Stop the original SoftPC Console event worker during host close. | NT4 process termination made an alert-only request sufficient; a worker retaining a session binding cannot survive a one-process session teardown. | Preserve the original `nt_remove_event_thread` condition and alert direction, but delegate its post-alert join to one host-local adapter. It duplicates the source-owned thread HANDLE before alerting, waits for termination and exposes no guest pointer, guest handle or input semantics. | `softpc.new/host/src/nt_event.c`; `../adapter-mvdm-host-out/softpc/{include/mvdm_softpc_event_thread.h,mvdm_softpc_event_thread.c}` |
| MVDM-HOST-DIV-207 | Observe the existing Console-record to SoftPC 8042/PIC/CPU40 interrupt chain during the fixed interactive matrix. | The fixed container proves a Console event is consumed but cannot otherwise distinguish conversion/admission failure from a source-owned IRQ, PIC acknowledgement, BIOS transfer or EOI-continuity failure. | Default-off hooks copy only converted scan code, stopped/disabled/full flags, queue depth, selected PIC IRR/ISR/IMR and scan result, IRQ/vector, selected EOI line, and an already-high PIC suppression scalar immediately beside unchanged original operations. They do not retain input records, alter guest/8042/PIC/CPU/BIOS state, route BOPs, or add an input producer. | `softpc.new/{host/src/{nt_event.c,nt_cpu.c},base/{keymouse/keyba.c,system/ica.c,ccpu386/c_main.c}}`; `../adapter-mvdm-host-out/softpc/{include/mvdm_softpc_termination.h,mvdm_softpc_termination.c}` |
| MVDM-HOST-DIV-208 | Wake the original SoftPC CPU from its short idle wait when host activity arrives. | The original NT4 `PulseEvent` notification can be lost on modern Win32 when activity arrives immediately before the CPU marks itself waiting. | Retain the original auto-reset event and all idle counters/timing.  Signal that same event with `SetEvent`, which preserves one pending wake for the next original wait and wakes one existing waiter. | `softpc.new/host/src/nt_unix.c` |
| MVDM-HOST-DIV-216 | Observe the completed original CPU40 `RETF` source and destination while attributing the immutable explicit-`COMMAND.COM` child handoff. | Fixed BOP/COMMAND records prove only that NTDOS reached `$Exec`'s original `DOCLI -> STI -> retf`; they cannot establish whether CPU40 completes that far return without retaining a guest alias or changing execution. | After unchanged `RETF` destination and stack updates, copy only live source/target `CS:IP` into a bounded, default-off report. It neither selects an address nor reads/writes guest memory, flags, stack or session state. | `softpc.new/base/ccpu386/ret.c`; `../adapter-mvdm-host-out/softpc/{include/mvdm_softpc_termination.h,mvdm_softpc_termination.c}` |
| MVDM-HOST-DIV-217 | Attribute the original `demChMod` path/result while locating the first second-`COMMAND.COM` startup predecessor. | The source service dereferences an ephemeral `DS:DX` alias; a diagnosis must not retain it or alter the source attribute/error flow. | At original entry and all unchanged result exits, a default-off helper takes a bounded synchronous copy of the current path plus scalar result. It does not change attributes, handles, guest registers or carry. | `dos/dem/demfile.c`; `../adapter-mvdm-host-out/softpc/{include/mvdm_softpc_termination.h,mvdm_softpc_termination.c}` |
