# mvdm-host-overlay

Private implementation support for material modern bindings that cannot remain
inside an original `mvdm-host` mirror file.  It is callable only by the
matching mirror; it exports no product ABI to app, session, a test, or another
component.

## Divergence register

| ID | Original purpose | Reason | Implementation | Files |
| --- | --- | --- | --- | --- |
| MVDM-HOST-DIV-010 | Persist `pHimemA20State` as a native `GetVDMAddr` pointer and write it after each A20 transition. | A native guest pointer cannot remain valid across calls or carry x64 width. | Retain the original 16:16 numeric location; the private state records the bound session and obtains a fresh one-byte lease for each state write. A stale or unavailable binding performs no write, matching the original null-pointer branch. | `xms.486/xms_a20_state.h`, `xms.486/xms_a20_state.c`; mirror call sites `../mvdm-host/xms.486/xmsa20.c`, `../mvdm-host/xms.486/xmsumb.c` |
| MVDM-HOST-DIV-013 | Read and normalize a protected guest LDT-entry array through `Sim32GetVDMPointer`, then install it into the host x86 LDT. | A raw guest pointer is not durable on x64, and modern user mode cannot own the historical host LDT. | The private buffer acquires/releases one checked selector-bounded byte span around the unchanged source loop.  The original final `DpmiSetX86Descriptor` import returns its source failure direction until a later Bochs-native descriptor-publication package is admitted. | `dpmi32/dpmi_descriptor_buffer.[ch]`, `dpmi32/dpmi_descriptor_unavailable.c`; mirror call site `../mvdm-host/dpmi32/dpmiselr.c` |
| MVDM-HOST-DIV-014 | Fill the original DPMI memory-info response through a protected `Sim32GetVDMPointer` alias. | The original PVOID is an unbounded native alias of guest memory. | The private generic DPMI buffer performs one checked copy-in/copy-out around the unchanged original field sequence. | `dpmi32/dpmi_protected_buffer.[ch]`; mirror call site `../mvdm-host/dpmi32/dpmimemr.c` |
| MVDM-HOST-DIV-015 | Persist DPMI DOSX shared-data, DTA and selector-table raw pointers in process globals. | Those pointers are invalid across bounded leases and cannot represent a modern x64 Bochs guest. | A private per-session numeric state record preserves the original decoded values and field order.  It is not a mapper, guest pointer, host pointer or second CPU state. | `dpmi32/dpmi_session_state.[ch]`; mirror call sites `../mvdm-host/dpmi32/dpmi32.c` |
| MVDM-HOST-DIV-016 | Read DPMI protected interrupt/fault registration frames through an unbounded native stack alias. | Guest bytes cannot escape a checked span, and unchecked frame numbers cannot index session-local state. | Read the source-defined fixed frame once through the selector-blind protected-span facade, preserve field/order/INT21 notification/AX behavior for valid frames, and reject only malformed spans or out-of-range table numbers. | `dpmi32/dpmi_interrupt_registration.[ch]`; mirror call sites `../mvdm-host/dpmi32/dpmiint.c` |
| MVDM-HOST-DIV-017 | Export original `MS_bop_0` and `MS_bop_4` ingress bodies. | The full historical `nt_bop.c` aggregation imports unrelated PIC/CCPU/XMS/DPMI/WOW mechanisms and would introduce a second executor. Its `MS_bop_0` also dereferences an unsafe NT4 process alias. | A private source-shaped subset preserves both spellings, dispatcher/IP ordering and idle branch; its only semantic binding is a checked real-mode SAS byte read plus the same-shaped monitor idle notification. | `softpc.new/host/src/nt_bop_dem_command.c`; original reference `../mvdm-host/softpc.new/host/src/nt_bop.c` |
| MVDM-HOST-DIV-042 | Publish the original generated CVIDC vector and EVID tables. | The original `vglfunc.c`/`evidfunc.c` table definitions bind typed function-pointer slots to K&R-style four-host-word generated rules; this is an unchecked call contract on x64. | `Generate-CvidcTypedTables.ps1` derives private build-tree tables from the exact original headers and initializer order. Each table slot receives a typed carrier which enters the original rule through its native host-word ABI. The two original table-definition files remain byte-exact mirrors and are not edited. | `../../tools/build/Generate-CvidcTypedTables.ps1`; selected by `../../tools/build/New-T310OriginalSoftpcNinja.ps1`; generated files under `build/M0-T310/S8/.../generated/cvidc/` |
| MVDM-HOST-DIV-044 | Publish original base/video handler tables and update callbacks. | Several original units rely on late K&R declarations or cast VOID marking procedures through boolean callback slots; their x86 behaviour is not a valid x64 function-pointer ABI. | `Generate-T310BaseVideoTypedSources.ps1` generates private build-tree carriers that obtain later definitions' exact prototypes and retain the original mark operation while returning the already-established `FALSE` result to callers that discard it. Original mirror sources remain unedited, apart from the matching `cga.c` global declaration repair. | `../../tools/build/Generate-T310BaseVideoTypedSources.ps1`; selected by `../../tools/build/New-T310OriginalSoftpcNinja.ps1`; generated files under `build/M0-T310/S8/.../generated/video/`; mirror exception `../mvdm-host/softpc.new/base/video/cga.c` |
