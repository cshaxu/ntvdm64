# monitor family

Same-shaped historical VDM-monitor declarations and session-local state.
It never supplies a kernel VDM, CSRSS/CSR broker, CCPU executor or Bochs
object.

## Registered divergences

| ID | Original purpose | Reason | Implementation | Files |
| --- | --- | --- | --- | --- |
| ADAPTER-MONITOR-001 | NT4 `NtVdmControl` entered a kernel VDM product service for operations such as `VdmQueryDir`. | Modern Windows has no supported kernel VDM control service, and this product must not recreate kernel VDM, CSRSS or BaseSrv. | Retain the reached enum, `VDMQUERYDIRINFO` layout, function spelling and status shape. Dispatch only synchronous `VdmQueryDir` through a bound typed monitor-control capability; all other services return `STATUS_NOT_IMPLEMENTED`. The binding is cleared with its owning session and is intentionally separate from the BaseSrv-shaped command capability. | `include/vdm.h`, `include/vdm_control.h`, `source/vdm_control.c` |
| ADAPTER-MONITOR-003 | Make the per-thread VDM TIB's DPMI state and `pNtVDMState` visible to original `dpmi32` sources on either x86 or x64 host compilation. | NT4 supplied these through its private kernel/user VDM product composition, which is absent and must not be recreated; its public declaration is gated by `_X86_` even though the selected guest ABI remains x86 on both host widths. | Import the byte-for-byte OpenNT `vdm.h` declaration mirror through an adapter-local, immediately removed declaration gate; retain only `VdmTib` TLS binding and the fixed-low-address `pNtVDMState` replacement in this adapter header. There is no kernel VDM pointer, CCPU executor, cross-session broker or persistent guest alias. | `include/monitor_context.h`, `include/vint.h`, `mvdm_vdm_tib.c`; `../../opennt-abi/source/public/internal/base/inc/vdm.h` |
| ADAPTER-MONITOR-004 | Preserve the original DPMI interrupt/fault registration and full `VDM_TIB` layouts. | Modern Windows lacks the historical monitor object consumed by `dpmiint.c`. | Do not copy the records into adapter code: import their original definition and retain only thread-local session-worker storage. Do not install hooks, publish a host address, call a second executor or replace a later atomic frame transaction. | `include/monitor_context.h`, `mvdm_vdm_tib.c`; `../../opennt-abi/source/public/internal/base/inc/vdm.h` |
| ADAPTER-MONITOR-005 | Make the historical `NtCurrentTeb()->Vdm` lookup resolve to the reached DPMI TIB. | The adapter-owned modern TLS TEB has no native VDM field. | The bound session worker writes only its thread-local original-layout `VDM_TIB` address to the adapter-private TEB; the address never crosses a component ABI, guest boundary or worker lifetime. | `include/monitor_context.h`, `mvdm_vdm_tib.c`; `../win32/include/nt.h` |
| ADAPTER-MONITOR-006 | Preserve `HostIdleNoActivity`, invoked by the original DOS BOP ingress after non-date/time services. | The original body signalled a CCPU-owned `IdleEvent`; the product has no CCPU executor or idle waiter. | Retain the exact call spelling as a no-op activity notification. It creates no scheduler, machine state or BOP policy. | `include/host_idle.h`, `source/host_idle.c` |
| ADAPTER-MONITOR-006 | Provide the current WOW task's `vpStack`, `vpCBStack`, callback-init flag and `htask16` fields reached by original `CallBack16`. | Original `TD` also carries native threads, window/GDI handles and process pointers, which cannot be copied into x86/x64 session state. | Keep only the callback-reached numeric subset as bound-session TLS. Preserve the original callback-stack selection, subtraction and word-alignment order; reject unbound, duplicate and underflow forms. | `include/mvdm_wow_task_frame.h`, `mvdm_wow_task_frame.c` |
| ADAPTER-MONITOR-006 | Provide the current WOW task's `vpStack`, `vpCBStack`, callback-init flag and `htask16` fields reached by original `CallBack16`. | Original `TD` also carries native threads, window/GDI handles and process pointers, which cannot be copied into x86/x64 session state. | Keep only the callback-reached numeric subset as bound-session TLS. Preserve the original callback-stack selection, subtraction and word-alignment order; reject unbound, duplicate and underflow forms. | `include/mvdm_wow_task_frame.h`, `mvdm_wow_task_frame.c` |
