/*
 * DIVERGENCE: selected DEM source includes the historical private winbasep.h,
 * but no declaration from it is reached by any selected DEM body.  Preserve
 * only the same-name include carrier; do not import the private Base surface.
 */
#ifndef ADAPTER_WIN32_WINBASEP_H
#define ADAPTER_WIN32_WINBASEP_H
#endif
