# win32 family

Same-shaped historical Win32/NTDLL/OEM facade used by imported `mvdm-host`
source. It uses public modern Windows APIs where the original private NT4
product composition no longer exists.

## Registered divergences

| ID | Original purpose | Reason | Implementation | Files |
| --- | --- | --- | --- | --- |
| ADAPTER-WIN32-CRT-001 | Let the original SoftPC host own its CPU-environment lookup, diagnostic output, input, and assertion CRT interposition. | Static modern UCRT exports `getenv`, `printf`, `fprintf`, `fgets`, `gets`, `puts`, `fwrite` and `_assert` under the same names, while the original host intentionally defines source-specific meanings. | A build-only redirect header binds `getenv`/`fprintf`/`_assert` privately for original host translation units on both targets. The original `printf` family remains the Win32/x86 process-wide provider; only Win64 private-binds that family to coexist with UCRT. Original source control flow is retained and app/UCRT keep their normal CRT entrypoints. | `include/mvdm_crt_redirect.h`, `../../tools/build/New-T310OriginalSoftpcNinja.ps1`, original `mvdm-host/softpc.new/{obj.vdm/ntvdm.c,host/src/fprt.c,host/src/stubs.c}` |
| ADAPTER-WIN32-001 | NT4 exported RTL string, heap, TEB/PEB and current-directory helpers to `oemuni`. | Modern public SDKs do not expose the historical private product composition safely on both x86 and x64. | Preserve source-facing names and counted-string/NTSTATUS contracts; use public Win32 conversion, heap and directory APIs with adapter-private TLS state. | `include/nt.h`, `source/opennt_support_rtl.c`; declaration mirrors: `opennt-host/public/sdk/inc/{ntrtl,nturtl}.h` |
| ADAPTER-WIN32-002 | Reached OEM calls reuse `TEB.StaticUnicodeString` after temporary `RtlInitUnicodeString` rebinding. | A real modern TEB cannot provide the historical scratch field safely. | Restore only the adapter-owned TLS buffer before a non-allocating OEM-to-Unicode conversion. | `source/opennt_support_rtl.c` |
| ADAPTER-WIN32-026 | Provide the reached original `RtlCreateEnvironment`, `RtlDestroyEnvironment` and `RtlSetEnvironmentVariable` APIs. | NT4 supplied mutable, ordered environment blocks through its private RTL/PEB composition; modern public headers do not provide that source-facing contract. | Preserve counted-string and mutable UTF-16 MULTI_SZ semantics with host-private HeapAlloc backing and public environment APIs. The block never crosses a guest boundary. | `source/opennt_support_rtl.c`; declaration mirror `../../opennt-host/public/sdk/inc/ntrtl.h` |
| ADAPTER-WIN32-027 | Original SoftPC keyboard conversion calls the fixed-buffer `RtlOemToUnicodeN` form. | The selected OpenNT declaration subset now exposes the original contract, but the modern public SDK does not provide its complete historical source binding. | Preserve `NTSTATUS`, byte-count, fixed-buffer and OEM code-page semantics with public `MultiByteToWideChar`; the caller retains ownership of both host-local buffers. | `source/opennt_support_rtl.c`; declaration mirror `../../opennt-host/public/sdk/inc/ntrtl.h`; caller `../../mvdm-host/softpc.new/host/src/nt_keycd.c` |
| ADAPTER-WIN32-030 | Original SoftPC host input calls NT4 Console Server `GetConsoleInputWaitHandle`, `ReadConsoleInputExW`, `WriteConsoleInputVDMW`, cursor count and the reached VDM input/mouse coordinate operations. | Modern public console APIs expose equivalent input records and window coordinates but not the private server exports or cursor reference count. | Retain exact caller names/parameters; use the waitable `STD_INPUT_HANDLE`, `Read/PeekConsoleInputW`, `WriteConsoleInputW`, cursor visibility, `VDM_IS_ICONIC`, `VDM_CLIENT_RECT` and `VDM_CLIENT_TO_SCREEN`. Visibility returns a terminal sign rather than an accumulated server count, matching the reached caller loops. Every other selector fails with `ERROR_CALL_NOT_IMPLEMENTED` and remains S27 display/fullscreen work. | `source/console_compat.c`; declaration carriers `include/conapi.h`, `../../opennt-abi/source/public/internal/base/inc/conroute.h`; callers `../../mvdm-host/softpc.new/host/src/{nt_event.c,nt_mouse.c}` |
| ADAPTER-WIN32-031 | Original host-video callers use the NT4 Console Server DIB invalidation, output-palette and event-active call shapes. | Modern public Console APIs expose neither the DIB/palette operation nor the paired Console Server event protocol. | Retain the original names and parameter forms.  A bound session forwards typed dirty-rectangle/palette/activity events to an app-owned presenter; no presenter returns `ERROR_CALL_NOT_IMPLEMENTED` for BOOL operations, while the original void activity form only records session-local activity.  This adapter creates no controller, window or GUI. | `include/conapi.h`, `source/console_compat.c`, `../../../session/{session.c,session.h}` |
| ADAPTER-WIN32-032 | Original SoftPC fullscreen/detach paths register a VDM with the private NT4 Console Server using `RegisterConsoleVDM`. | The original operation duplicates hardware events and maps VDM state/text sections inside Console Server; modern public Console APIs expose neither that registration protocol nor an equivalent state mapping. | Preserve the complete function name and pointer-bearing ABI. For the selected windowed CPU40 path, acquire only the original caller's required host-local text buffer from the session-owned bounded text-plane contract; return a zero-length/null hardware-state mapping, which the original source accepts when fullscreen hardware is unavailable. App presentation can obtain only a copy through session, never this adapter-facing pointer. Fullscreen/controller event and shared Console-Server mappings remain unavailable. | `include/conapi.h`, `source/console_compat.c`; callers `../../../mvdm-host/softpc.new/host/src/{nt_det.c,nt_fulsc.c}`; `../../../session/session.[ch]` |
| ADAPTER-WIN32-033 | Original SoftPC configuration and host-input paths call the private Console Server `SetConsoleKeyShortcuts` and `ConsoleMenuControl` APIs. | Modern public Console APIs cannot reserve global shortcut combinations or attach a command-range event route to a console menu. | Preserve both source-facing names and parameter forms. `SetConsoleKeyShortcuts` returns `FALSE` and `ConsoleMenuControl` returns `NULL`, each with `ERROR_CALL_NOT_IMPLEMENTED`; no keyboard hook, system menu or partial event route is invented. | `include/conapi.h`, `source/console_compat.c`; callers `../../../mvdm-host/softpc.new/host/src/{config,nt_mouse,nt_hosts}.c` |
| ADAPTER-WIN32-034 | Original COMMAND `cmdkeyb.c` calls private Console Server `GetConsoleKeyboardLayoutNameA` for the active keyboard-layout name. | Modern public Win32 does not export the historical console-specific API. | Retain the original name, `LPSTR` parameter and `BOOL` result, forwarding only to public `GetKeyboardLayoutNameA`, whose fixed layout-name result and failure direction match the reached caller contract. No console broker or keyboard-layout cache is introduced. | `source/console_compat.c`; caller `../../../mvdm-host/dos/command/cmdkeyb.c` |
| ADAPTER-WIN32-004 | Original DEM `dem.h` uses the historical `devioctl.h` storage declaration layout and declares a local `GetDiskSpaceInformation` helper. | The modern SDK exposes an unrelated macro alias with that name and does not automatically select the source-mirrored storage carrier. | Expose the original storage carrier, then remove only the conflicting modern convenience macro. | `include/nt.h`, `include/ntioapi.h` |
| ADAPTER-WIN32-005 | Original DEM directory, volume and symbolic-link branches use selected `ntioapi.h` layouts and NT call shapes. | Modern public headers omit a complete compatible declaration set, while the byte-exact historical carrier cannot be included wholesale beside `winternl.h`. | Retain the reached source spellings and resolve matching `ntdll` exports at call time; missing exports return `STATUS_NOT_IMPLEMENTED`. | `include/ntioapi.h`, `source/ntioapi_facade.c`, `include/nt.h` |
| ADAPTER-WIN32-006 | Original DEM bodies require selected historical NT status/object/device constants, RTL list macros, and NT/RTL function declarations. | Modern headers omit a coherent NT4 source form and direct full historical header inclusion conflicts with the current SDK. | The adapter retains only its implementation binding; original NT/RTL declaration subsets are in `opennt-host`. | `include/ntioapi.h`; `../../opennt-host/public/sdk/inc/{ntrtl,nturtl}.h` |
| ADAPTER-WIN32-007 | Original COMMAND `cmd.h` includes the historical `vdmapi.h` declaration surface. | The previous local placeholder hid `VDMINFO`/`GetNextVDMCommand` declarations. | `include/vdmapi.h` is a declaration-only forwarding carrier to the byte-exact platform-ABI mirror; implementation remains session/monitor-owned. | `include/vdmapi.h` |
| ADAPTER-WIN32-008 | Original COMMAND `cmdkeyb.c` includes historical private console declarations from `winconp.h`. | Modern SDK headers do not carry this NT4-private declaration carrier. | `include/winconp.h` forwards the byte-exact declaration carrier. The one ANSI layout-query symbol supplied by this adapter is redeclared without the obsolete DLL-import attribute; all other private forms remain declaration-only. | `include/winconp.h` |
| ADAPTER-WIN32-009 | Original host-control `nt_bop.c` calls the historical NTDLL `DbgBreakPoint` declaration. | The selected modern SDK shape does not expose the declaration consistently through the otherwise required headers. | Its declaration is restored in the `opennt-host` source subset; the debugger family owns all behavior and no call route is enabled. | `../../opennt-host/public/sdk/inc/ntrtl.h` |
| ADAPTER-WIN32-010 | Historical x86 DPMI sources obtain a per-thread VDM TIB through `NtCurrentTeb()->Vdm`. | The public modern TEB cannot safely expose or own the NT4 VDM product field. | The adapter-private TLS TEB retains the reached field spelling; monitor binds it only to the current session-worker's TLS carrier. | `include/nt.h`; `../monitor/mvdm_vdm_tib.c` |
| ADAPTER-WIN32-041 | Original WOW32 stores and retrieves its current `TD *` through `NtCurrentTeb()->WOW32Reserved`; the `CURRENTPTD()` macro is an assignable lvalue and the original `wkman.c` lifecycle owns the allocation and ordering. | A modern public TEB intentionally exposes neither this NT4 WOW product slot nor a safe writable replacement. | Extend only the adapter-private TLS TEB carrier with the same-named host-private pointer field. Original WOW source continues to own `TD`, `CURRENTPTD()` and all task lifecycle semantics; this carrier neither maps guest memory nor creates a scheduler. | `include/nt.h`; original `../../mvdm-host/wow32/{wow32.h,wow32.c,wkman.c}` |
| ADAPTER-WIN32-042 | Expose the original USER/WOW callback declaration endpoints `PFNW32ET` and `WCB16_CDECL`. | The current SDK has already defined `_WINUSERP_`, so the byte-identical OpenNT `winuserp.h` carrier is skipped before it can declare the consumed callback type; this selected source cohort also excludes modern `wownt32.h`, whose export declarations conflict with original WOW32 bodies. | Provide only the same-named `VOID (APIENTRY *)(VOID)` typedef and public cdecl flag value `0x1`; no USER/GDI implementation, GUI policy, or callback route is selected. | `include/wow_user_declarations.h`; `include/nt.h`; source evidence `../../opennt-abi/source/public/internal/windows/inc/{winuserp.h,wowuserp.h}` and `public/sdk/inc/wownt32.h` |
| ADAPTER-WIN32-011 | Original Redirector DS declarations use the historical `NT_PRODUCT_TYPE`/`PNT_PRODUCT_TYPE` shape through `netlibnt.h`. | The current adapter-owned `nt.h` deliberately avoids wholesale NT4 SDK inclusion; the modern `winnt.h` basis omits this retired enum, while a post-`windows.h` `ntdef.h` include duplicates SDK definitions. | Preserve the exact three-value NT4 enum/pointer declaration locally. It adds no host service or product policy and leaves the original MVDM body unchanged. | `include/nt.h` |
| ADAPTER-WIN32-012 | Original SoftPC host-control sources include historical `conapi.h`, including `CONSOLE_GRAPHICS_BUFFER_INFO`, `APPKEY`, VDM console operation selectors, `SetConsoleKeyShortcuts`, `ConsoleMenuControl`, and `RegisterConsoleVDM`. | The byte-exact NT4 header reaches private kernel declarations and redeclares APIs/structures now owned by the modern SDK, so it is not a valid modern user-mode include closure. `ConsoleMenuControl` depends on Console Server command-range event injection; `RegisterConsoleVDM` additionally depends on Console Server/Win32k registration, event duplication and state/text section mappings. | Retain only the reached declaration shapes, exact VDM selector values, source-shaped `ConsoleMenuControl(HANDLE, UINT, UINT) -> HMENU`, and the full pointer-bearing `RegisterConsoleVDM` ABI. The selected windowed CPU40 provider may supply only its session-owned host-local text buffer; no system-menu, fullscreen, controller or hardware-event provider is fabricated. | `include/conapi.h`; `../../mvdm-host/softpc.new/host/src/{nt_mouse,nt_hosts,nt_event,nt_det,nt_fulsc}.c` |
| ADAPTER-WIN32-013 | Original `nt_error.c` keeps a dialog-local `PERRORDIALOGINFO` through `SetWindowLong(..., DWL_USER, (LONG)pointer)`. | The original pointer cast is not width-safe on x64 and `DWL_USER` has been superseded by `DWLP_USER`. | Keep the original per-dialog lifetime and result ordering through pointer-width public APIs; the pointer remains entirely inside the host window object and never becomes a guest/component identity. | `include/dialog_context.h`, `source/dialog_context.c` |
| ADAPTER-WIN32-014 | Original SoftPC `nt_mem.c` returns the NT4 virtual-memory status spellings for uncommitted, unallocated and undecommittable regions. | The current public user-mode declaration set omits these three historical constants even though their values and observable failure meanings remain stable. | Supply only the original `NTSTATUS` numeric declarations; no virtual-memory behavior, pointer conversion or memory policy is added. | `include/nt.h` |
| ADAPTER-WIN32-015 | Original SoftPC CMOS code uses the NT4 `TIME_FIELDS` layout and `RtlTimeToTimeFields`/`RtlTimeFieldsToTime` declarations. | The selected modern SDK route omits those still-public NTDLL declarations. | Their unchanged declaration subset is restored in `opennt-host`; behavior remains the system NTDLL implementation, and no CMOS policy is added. | `../../opennt-host/public/sdk/inc/ntrtl.h` |
| ADAPTER-WIN32-023 | Original SoftPC host sources call `NtWaitForMultipleObjects` with the NT4 `WaitAll`/`WaitAny` selector spelling. | Modern public user-mode headers omit the kernel-shaped `WAIT_TYPE` declaration. | Preserve only the original selector values in the adapter-owned declaration binding; a later selected NTDLL/public-host binding owns the call behavior. | `include/nt.h` |
| ADAPTER-WIN32-021 | Original SoftPC diagnostic source declares CRT wrappers with the historical `_CRTAPI1` macro. | Modern MSVC omits that retired macro. | Bind the original spelling to its historical `__cdecl` calling convention; no diagnostic function body is replaced. | `include/nt.h` |
| ADAPTER-WIN32-024 | Original SoftPC diagnostic source uses the paired `_CRTAPI1`/`_CRTAPI2` calling-convention spellings while intentionally interposing selected CRT stream functions. | Current UCRT defines those names inline by default and current MSVC omits `_CRTAPI2`; without graph-wide suppression selected original units emit competing inline definitions. | Bind `_CRTAPI2` to `__cdecl` and compile the selected MVDM graph with the documented `_NO_CRT_STDIO_INLINE` switch, preserving `fprt.c` as the original `printf`/`fprintf` provider and retaining all original caller bodies. Runtime stream endpoints remain a separate adapter boundary. | `include/nt.h`, `../../../tools/build/New-T310OriginalSoftpcNinja.ps1`, `../../../mvdm-host/softpc.new/host/src/fprt.c` |
| ADAPTER-WIN32-035 | Original process entry enumerates the NT4 CPU-environment registry values through `KEY_VALUE_FULL_INFORMATION`, `NtOpenKey` and `NtEnumerateValueKey`. | Modern public headers retain the native operation but omit this historical buffer declaration and call forms. | Provide only the documented record, selector and function declarations, leaving original registry/open/enumeration/conversion/failure flow unchanged. | `include/nt.h`, `../../../mvdm-host/softpc.new/obj.vdm/ntvdm.c` |
| ADAPTER-WIN32-036 | Original SoftPC/MVDM callers use the historic external `_sprintf` and `_sscanf` CRT symbols while `fprt.c` intentionally provides paired `printf`/`fprintf` interposition bodies. | Graph-wide `_NO_CRT_STDIO_INLINE` restores `fprt.c`'s historical ownership but modern UCRT no longer exports the two older symbols separately. `legacy_stdio_definitions.lib` exports all four symbols from one object and therefore conflicts with the retained original `fprt.c`. | `crt_compat.c` retains only the two historical variadic entrypoints and delegates their unbounded source contract to the matching modern UCRT common routines. No MVDM caller or original `fprt.c` body changes. | `source/crt_compat.c`, `../../../tools/build/New-T310OriginalSoftpcNinja.ps1`, `../../../mvdm-host/softpc.new/host/src/fprt.c` |
| ADAPTER-WIN32-038 | Original SoftPC timer and CCPU copy bodies call `RtlExtendedLargeIntegerDivide`, `RtlExtendedIntegerMultiply` and `RtlFillMemoryUlong`. | The current x64 NTDLL import surface omits these historical entrypoint spellings even though their fixed-width arithmetic/copy contracts remain sufficient. | Preserve the original signatures in the selected OpenNT declaration subset and implement only host-local signed 64-bit divide/multiply and repeated 32-bit pattern copy. No result becomes a guest pointer, host identity, or changed SoftPC control path. | `source/opennt_support_rtl.c`, `../../../opennt-host/public/sdk/inc/ntrtl.h`; callers `../../../mvdm-host/softpc.new/host/src/nt_timer.c`, `../../../mvdm-host/softpc.new/host/src/copy_fnc.c` |
| ADAPTER-WIN32-039 | Original WOW reset calls private USER client `ShowStartGlass`. | Modern public Win32 does not expose the compound Console-Server/USER server action, while the selected profile explicitly excludes GUI presentation. | Preserve the void name, argument and call order as a non-GUI presentation no-op. It writes no MVDM/WOW state and is not a USER/GDI provider; `NtRaiseHardError` remains separately unavailable. | `source/wow_private_unavailable.c`; callers `../../../mvdm-host/softpc.new/host/src/nt_reset.c`, `../../../mvdm-host/wow32/{wkman,wow32}.c` |
| ADAPTER-WIN32-040 | Original WOW hard-error handling invokes CSRSS `NtRaiseHardError`. | Public modern Win32 lacks the same broker and response contract. | Preserve the full NT4 ABI, set no fabricated response, return `STATUS_NOT_IMPLEMENTED`, and controlled-stop an actually bound session. | `source/wow_private_unavailable.c`; caller `../../../mvdm-host/softpc.new/host/src/nt_error.c` |
| ADAPTER-WIN32-043 | Original SoftPC `graphicsResize` requests the private NT4 `CONSOLE_GRAPHICS_BUFFER`, through which Console Server maps writable DIB bytes and a mutex; its source later supplies an `HPALETTE` through `SetConsolePalette`. | Modern public Console APIs do not expose graphics screen buffers or their mapped DIBs, and app must not depend on a host palette handle, but original CGA/EGA/VGA code directly renders into that byte plane. | Keep the original DIB descriptor, palette call and every original renderer. The adapter validates it, obtains a bounded session-owned byte plane, creates only a private mutex plus duplicate output lifecycle handle, and copies public palette RGB values into session storage. App obtains bounded pixel/palette snapshots, not raw storage or an `HPALETTE`. | `include/conapi.h`, `source/console_compat.c`, `../../../session/session.[ch]`; caller `../../../mvdm-host/softpc.new/host/src/nt_graph.c` |
| ADAPTER-WIN32-022 | Original SoftPC `nt_event.c` includes the private NT4 `winuserp.h` carrier. | The complete header belongs to the USER product shell and is not a modern public-user-mode closure. A source scan shows this selected file reaches none of its private declarations. | Preserve the include spelling as a public-SDK-only boundary header; no USER behavior or substitute declaration is supplied. | `include/winuserp.h` |
| ADAPTER-WIN32-025 | Original user-mode VDM declaration slices gate `NtVdmControl`/directory-query forms on the historical `_NTDEF_` marker normally set by NT4 `nt.h`. | The modern SDK supplies equivalent basic types but not the retired source marker. | Define the marker only in the adapter-owned declaration binding so the original user-mode declaration shapes are visible; no kernel VDM implementation or route is enabled. | `include/nt.h` |
| ADAPTER-WIN32-016 | Original SoftPC fixed-disk code calls the NT4 `NtQueryInformationFile`, `NtFsControlFile` and `NtDeviceIoControlFile` forms with a 32-bit `IO_STATUS_BLOCK`, `FILE_ALIGNMENT_INFORMATION`, and `FSCTL_QUERY_FAT_BPB_BUFFER`. | Modern `winternl.h` exposes native pointer-width status storage, while current public SDK headers retain the control code but omit the historical FAT-BPB response carrier. | Preserve original call names/layouts in the source-facing subset; restore the exact OpenNT 0x24-byte carrier, resolve public NTDLL exports at runtime, and copy only native completion status into the original-width host-local record. Missing exports return `STATUS_NOT_IMPLEMENTED`. | `include/ntioapi.h`, `source/ntioapi_facade.c` |
| ADAPTER-WIN32-028 | Original SoftPC sound opens `\\Device\\Beep` through the NT4 `NtCreateFile` form with the same 32-bit `IO_STATUS_BLOCK` carrier used by fixed-disk paths. | Modern `winternl.h` accepts a pointer-sized native status record, which is not source-compatible with the selected OpenNT call and would be width-unsafe on x64. | Preserve the original call name and parameter order; resolve public NTDLL `NtCreateFile`, retain native completion storage only at the public boundary, then copy the documented 32-bit status result back to the original host-local record. | `include/ntioapi.h`, `source/ntioapi_facade.c`, `../../mvdm-host/softpc.new/host/src/nt_sound.c` |
| ADAPTER-WIN32-017 | Original SoftPC timer/heartbeat source calls NT4 RTL integer arithmetic, critical-section and process-control forms. | The selected modern SDK headers do not expose a coherent compatible declaration subset. | Their original declaration subset is restored in `opennt-host`; NTDLL remains implementation owner and no timer/controller substitute is added. | `include/nt.h`, `../../opennt-host/public/sdk/inc/ntrtl.h` |
| ADAPTER-WIN32-019 | Original SoftPC and COMMAND worker sources pass cdecl callbacks to `CreateThread`. | Modern x86 requires a WINAPI thread start routine; direct use is ABI-unsafe, although x64 happens to share the calling convention. `nt_timer.c` returns `DWORD (LPVOID)` while `cmdexec.c` uses a no-argument `void` worker and `cmdredir.c` uses `void(LPVOID)` pipe workers; the original calls omit a session parameter. | Translation-unit-local bridges preserve the source callback forms, arguments and creation flags, capture the creator's process-local session binding, bind it only while the worker executes, then unbind it. The void forms return `DWORD 0` only at the public Win32 boundary. No guest pointer, fixed-width MVDM field or callback ABI changes. | `include/thread_start_compat.h`, `source/thread_start_compat.c`, `../../../mvdm-host/{softpc.new/host/src/nt_timer.c,dos/command/{cmdexec.c,cmdredir.c}}`, `../../../tools/build/New-T310OriginalSoftpcNinja.ps1` |
| ADAPTER-WIN32-037 | Original COMMAND changes `ntvdm.exe` standard handles before creating a non-DOS child. | The app's stdin/stdout/stderr are not session-owned and must remain unaffected by a child launch. | Preserve the source call sequence with a translation-unit-local interception: each worker records the selected handles and applies them only to a copied `STARTUPINFOA` at `CreateProcessA`; the parent process handles are never changed. | `include/command_process_compat.h`, `source/command_process_compat.c`, `../../../mvdm-host/dos/command/cmdexec.c`, `../../../tools/build/New-T310OriginalSoftpcNinja.ps1` |
| ADAPTER-WIN32-020 | Original SoftPC heartbeat shutdown calls private `NtAlertThread(HANDLE)` to interrupt an alertable NT wait. | That private NT4 thread-alert primitive is not a supported modern application API. | Keep the original name, handle parameter and `NTSTATUS` direction; queue a no-op APC through public `QueueUserAPC`, which wakes the original heartbeat's alertable wait. The original source separately recognizes its modern `STATUS_USER_APC` completion as the same termination event. | `include/nt_thread_alert_compat.h`, `source/nt_thread_alert_compat.c`, `../../../mvdm-host/softpc.new/host/src/nt_timer.c` |
| ADAPTER-WIN32-029 | Original SoftPC event, heartbeat and communications paths wait through `NtWaitForMultipleObjects` with `WaitAny`/`WaitAll`, NT timeouts and alertable completion. | Modern public headers omit the NT4 `NTSTATUS`-shaped call even though public Win32 exposes the same multiple-object and APC wait semantics. | Preserve the original function name, parameter order, `WaitAny`/`WaitAll`, 100-ns timeout direction and result values. Translate only at the boundary to `WaitForMultipleObjectsEx`; public APC completion returns the already-admitted `STATUS_USER_APC` direction. | `include/nt.h`, `source/nt_wait_compat.c`, `../../../mvdm-host/softpc.new/host/src/{nt_event,nt_timer,nt_ntfun}.c` |
| ADAPTER-WIN32-REDIR-001 | Original Redirector async-pipe worker calls `CreateThread` with a cdecl callback form. | Modern public `CreateThread` requires a `WINAPI` callback; calling the original form directly is ABI-unsafe on x86. | A forced source binding retains the original call text and fails with `ERROR_CALL_NOT_IMPLEMENTED` until S4 supplies the separately admitted session-worker thunk. | `include/mvdm_redirector_thread.h`, `source/mvdm_redirector_thread.c` |
