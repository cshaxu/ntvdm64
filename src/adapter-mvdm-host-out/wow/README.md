# wow family

This family preserves the historical WOW32/WOWEXEC host-product boundary.
The complete original WOW32 provider is selected on the x86 CCPU40 path. It
does not recreate NT4 USER, CSRSS, Win32k, a generic Win16 API, or a guest UI
server.

The prior copied callback transaction relied on retired machine-frame recovery
code. Its repository copy was removed; any retained comparison material is
external-only and is not a production source or build input.

## Registered bindings

| ID | Original purpose | Reason | Implementation | Files |
| --- | --- | --- | --- | --- |
| ADAPTER-WOW-001 | `wow32/wcall16.c` obtains temporary `VDMFRAME` and `CBVDMFRAME` aliases with `GETFRAMEPTR`, flushes a written callback frame, then frees both aliases before calling `host_simulate`. | The NT4 `FlatAddress` form exposes process pointers; a native pointer cannot cross the current session boundary or the recursive CPU call. The original `vpStack` is a packed 16:16 address, not a SAS-linear address. | `wow_callback_frame_lease` first calls the selected CCPU40 `mvdm_softpc_effective_address` contract, then enters only the `adapter-mvdm-host-out/softpc` bounded guest-pointer seam. That owner reaches the existing session guest-memory mapping instance. The original source retains task/frame algorithm, stack transfer, CPU resume, result and failure semantics; the selected `CallBack16` fixed-frame points now acquire, commit/release and reacquire at those original points. | `include/wow_callback_frame_lease.h`; `wow_callback_frame_lease.c`; `../softpc/{include/mvdm_softpc_guest_memory.h,mvdm_softpc_guest_memory.c}`; original target `../../mvdm-host/wow32/wcall16.c` |
| ADAPTER-WOW-002 | A late-loaded WOW32 DLL keeps the historical `Sim32FlushVDMPointer` function spelling after its selected CCPU profile makes that operation a no-op. | Under CPU40, original `softpc.h` defines this operation as `TRUE`; leaving the historical external declaration unresolved would incorrectly require a CPU30 pointer-cache provider. | `wow_sim32_pointer_compat.c` preserves the exact four-argument cdecl shape and always returns `TRUE`; it owns no pointer, mapping, cache or guest state. Callback-frame writes remain owned by ADAPTER-WOW-001. | `wow_sim32_pointer_compat.c`; original `../../mvdm-host/inc/softpc.h` CPU40 macro and `../../mvdm-host/softpc.new/host/src/sim32.c` declaration surface |
| ADAPTER-WOW-003 | `WU32RegisterClass`, `FindPWC`/`FindPWW` and the first `W32Win16WndProcEx` message route require NT4 USER's private class/window words. | Public USER32 has no `GCL_WOW*`/`GWL_WOW*` storage, and native HWND/pointer values cannot enter 16-bit guest data. | `wow_window_projection` registers a public class with a native proxy, retains only copied original-shaped class/window words in the current session, gives callbacks a session-local 16-bit window ID, and retires all records through the existing session teardown contract. Original WOW32 retains its message thunk and `CallBack16` ordering. | `include/wow_window_projection.h`; `wow_window_projection.c`; original targets `../../mvdm-host/wow32/{wuclass.c,walias.c,wmdisp32.c}`; parent WOW import contract in `tools/build/New-T310OriginalSoftpcNinja.ps1` |
| ADAPTER-WOW-013 | `WU32NotifyWow(FUN_FINALUSERINIT)` needs the private `pfnWowGetDefWindowProcBits` callback and matching `dwBldInfo` output field. | Current USER32 retains `UserRegisterWowHandlers` but leaves its NT4-private output table empty, producing a false `user.exe and user32.dll are mismatched` panel before any actual Win16 UI operation. | `wow_private_user_get_def_window_proc_bits` supplies only the source-shaped zero/default-procedure bitmap expected by the original USER16 handshake; `wuman.c` binds its absent output build field to the selected USER16 input value. It owns no USER state, window, message queue, task or UI policy. | `include/wow_private_user_compat.h`; `wow_private_user_compat.c`; original targets `../../mvdm-host/wow32/{wow32.c,wuman.c}` |
| ADAPTER-WOW-014 | `W32Thread` invokes the private `pfnInitTask` output callback before the first WOW task resumes. | Current USER32 leaves this NT4 slot null, causing an immediate execute-at-zero fault after `SYSTEM.INI` bootstrap profile processing. | `wow_private_user_init_task` validates the original task identity and accepts it into the standalone container, which already owns task lifetime; it deliberately owns no USER private task broker, window, or show-placement state. | `include/wow_private_user_compat.h`; `wow_private_user_compat.c`; original target `../../mvdm-host/wow32/{wow32.c,wkman.c}` |
| ADAPTER-WOW-015 | `WU32WOWGetIdFromDirectory` invokes the private `pfnWOWGetIdFromDirectory` output slot to select a grouped icon/cursor resource ID. | Current USER32 leaves the NT4 slot null even though its public resource selector is still present, causing an execute-at-zero fault once USER16 proceeds past boot. | `wow_private_user_get_id_from_directory` validates RT_ICON/RT_CURSOR and delegates only the directory selection to public `LookupIconIdFromDirectory`; it owns no USER state or resource lifetime. | `include/wow_private_user_compat.h`; `wow_private_user_compat.c`; original targets `../../mvdm-host/wow32/{wow32.c,wuser.c}` |
| ADAPTER-WOW-016 | `WU32LoadCursor` invokes the private `pfnServerLoadCreateCursorIcon` output slot after expanding a Win16 icon/cursor resource. | Current USER32 leaves that NT4 server-construction slot null. | `wow_private_user_load_create_cursor_icon` accepts only RT_ICON/RT_CURSOR: source-shaped split DIBs become a public colour icon plus AND mask or a hotspot-preserving monochrome cursor; a null-module integer standard resource instead recovers public `LoadCursor`/`LoadIcon`. Packed Win16 `HMODINST32` values are never native module handles, and no private USER cache is created. | `include/wow_private_user_compat.h`; `wow_private_user_compat.c`; original targets `../../mvdm-host/wow32/{wow32.c,wucursor.c}` |
| ADAPTER-WOW-017 | `WK32Yield` and `WOWDirectedYield16` invoke NT4 USER's private task-yield output slots after original WOW32 has created task threads. | Current USER32 leaves those slots null; direct concurrent CCPU simulation is invalid because the selected CPU state is session-shared. | `wow_private_user_yield_task` and `wow_private_user_directed_yield` retain the original task threads and synchronize their existing cooperative hand-off with per-task events. They neither create a USER task list nor treat the opaque USER thread identifier as a native handle. | `include/wow_private_user_compat.h`; `wow_private_user_compat.c`; original targets `../../mvdm-host/wow32/{wow32.c,wkman.c,wkgthunk.c}` |
| ADAPTER-WOW-018 | `WU32SignalProc` and `W32DestroyTask` invoke `pfnWOWCleanup` as an NT4 private USER-server teardown callback. | Current USER32 leaves the callback null; its first reached `SG_EXIT` caused an execute-at-zero terminal. | `wow_private_user_cleanup` accepts the historical module/task parameters and returns success. Public projected-window state is already session-owned, and the adapter creates no private USER objects to enumerate or destroy. | `include/wow_private_user_compat.h`; `wow_private_user_compat.c`; original targets `../../mvdm-host/wow32/{wow32.c,wuser.c,wkman.c}` |
| ADAPTER-WOW-020 | `WU32LoadMenu` invokes NT4 USER's private `pfnServerLoadCreateMenu` output slot after USER16 has found, sized, loaded and locked a Win16 `RT_MENU` resource. | Current USER32 leaves that server-construction slot null, while a projected class's packed Win16 module identity cannot be passed to public `RegisterClassA` as a native module. | `wow_private_user_load_create_menu` accepts only the already materialized public Unicode menu template and delegates construction to `LoadMenuIndirectW`; it neither interprets the packed module/name identity nor owns resource lifetime. The original `FindResource16 → SizeofResource16 → LoadResource16 → LockResource16 → output callback → UnlockResource16` sequence is retained at the actual `W32CreateWindow` class-menu boundary. | `include/wow_private_user_compat.h`; `wow_private_user_compat.c`; original targets `../../mvdm-host/wow32/{wow32.c,wumenu.c,wuwind.c,wres16.c}` |
