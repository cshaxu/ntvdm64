# softpc family

Same-shaped reached SoftPC/CCPU/SAS facade. For a Bochs-selected session it may
use typed `adapter-bochs` operations but never a Bochs type or global. For a
SoftPC-selected session, T310/S4 restores the original executable
`mvdm-host/softpc.new` call graph instead; this facade does not redirect that
path into Bochs or provide a fallback.

## Registered divergences

| ID | Original purpose | Reason | Implementation | Files |
| --- | --- | --- | --- | --- |
| ADAPTER-SOFTPC-001 | Define the historical SoftPC host error-table `ERROR_STRUCT`, `EH_*`, and `EV_*` declarations consumed by host bodies. | The byte-exact `softpc.new/host/inc/error.h` documents but does not declare these forms; the matching declarations survive in the byte-exact `softpc.new/base/inc/error.h`. | A build-island-only force-included same-shaped declaration bridge; no original mirror header or body is edited. | `include/error_abi.h` |
| ADAPTER-SOFTPC-002 | Preserve an original x86 MVDM host-pointer identity in a 32-bit DTA/FCB field. | `demsrch.c` directly stores `PFFINDLIST`; that pointer is neither width-safe on x64 nor permitted in guest/MVDM state. No original call-shaped identity API exists. | `mvdm_host_identity_*` resolves only through the thread-bound session's existing `host_resource` mapping instance. It publishes, resolves, reverse-looks-up and releases opaque surrogate32 values, retains `FFindId` as numeric source data, and never acquires guest memory. | `include/mvdm_host_identity.h`, `mvdm_host_identity.c` |
| ADAPTER-SOFTPC-003 | Retain a 16:16 guest address across a historical DEM callback and subsequently dereference it through SoftPC/Sim32. | A native `GetVDMAddr` pointer cannot survive across callbacks on x64 or across a bounded guest-memory lease. | `mvdm_guest_location_*` retains only segment/offset numeric data and obtains a fresh bounded lease from the thread-bound session for each access. It does not create a mapper, token or host pointer. | `include/mvdm_guest_location.h`, `mvdm_guest_location.c` |
| ADAPTER-SOFTPC-004 | Return mutable SFT/JFT pointers from the historical VDD file-handle APIs. | Original NTVDM permanently aliases guest RAM and stores a native `HANDLE` in a 32-bit SFT field; neither is legal on x64 or across bounded guest leases. | `mvdm_vdd_sft_shadow_*` returns session-owned host shadows only for the synchronous original caller, commits explicit final writes through fresh bounded leases, validates pointer lifetime with the existing host-resource map, and stores host HANDLE identities as opaque 32-bit values. | `include/mvdm_vdd_sft_shadow.h`, `mvdm_vdd_sft_shadow.c` |
| ADAPTER-SOFTPC-005 | Supply reached original `softpc.h` 16/32-bit `get*`/`set*` monitor imports for COMMAND, XMS and DPMI source bodies. | The original CCPU register storage is excluded in favour of Bochs, but the original source retains these exact call shapes. | `mvdm_command_registers.c` keeps the reached source names and widths, including `getCS` required by `xmsNotifyHookI15`, `getPE` and DPMI's 32-bit register forms. It calls only selector-blind typed `adapter-bochs` mechanics; guest bytes remain separate bounded leases. | `include/mvdm_command_registers.h`, `mvdm_command_registers.c` |
| ADAPTER-SOFTPC-008 | Supply the reached historical SAS A20 imports used by original XMS code. | CCPU/SAS is Bochs-replaced, but the original XMS helpers call the exact SAS spellings. | `mvdm_a20.c` preserves the three historical call forms and translates only the mechanical line state to `adapter-bochs`; it has no XMS dispatcher or UMB policy. | `include/mvdm_a20.h`, `mvdm_a20.c` |
| ADAPTER-SOFTPC-006 | Copy a short guest C string into host-local storage or copy a known host span to guest bytes for source bodies that formerly used `GetVDMAddr`. | The historic raw-pointer macro has no span or lifetime contract on x64. | `mvdm_guest_location_copy_c_string` takes one-byte synchronous leases until a supplied maximum/terminator; `copy_to_guest` uses one exact write lease. Both use the existing thread-bound session `guest_memory` instance. | `include/mvdm_guest_location.h`, `mvdm_guest_location.c` |
| ADAPTER-SOFTPC-011 | Original `v86/monitor/i386/sas.c` exposes scalar/string guest-memory operations while treating a numeric VDM address as a host pointer and reserving low process addresses. | That NT4 process-layout assumption is invalid on x64 and would bypass Bochs RAM bounds/ROM handling on both architectures. | Preserve the reached `sas_*` names, fixed numeric argument widths, little-endian scalar results and no-op/failure direction. The source-derived facade forwards only checked numeric reads/writes to `adapter-bochs`; it does not allocate a second SAS map, map host memory, create a mapping manager or expose a guest pointer. Its historical `*_no_check` spellings deliberately retain the same scalar result but still enforce the only safe Bochs bounds/ROM guard. | `include/mvdm_sas.h`, `mvdm_sas.c` |
| ADAPTER-SOFTPC-010 | `softpc.new/base/keymouse/keybd_io.c:UpdateKbdInt15` caches the BIOS INT 15h vector and invalidates it if guest code hooks the IVT. | The exact firmware translation unit includes the full historical keyboard controller, timer, IRQ and CPU-reentry product shell, none of which is a legal host-runtime input for this focused monitor binding. | `mvdm_int15.c` preserves the original public function name, 16-bit parameter form, IVT `15h` compare/update/reset ordering, and source-shaped SAS calls. Composition-only capture/reset hooks correspond to the original keyboard initialization lifecycle; they add no keyboard/IRQ/CPU behavior. | `include/mvdm_int15.h`, `mvdm_int15.c` |
| ADAPTER-SOFTPC-013 | Supply the transient protected-mode `Sim32GetVDMPointer` access shape reached by original DPMI descriptor code. | A protected guest span cannot be exposed as a durable native pointer across the modern x86/x64 boundary. | A selector-blind byte-copy facade resolves the source selector against copied active machine segments, then forwards one bounded read/write operation by neutral segment slot to `adapter-bochs`; the mirror's private overlay owns descriptor layout and lifetime. | `include/mvdm_protected_span.h`, `mvdm_protected_span.c` |
| ADAPTER-SOFTPC-014 | Supply the historical monitor register-operation shape when one original host routine changes protected CS/SS/EIP/ESP as a unit. | Immediate field setters would expose an invalid partial Bochs frame while the source operation is still running. | A thread-local copied expected/candidate frame retains the source's scalar widths and write order, then commits through the prevalidated same-CPL selector-blind `adapter-bochs` transaction. It exposes no DPMI/BOP/guest-object meaning. | `include/mvdm_protected_frame_transaction.h`, `mvdm_protected_frame_transaction.c`, `mvdm_command_registers.c` |
| ADAPTER-SOFTPC-015 | Preserve a historical 32-bit process-pointer result in a DPMI register pair. | The result must remain x86-shaped while neither a 32-bit nor 64-bit host pointer may become guest data. | Reuse the sole session host-resource mapping manager through `mvdm_host_identity_publish`; no new mapper, pointer split or guest-memory alias is added. | `mvdm_host_identity.c`, `include/mvdm_host_identity.h`; mirror caller `../mvdm-host/dpmi32/dpmiint.c` |
| ADAPTER-SOFTPC-016 | Supply the real-mode DPMI stack/interrupt-return work that historically borrowed `Sim32GetVDMPointer` stack and IVT aliases. | Host aliases cannot be durable on x86/x64 and cannot bypass session-owned guest-memory lease bounds. | Preserve the original numeric far-frame layout, IVT decoding, low-word FLAGS carry merge and no-result failure direction. Obtain only fresh bounded `guest_memory` leases and commit CS/SS/SP/IP/FLAGS through the selector-blind `adapter-bochs` frame primitive. | `include/mvdm_realmode_stack.h`, `mvdm_realmode_stack.c`; mirror caller `../mvdm-host/dpmi32/stack.c` |
| ADAPTER-SOFTPC-REDIR-001 | Historical Redirector source treats `GetVDMAddr`/`Sim32GetVDMPointer` results as x86 process addresses. | Bochs guest RAM and modern x86/x64 host memory are distinct; a raw alias cannot cross a component boundary or outlive a provider call. | A dedicated source include binding retains the original declarations and names but leases copied guest ranges only for one synchronous provider call. Protected-mode and asynchronous retention are rejected. | `include/softpc.h`, `include/mvdm_redirector_pointer_scope.h`, `mvdm_redirector_pointer_scope.c` |
| ADAPTER-SOFTPC-017 | Original WOW32 `GETVDMPTR`/`FLUSHVDMPTR`/`FREEVDMPTR` macros use a process-wide VDM pointer alias. | The historical alias is unsafe on x86/x64 and cannot represent bounded Bochs guest RAM. Redirector's fixed predeclared scope cannot model dynamic WOW structure lifetimes. | `mvdm_wow_pointer_scope_*` resolves one numeric real/protected VDM span, obtains only a synchronous `session.guest_memory` bounce lease, commits only at source-shaped flush, and rejects a retained/stale/nested scope. The protected resolver is selector-blind and validates native descriptor access before the session lease. | `include/mvdm_wow_pointer_scope.h`, `mvdm_wow_pointer_scope.c`, `include/mvdm_protected_span.h`, `mvdm_protected_span.c` |
| ADAPTER-SOFTPC-018 | Provide the reached non-fast WOW32 `SETVDMSTACK`, `VDMSTACK` and `host_simulate` SoftPC forms. | Original CCPU state cannot be linked, and direct CPU-field writes would violate the Bochs boundary. | `mvdm_vdm_stack_*` preserves 16:16 numeric stack formation and commits through one typed real-mode frame; `host_simulate` delegates only finite CPU resume/loop to `adapter-bochs`. | `include/mvdm_vdm_stack.h`, `mvdm_vdm_stack.c` |
| ADAPTER-SOFTPC-019 | Preserve the historical no-argument `host_simulate(void)` call shape after CCPU replacement. | The original executor has no modern safe ownership or typed stop result; returning a Bochs object or admitting service meaning here would alter the source contract. | The thread-bound session supplies only an opaque finite budget and receives a copied mechanical outcome. `mvdm_vdm_stack.c` invokes the selector-blind typed resume contract in `adapter-bochs`; BOP, selector and provider decisions remain outside this family. | `include/mvdm_vdm_stack.h`, `mvdm_vdm_stack.c`; `../../adapter-bochs/machine_lifecycle.{h,cc}`; `../../session/session.{h,c}` |
| ADAPTER-SOFTPC-020 | Preserve `host_simulate(void)` while one session chooses a machine backend. | T310 imports the original SoftPC candidate before its S4 machine composition is runnable; silently taking the Bochs path would falsify the selection. | For selected Bochs, retain the typed finite resume. For selected original SoftPC, record fixed `SESSION_MECHANICAL_STATUS_BACKEND_UNAVAILABLE` and perform no fallback. S4 replaces only that unavailable branch through original composition. | `mvdm_vdm_stack.c`, `../../session/session.{h,c}` |
| ADAPTER-SOFTPC-021 | Keep the Bochs-recovery rows in this register from redefining the selected original SoftPC executor. | Earlier records describe the existing Bochs-selected binding, while T310 makes original SoftPC the default selected backend. | Every existing `adapter-bochs` reference in this register is scoped to a Bochs-selected session. A SoftPC-selected session preserves the original source-shaped call graph and may not enter these Bochs mechanics. | This README; T310 S4 composition evidence |
| ADAPTER-SOFTPC-022 | Observe one outer original `host_start_cpu` invocation as a session-owned mechanical result. | `c_cpu_unsimulate` returns both nested BIOS calls and the outer invocation; modifying it to complete a session would confuse those two original lifecycles. | A narrow outer bridge binds the selected SoftPC session, calls the unchanged original `host_start_cpu`, records only `SOFTPC_RETURNED` after it returns, then unbinds. It owns no CPU, BOP, BIOS or guest meaning. | `include/mvdm_softpc_execution.h`, `mvdm_softpc_execution.c` |
| ADAPTER-SOFTPC-023 | Implement the reached, historically undefined `VdmSetPhysRecStructs(ULONG, ULONG, ULONG)` external-page binding seam used by original `nt_mem.c`. | NT4 treated the first `ULONG` as a raw host pointer; that is invalid and unsafe on both modern x86 and x64, while a no-op would lose source-shaped externally backed guest memory. | `mvdm_softpc_physical_mapping_*` publishes one stable 32-bit ID through the existing session `guest_memory` mapping-manager instance, validates the source span and alignment, binds it to the original Intel/SAS reservation, resolves only immediate CCPU physical access, and releases it on the original remove path or session teardown. It does not select by `i386`, use Bochs, copy backing bytes or expose a raw pointer outside the synchronous CCPU physical access. | `include/mvdm_softpc_physical_mapping.h`, `mvdm_softpc_physical_mapping.c`; mirror callers `../../mvdm-host/softpc.new/host/src/nt_mem.c`, `../../mvdm-host/softpc.new/base/ccpu386/ccpusas4.c` |
| ADAPTER-SOFTPC-017 | Original WOW32 `GETVDMPTR`/`FLUSHVDMPTR`/`FREEVDMPTR` macros use a process-wide VDM pointer alias. | The historical alias is unsafe on x86/x64 and cannot represent bounded Bochs guest RAM. Redirector's fixed predeclared scope cannot model dynamic WOW structure lifetimes. | `mvdm_wow_pointer_scope_*` resolves one numeric real/protected VDM span, obtains only a synchronous `session.guest_memory` bounce lease, commits only at source-shaped flush, and rejects a retained/stale/nested scope. The protected resolver is selector-blind and validates native descriptor access before the session lease. | `include/mvdm_wow_pointer_scope.h`, `mvdm_wow_pointer_scope.c`, `include/mvdm_protected_span.h`, `mvdm_protected_span.c` |

For a Bochs-selected session, machine operations use typed `adapter-bochs`
mechanics. For a SoftPC-selected session, the original `softpc.new` call graph
is restored without Bochs binding. This family never includes a Bochs type or
calls `bochs-core` directly.
