/* DIVERGENCE(BOP-DIV-107): XMS package configuration, call validation and
 * scoped composition have no separately composable OpenNT translation unit.
 * The project-authored body exceeds the mirror threshold, so this original
 * owner-family path retains only the private one-linkage boundary. */
#include "opennt-bop-overlay/xms/opennt_xms_composition.c"
