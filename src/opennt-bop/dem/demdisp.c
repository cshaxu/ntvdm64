/* DIVERGENCE(BOP-DIV-090): the reached DEM dispatcher subset exceeds the
 * mirror threshold.  It remains in the matching private overlay and is
 * included here so the original shared dispatch globals keep one linkage and
 * one observable state for all DEM provider bodies. */
#include "opennt-bop-overlay/dem/demdisp.c"
