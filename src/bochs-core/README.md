# bochs-core

Pinned Bochs 2.6 original mirror. This root is empty during M0 T272 S1.
Only `adapter-bochs` may call this component. Any later divergence follows the
mirror audit rules and is registered here.
