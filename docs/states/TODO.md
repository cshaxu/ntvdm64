# Debt Ledger

| Priority | Debt | Admission path |
| --- | --- | --- |
| P1 | Original DOSX INT21/4A host-reallocation failure corrupts descriptor state: dxmmgr.asm::ModifyXmemBlock records failure but falls through mxb60, treating input BX:CX as the new block address and applying requested limits before testing Success. A targeted host failure retains the old block yet the real guest publishes base 4570h, then a descriptor from that wrong header. See [S38 evidence](../etc/evidence/m0-t420-s38-dpmi32-capability-review.md). | Register under standing immutable-guest authority; preserve the failing reproducer, never count it as rollback success. Do not patch guest or fake host allocation success. Returning only the old address cannot correct the guest's retained new-size limit. This disposition does not exempt the separately corrected portable host BOOL contract or other host allocation defects. |
| P2 | Original DOSX INT21/48 allocation-failure error propagation: after an injected host allocation failure, axb130 queries XMS free space and pmallc returns that AX rather than DOS error 8. Actual AX=3A00, BX=FFFF, CF=1 is captured in [S38 evidence](../etc/evidence/m0-t420-s38-dpmi32-capability-review.md). | Under standing owner authority, preserve immutable guest and the strict failing error-code reproducer; do not normalize it in host. Separately verify failed-growth data preservation and release. This does not exempt host allocation defects. |
| P2 | Original guest remote-printer setup limitation: `macro.asm::$UserOper` documents forwarding printer-info calls without corresponding redirector remote-print support. Real/protected 5E02/5E03 with invalid index FFFF return CF=1/AX=1 consistently; this is not working printing. See [DPMI32 evidence](../etc/evidence/m0-t420-s38-dpmi32-capability-review.md). | Preserve immutable guest and the negative comparison under standing owner authority. Do not introduce a replacement print provider. Other indices and any future externally supported printing contract need separate evidence; current tests do not prove them. |
| P2 | Original guest Redirector spool multiplex defect: `srvcall.asm::$ServerCall` requests INT 2Fh/AX=1125h for DOS 5D07--5D09. The resident handler is installed (`1100h -> AL=FF`) and receives 1125h, but `resident.asm::NetSpoolStuff` then compares that AL=25h against 7/8/9 and returns CF/AX=1. Thus direct real-mode and protected 5D07 both reproduce invalid function; this is not a DPMI translation loss. See [S38 evidence](../etc/evidence/m0-t420-s38-dpmi32-capability-review.md). | Preserve the immutable guest and its negative probe. Do not patch the guest dispatch, fake a spool provider, or count 5D07--5D09 as working spool service. A future remedy requires a separately admitted source-policy-compatible design and real spool lifecycle/teardown acceptance. |
| P2 | Owner-accepted original INT31/0500 invalid-buffer limitation: the original host writes its 36-byte memory-information structure without validating the complete ES span, and the original DOSX return path clears CF. A reduced-limit probe reproduces the write while deliberately violating the required valid-buffer precondition. See [DPMI32 evidence](../etc/evidence/m0-t420-s38-dpmi32-capability-review.md). | Preserve the negative reproducer and immutable guest; no invented error policy or guest patch. This specific owner acceptance is not a blanket exemption for original host defects or project host/adapter failures. Normal-input successes do not prove invalid-span protection. |
| P2 | Owner-accepted immutable DOSX debug-service limitations: INT31/0B02 reports inverted/stale watchpoint status; rejected invalid watchpoint requests can retain a slot; task exit does not automatically release watchpoints, while explicit release works. See [DPMI32 evidence](../etc/evidence/m0-t420-s38-dpmi32-capability-review.md). These are reproduced limitations, not passing capabilities. | Retain original guest media and reproducers. Any future remedy requires an explicitly admitted, source-based and guest-immutability-compatible design. Do not patch guest code/data or add launcher/broker debug policy. This acceptance excludes unresolved host binding, memory-span and PM-stack lifecycle issues. |
| P3 | Original suballocator partial callback failure rollback is incomplete: fixed-size FreeChunk restoration omits trailing bitmap bytes, and earlier successful commits/decommits are not undone. The selected original CPU40 SAS callbacks have no such fallible operations after local zeroing removal; this is not an active product failure. See [suballocator evidence](../etc/evidence/m0-t420-s36-suballoc-capability-review.md#final-capability-ledger-and-closure). | Before admitting a fallible backing commit/decommit provider, verify whole-operation rollback of metadata and data, including compensation failure; do not patch only the bitmap or revive local clearing callbacks. |
| P1 | Confirmed original guest COMMAND.COM bug: with low DOS and a sufficiently large inherited environment, `rdata.asm::EndInit` releases INIT, then retains references to INIT-resident `EnvSiz` and `Alloc_error`. A valid in-bounds environment copy overwrites EnvSiz (`0118 -> 4543`) and the subsequent guest comparison enters the discarded error path. This is not an XMS allocator or host lease overrun. The owner explicitly accepts registration as an unresolved original-guest bug; see [direct copy evidence](../etc/evidence/m0-t420-s35-xms-capability-progress.md#direct-environment-copy-witness). | Retain the immutable original guest and the reproducer. No guest rebuild, binary/runtime patch, environment truncation, injected /E:, forced DOS=HIGH or allocator special case is authorized. Any future remediation requires a separately approved policy-compatible proposal and ordinary-environment low-DOS regression; this debt is not a passing capability result. |
| P1 | Audit and synchronize the sibling SoftPC privilege-changing stack-width repair, reference commit `ce5f53515d3e6ce0a64e66a5465aa7a8fbca00f7` in the sibling SoftPC repository (2026-09-19). Current `src/mvdm/softpc.new/base/ccpu386/{call.c,iret.c,ret.c}` still select SP/ESP by operand size after loading a new SS, as does the pinned OpenNT source. The existing `c_stack.c::set_current_SP` instead uses the new SS address width; the original interrupt path already uses it. The candidate replaces twelve executable lines with three, without a new helper, adapter or overlay. This is not evidence that it causes the current DOSX/WOW or XMS failure. | Admit a bounded source-policy-reviewed CPU stack-transition profile before implementation. Verify actual cross-privilege CALL, RETF and IRET with 16/32-bit operand and SS-width combinations, high ESP bits, frame contents and fault behavior; the sibling's helper test alone is insufficient. Then run formal x86 and all existing direct/nested COMMAND, MEM and EDIT regressions. Existing FIST64 rounding, setjmp dereference and pre-dispatch PIC rejection fixes must not be duplicated; sibling snapshot and host-allocation changes are not directly portable. |
| P2 | Audit two sibling SoftPC 8042 repairs from the same `ce5f53515d3e6ce0a64e66a5465aa7a8fbca00f7` reference: `src/mvdm/softpc.new/base/keymouse/keyba.c` still marks command `0xC0` output valid without assigning `input_port_val`, and still clears IRQ1 after reading the output buffer only under `JOKER`. The input-port assignment is a concrete missing operation; unconditional IRQ deassertion requires review against this product's PIC/event ordering before adoption. | Admit the finite controller/PIC boundary after source-policy review. Test the initialized input-port response, masked IRQ1 polling, buffer-read deassertion, queued subsequent bytes and normal interrupt delivery, followed by existing keyboard/mouse and COMMAND/MEM/EDIT regressions. Keep original controller ownership and minimal mirror diffs; do not bulk-port the sibling commit or treat this debt entry as repair admission. |
| P1 | Full WRITE lifecycle remains unproved; preserve the malformed-arena/environment evidence and unaccepted provider/transition WIP without promoting it to a product baseline. | Owner review of the original-owner audit, followed by an active recovery packet. |
| P2 | MEM.EXE reports 4294939856 as largest executable program size alongside 655360 conventional bytes. Cause and impact remain unproved; see [integration evidence](../etc/evidence/m0-t406-s1-physical-mapping-design.md#p4-ordinary-program-integration-mapping-coverage). | Original DOS allocation/query and memory-provider owner audit, preferably assessed with the DPMI/XMS candidate; no repair admitted by the mapping observation. |
| P2 | The original 8253 mode-zero elapsed-counter calculation may report subsequent terminal-count periods differently from NTVDMx64's comparative patch. No current workload reproduces a timer fault. | Admit only after a real DOS/Win16 timer symptom; then compare the exact mode-zero first-count and 65536-wrap contract with focused positive and negative tests. |
| P1 | Preserve a reproducible Bochs 2.6 source/notice/patch manifest after T95 imports the local baseline. | Source-policy review and manifest verification. |
| P1 | Keep the Bochs build surface minimal as later guest callers request features. | Per-feature OpenNT caller evidence, boundary rationale and negative test. |
| P2 | Produce a neutral Bochs/OpenNT hard-boundary report for later `ntvdm64` architecture work. | First admitted bridge slice with no runtime dependency on `ntvdm64`. |
| P2 | Research original OpenNT shell-out/session-broker paths for a DOS guest request to launch or wait for a Win32 program, a WOW16 program, or another VDM. | Establish exact OpenNT caller/ABI/failure maps and decide whether a modern non-invasive, explicitly permitted host-launch capability exists; otherwise retain an explicit permanent deferral. |
| P1 | Complete real external COM transmit/receive acceptance for the retained original `rs232_io.c` INT 14h sender/receiver and `com.c` → `nt_com.c` provider path. The current host has only `COM3` (`usbser` USB Billboard), with CTS/DSR both low, so the original CTS gate correctly prevents external transmit; no product diff, overlay, provider or fake success is retained. The test-only direct-source mock covers the original provider's successful open/write/read/close contract but does not substitute for a real peer. | Supply a CTS-capable paired/null-modem or loopback serial endpoint, or explicitly admit a virtual serial-pair provider. Re-run the S30 real DOS guest transmit, host-driven receive, failure and teardown matrix; see [S30 external COM boundary](../etc/evidence/m0-t420-s30-external-com-boundary.md). |
| P2 | Reconcile T271's residual BOP/package recovery evidence with the canonical `mvdm-host` rebootstrap: five compatibility headers, app-held selector/service composition, and test-to-overlay privacy. | During rebootstrap, classify each residual as an original MVDM package unit, app composition, session/broker state, or a declared adapter contract; use `m0-t271-closure-20260825.md` and its P10 ledger as historical evidence, not as a source-layout precedent. |
| P3 | Migrate indexed legacy documentation without breaking provenance links. | One manifest-backed migration batch with link verification. |
This ledger contains no active work, roadmap, or completed-task record.
