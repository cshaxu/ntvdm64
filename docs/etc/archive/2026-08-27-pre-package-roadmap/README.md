# Pre-package-roadmap archive

This directory retains the exact Queue authority superseded on 2026-08-27 by
the package-recovery program.

Existing `proposal-*.md` records remain at their original
`docs/etc/operations/` paths because closed Status/history records link to
them. They are historical supporting evidence, not candidates in the current
Queue. Moving them in bulk would break provenance links and violate the
documentation rule against mass-moving retained evidence.
