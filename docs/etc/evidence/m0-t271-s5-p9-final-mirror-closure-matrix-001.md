# M0 T271 S5 P9 — final mirror-closure matrix

## Question

After P8 remediation, does the complete `opennt-bop` production input meet
the admitted T271 mirror/source-recovery closure criteria?

## Inputs

- S5/P1--P8 evidence and the current `opennt-bop`/overlay README registers.
- Formal Ninja root `build/M0-T271-S5/r001`.
- All private-overlay boundary scans and source-path fixtures.

## Procedure

1. Re-ran the register-quality scan: no task-labelled/free-form
   `DIVERGENCE` marker may remain in either BOP root.
2. Re-ran the private-overlay consumer scan across app, session and every
   adapter.
3. Rebuilt and executed the formal source-path matrix: DEM native session,
   COMMAND native session, XMS source mirror, Redirector failure/helper,
   config-complete and no-support; then memory-size, tape/INT15 and
   no-EMS SoftPC overlays.  P8 separately rebuilt/ran interrupt-06, mouse,
   printer, keyboard-handoff and config-complete fixtures.
4. Regenerated the documentation inventory and ran production-tree,
   documentation-governance and whitespace gates.

## Observations

- Every live input is classified as an exact source input, declared true
  subset, minimal same-shaped source modification, or one of the 13 named,
  private, source-specific overlay bodies.  Each overlay has a sole matching
  mirror translation-unit include boundary and no external consumer.
- No project-authored ingress, route, observation or session implementation
  remains below `src/opennt-bop`; final fixed-frame/session composition is in
  `app/bop`, and the typed interrupt-06 operation state is in
  `adapter-softpc`.
- The formal matrix compiled/linked and returned zero.  It includes source
  entry/table paths for DEM, COMMAND and XMS, plus all 13 overlay owner
  groups: COMMAND, DEM, SoftPC, VDMREDIR and XMS.
- Production-tree and documentation-governance gates pass; `git diff --check`
  passes; the stable-marker and external-overlay-consumer scans are clean.

## Interpretation and confidence

S5's technical exit evidence is complete.  `opennt-bop` now meets the mirror-component standard for its
admitted provider set: it contains no generic project composition root, and
the remaining exceptional bodies are source-cited, registered, private and
locally verified.  The small number of overlays is a proved set of
source-specific product-closure gaps, not a generic compatibility layer.

This is source/package code closure.  It does **not** claim that deferred
DPMI, WOW16, Redirector lifecycle, VDD/debugger, full SoftPC device product
shells or continuous native guest execution are complete; their current
source/failure dispositions remain owned by their separately admitted future
packages.

## Follow-up

The owner may now formally close T271 and select/admit the next Queue package;
no trace-led leaf implementation is authorized by this evidence.
