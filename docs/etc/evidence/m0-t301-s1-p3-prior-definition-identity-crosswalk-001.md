# M0 T301 S1 P3 — Prior Physical Definition Identity Crosswalk

T301 compared its frozen one-degree calls with the earlier approved-original
physical-definition ledger without merging spellings. The crosswalk contains
142 rows: 64 current call identities, 19 spellings and 39 distinct original
definition identities.

The principal candidate packages are Base Win32 client VDM/date code and
selected USER/GDI client bodies. Each row remains an unselected physical
candidate: T301 still needs caller-compatible declaration/variant selection
and a static final disposition. This is source provenance recovery only; it
does not import, link or enable a provider.
