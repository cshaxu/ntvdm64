# M0 T301 S1 P20 — `one-ledger1` Physical Signature Gate

P20 examined only the bounded signature regions for P19's 310 unique-source,
declaration-reachable candidates: the original direct call, reachable header
declaration token and original C/C++ definition signature.  It also recorded
the caller and definition preprocessor contexts.  It did not scan a candidate
function body or emit a second-degree call.

- 251 rows have matching call/declaration/definition arity and are recorded as
  future physical body-expansion seeds.
- 59 rows remain one-degree unresolved because historical syntax or conditional
  form prevented a complete shape confirmation.

A passing row is not a package, provider, adapter, link or runtime admission.
The next action—reading the 251 source bodies and emitting only their direct
external calls—requires separate S admission because it begins degree two.
