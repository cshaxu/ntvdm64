# T136 S1 prefetch-predecessor observation closure 001

## Change

`BX-TRACE-056` is implemented in the original Bochs
`BX_CPU_C::prefetch` `EIP > CS.limit` branch. It is default-off. When enabled
in real mode it reports only current CS, existing `PREV_RIP`, current EIP,
and the already-read CS limit, before the unchanged original #GP call.

It has no guest-memory access, guest address filter, instruction decoder,
BOP/adapter branch, state write, device operation, or altered exception
delivery.

## Build closure

The named r4 derivative is generated by
`tools/New-T136S1PrefetchPredecessorDerivative.ps1`. It retains the T130
engine shape and compiles `cpu/cpu.cc` once as `cpu/cpu_trace.obj` with
`BX_NTVDM_ENABLE_PREFETCH_PREDECESSOR_DIAGNOSTIC=1`. The shim places that
object before the retained `cpu/libcpu.a` and does not declare an archive
target.

The MSVC x86 command recorded by the r4 manifest completed successfully.
Its known pre-existing link warning is `LNK4098` (`msvcrt.lib` conflict),
already governed by T132; it is not masked or treated as a new CRT recipe.

## Verification

- `tests/bx-ntvdm-adapter/Test-BochsPrefetchPredecessorBoundary.ps1` passes.
- The r4 map resolves `BX_CPU_C::prefetch` to `cpu_trace.obj`, proving the
  direct object wins over the retained archive.
- r4 hashes:
  - executable: `0EF41EF1D1796BB79BFD99D14BD906D5365BE8D5850FA98B007FCBDC245831EE`
  - `cpu_trace.obj`: `5E5BEC1C631C2EBD01D23849374CA4695727186BDA53EB6CCE941CFB4C468FC8`
  - shim: `EB50B03239C243884D8CFBDE7D71DFB100D1EDADD5D70817A7D4863C02AEFFD8`

## Disposition

The next admissible action is one bounded, unchanged-profile observation run
that looks only for the new prefetch record and preserves the existing
watchdog/termination discipline. No BOP or provider work is admitted by this
diagnostic.
