# M0 T195 S6: Repaired Boundary Closure

## Closure Question

Does the current minimal closure prove that Bochs is selector-blind and that
the two former semantic intrusion groups are adapter-owned, while retaining a
reconciled 58-entry exception ledger?

## Reproducible Closure

The fresh root is
`artifacts/build/t195-s6-repaired-closure-r1`. It was generated by:

```powershell
powershell.exe -ExecutionPolicy Bypass -File tools/New-T98S1CurrentAdapterEngineDerivative.ps1 `
  -BuildRoot O:\repos.hobby\ntdos64\artifacts\build\t195-s6-repaired-closure-r1 `
  -MachineComposition
```

Then, from that root, MSVC x86 was initialized with `vcvars32.bat` and ran:

```text
nmake /nologo /f ntdos64-t98-current-adapter.mak ntdos64-t98-current-adapter.exe
```

The link succeeds. It retains the known pre-existing CRT warnings from the
original `illegalp.c.obj` island (`LNK4098` and its UCRT `sprintf` import),
with no unresolved import or migration-specific warning.

## Final Map Facts

The final map contains:

- `_bx_ntvdm_exception_observer_v1_observe` in the adapter observer object;
- `_bx_ntvdm_machine_composition_v2_dispatch` and mechanics compatibility
  imports in the adapter machine-composition object; and
- `_unexpected_int` and `_illegal_op_int` in their unchanged original object
  islands.

It contains none of `machine_bop`, `bop_catalog_listener`, or
`machine_composition_v1_probe`.

## Negative Source And Ledger Checks

A source/current-generator scan rejects the obsolete listener macro, listener
symbol, v1 machine component/probe, old machine include/object path, fixed
`prev_rip + 3` decision, the two historical handler names, and `02h`/`06h`
tokens in Bochs. The corresponding machine identity exists only in adapter
source.

The exception register was parsed by table first-column ID and compared to the
S1 audit IDs: `register=58`, `audit=58`, `missing=0`, `extra=0`.

The adapter observer, generic #UD seam and machine-composition boundary tests
all pass. These checks prove source/build ownership, not guest runtime reach
or continuous execution.

## T195 Outcome

T195 closes its repair scope:

1. BOP catalogue identity/observation is adapter-owned.
2. Historical `02h`/`06h` selection, original-handler invocation and resume
   decision are adapter-owned.
3. Bochs retains generic copied event delivery, checked port/RAM/stack/report
   mechanics and typed result application.
4. The external-intrusion register records migrated, removed, retained and
   historical rows without treating its count as a live patch count.

The closure does not claim that BOP providers are fully implemented, that
every BOP has executed, or that the guest runs continuously. Those runtime
questions belong to the next admitted task after the queue's required
Bochs-intrusive-change reconciliation.
