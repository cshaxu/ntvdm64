# T177 S4 native POST and machine-composition observation 001

## Inputs and procedure

The fresh derivative at
`artifacts/build/current/t177-s4-native-post-machine-composition-r1` was
generated by the existing derivative generator with both
`-DeferredStartupPlan` and `-MachineComposition`. Its manifest declares two
Bochs replacements only: `main.o` and `cpu\\exception.o`; it retains the CPU,
I/O-device, memory and GUI archives. The generated makefile declares the
existing three machine objects (`bx_ntvdm_machine_bop_v1.obj`, `unexp_nt.c.obj`
and `illegalp.c.obj`) and no archive prerequisite.

One MSVC/x86 `nmake` link succeeded. The only diagnostics were the retained
`exception.cc` narrowing warning and the already-known CRT/default-library
warnings from `illegalp.c.obj`. The resulting executable SHA-256 is
`8758F4335CB32B4FB97688ED3860E8B0C9E86D8155E6AA316E54F346177E8BFA`.

Exactly one existing deferred-startup observer invocation used the retained
POST option ROM/profile, one-million benchmark and 15-second watchdog. Its
immutable record is
`artifacts/analysis/t177-s4-native-post-machine-composition-observation-001/observation.json`;
the Bochs log SHA-256 is
`9FCD7622BC09460599671F181D52901027BFC6F65A3E4115C8BD3FDBB248E818`.

## Observation

| Marker | Result |
| --- | --- |
| Object/build boundary | Passed: two Bochs objects only; no archive rebuild or source edit. |
| Native POST/deferred guest flow | Reached normal `50:11` NTIO transaction, full COMMAND fast-read, and accepted `50:36` resume. |
| BOP/transaction totals | 31 observed BOPs and seven committed transactions. |
| Selector `06h` | Zero observations. |
| Machine-composition outcome | Zero log records. |
| Direct-entry aperture/stack failure | Absent. |
| Terminal condition | `cpu loop quit`, exit code 1, no watchdog termination. This is not a DOS/CLI completion claim. |

## Interpretation

The source-declared combined form links and executes inside the native
POST-initialized lifecycle. It removes the direct-pre-POST confounder from the
earlier machine-composition observation. Under this bounded real path,
selector `06h` is simply not reached, so neither its original SoftPC handler
nor any machine BOP result explains the historical stack fault.

No BOP provider, device, vector, firmware or adapter behavior was changed.
The next CLI-result work must start from the separately observed `50:36`
guest-transfer boundary rather than treating this exit code as a DOS result.
