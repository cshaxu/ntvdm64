# M0 T280 S13 — DEM shared NT/RTL object surface

Recover as one adapter-win32 source-shaped package the original NT status,
object/device constants and reached RTL declarations identified by S12. Reuse
exact OpenNT declaration evidence first; retain original DEM call order and
failure branches. No DEM body link or provider route is enabled.

The package must separately classify `demsrch` persisted find-list identity:
it may use only the existing session `host_resource` mapping instance through
a later adapter-softpc binding, never a pointer cast. Guest DTA access remains
the distinct `guest_memory` lease.
