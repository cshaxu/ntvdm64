# M0 T328 — Minimal vertical-slice integration closure

## Purpose

Recover the exact original SoftPC CPU40 FDC/INT15 wait-continuation cohort
that follows the now-complete COMMAND configuration package, then repeat one
fixed-container observation. This is a source-owner package, not a BOP backlog
and not a guest-DOS rewrite.

## S plan

### S1 — Original FDC/INT15 wait-continuation contract

Trace the original CPU40 `wait_int` entry, all FDC/INT15 callback registration,
resume/unsimulate conditions, and each nullable callback/function-pointer
slot. Classify each edge as a selected original source body, existing
same-shaped adapter, required original package import, or explicit later owner.
No behavior change occurs here.

### S2 — Earliest complete original owner recovery

Recover only the earliest proven missing original binding in the complete
wait-continuation cohort. Preserve CPU40, interrupt, callback and failure
order. A changed slot must be sourced from its original owner or a same-shaped
bounded adapter; no BOP, guest-media, device-success or alternate executor
implementation is allowed.

### S3 — Fixed-container vertical result

Rebuild the selected product on independent x86 and x64 graphs. With the
immutable short-root media container, run one bounded x86 observation and
record either an original declared-command/guest boundary or the next exact
source-defined owner transfer.

## Exit

Close only with a fixed-container original guest-result boundary or a narrow
source-defined owner transfer. No x64 runtime claim follows from its required
link closure.
