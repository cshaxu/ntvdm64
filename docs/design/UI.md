# Product UX

`runner-run` is a command-line research tool. It classifies an input image,
performs ordinary host PE handoff for PE32/PE32+, and requires an explicit
user-selected engine for DOS MZ/COM execution. NE/WOW input is rejected with a
clear diagnostic. The tool does not install software, modify system state, or
silently substitute a guest runtime.
