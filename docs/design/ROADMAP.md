# Roadmap

## Current Route

1. Close the first reached historical SoftPC/host dependency using its
   original owner and recorded caller contract.
2. Recover only the caller-defined semantic minimum when original source is
   absent, with focused positive and negative fixtures.
3. Continue through CCPU, SAS, BIOS, dispatcher, DEM, and NTDOS ownership
   until a bounded fixed-profile bootstrap boundary is reached.
4. Consider a separate wrapper-design planning pass only after that result.

## Deferred

External execution cores, mantles, generic VM/device work, and wrapper
implementation are not current roadmap work or gates. The retained detailed
roadmap at `../../ROADMAP.md` is historical planning evidence until it is
migrated in a bounded reviewed batch.
