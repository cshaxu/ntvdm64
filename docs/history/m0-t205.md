# m0-t205 historical record

Consolidated task record. Raw contemporaneous records are retained in the external documentation archive.

## m0-t205-closure-20260815.md

# M0 T205 Closure — XMS whole-package recovery boundary

T205 closes the XMS `52:00..0B` owner package for the current no-device
minimal-machine profile.

- `00..05`, `0A` and `0B` are source-built through one XMS session using
  selector-blind mantle A20/extended-memory mechanics.
- `06..08` remain one explicit UMB subgroup because T205 S3 rejected a
  synthetic C3 memory map.
- `09` remains explicit because T205 S5 rejected an adapter or synthetic C5
  INT15 lifecycle.
- `0C` is correctly classified as the non-callable original
  `XMS_LASTSVC` boundary.
- The whole-family MSVC x64 `/MT` fixture proves each callable identity's
  implemented or typed-stop disposition.  It is not a native machine, BIOS,
  device, continuous-guest-execution or complete-XMS-feature claim.

Evidence: [S1 plan](../../artifacts/documentation-archive/20260910/strict-topology/etc/operations/t205-xms-whole-package-plan-001.md),
[S2 closure](../../artifacts/documentation-archive/20260910/strict-topology/history/raw/m0-t205-s2-closure-20260815.md),
[S3 C3 decision](../../artifacts/documentation-archive/20260910/etc/research/t205-s3-c3-umb-machine-admission-decision-001.md),
[S5 C5 decision](../../artifacts/documentation-archive/20260910/etc/research/t205-s5-c5-int15-machine-admission-decision-001.md),
and [S6 convergence](../../artifacts/documentation-archive/20260910/etc/research/t205-s6-xms-whole-package-convergence-001.md).


## m0-t205-s2-closure-20260815.md

# M0 T205 S2 Closure — XMS A20/block/geometry group

S2 closes the admitted non-UMB/non-INT15 XMS group: `52:00..05`, `0A` and
`0B`. One selector-blind mantle capability now owns A20, bounded extended
memory, physical backing-RAM copy and allocator statistics; one bx-vdm XMS
session owns copied record decoding and source-derived registers.

- `00` retains typed A20 set/query translation.
- `01` reads the historical 12-byte `SS:BP-12` record through the existing
  mechanical action and sends only copied physical addresses/count to mantle.
- `02/03/0B` use opaque allocation lifecycle and return source-shaped AX/CX
  failures for safe indeterminate/stale cases.
- `04` returns the normal-user-mode `GetSystemInfo` page-size contract.
- `05` reports declared machine capacity; `0A` reports total/largest free KiB.

Strict MSVC x64 `/MT /W4 /WX` package regression covers every S2 slot through
common ingress, including move mapping and failure paths. Mantle static
boundary checks pass. The broader minimal-machine recipe still fails at its
pre-existing omitted CPU/decode object link closure, so no native machine or
trace claim is made. `06..09` remain explicitly deferred for separate UMB and
INT15 mechanical capability work.

