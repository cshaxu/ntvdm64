# M0 T281 closure — original host control plane and DEM

T281 is closed as a source-composition and recovery-partition package.

- The exact original `nt_bop.c` dispatcher composes as a selector-disabled
  static island on x86 and x64.
- All sixteen original DEM common-control and provider translation units
  compose as one x86 and one x64 static archive. No BOP ingress, provider call,
  machine executor or guest trace is enabled.
- The complete 56-source original `softpc.new/host/src` set is partitioned
  exactly once: 23 named adapter prerequisites, 18 Bochs-replacement machine
  implementations, and 15 specialist-owner sources. None can be selected as
  an unreviewed all-host aggregate or a trace-derived leaf.

This closes static source identity and next-boundary evidence only. It does
not claim an active BOP route, executable host-control plane, CCPU executor,
or continuous guest execution. The next Queue-order package must use the S5
disposition ledger as its source-interface inventory.
