# M0 T130 closure — 2026-08-12

T130 replaced the machine-composition seam's lossy boolean with its existing
fixed result enum and a generic non-decline outcome log.  The focused component
and boundary tests pass; the narrow derivative rebuilt only `main.o`,
`cpu/exception.o`, and the machine component alongside the predeclared current
adapter closure, without rebuilding a Bochs archive or enabling a device.

The corrected T122 handoff input reaches the original `unexpected_int` island:
NTDOS and three pre-existing multi-write transactions commit first, then the
trace records `machine composition outcome=1` repeatedly.  `1` is
`HANDLED_RESUME`, proving the original handler's PIC/BDA mechanics and its
existing RIP+3 continuation.  The repeated loop remains downstream of the
already visible real-mode `EIP=10000 > CS.limit=FFFF` / INT 0Dh path, not a
machine BOP failure.  The watchdog terminated only the owned process tree; no
continuous execution is claimed.

T131 is admitted to classify the complete machine BOP catalogue statically,
not to add another trace-triggered handler.
