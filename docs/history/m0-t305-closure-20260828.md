# M0 T305 closure — DEM/COMMAND first source boundary

## Result

M0 T305/S1 is closed at its admitted source-closure boundary.  The exact
original DEM and COMMAND dispatchers compile on both host architectures.  The
original aggregate `nt_bop.c` cannot be linked/adopted solely for its two
reached entry functions because the same translation unit imports the excluded
historical SoftPC/PIC executor closure.

## Evidence

The owner map, command lines, architecture results and retained boundary are
in [the S1 audit](../etc/evidence/m0-t305-s1-dispatcher-boundary-audit-001.md).

## Scope result

- Retained original source: `MS_bop_0`, `MS_bop_4`, `DemDispatch` and
  `CmdDispatch` remain original MVDM owners.
- Verified: `demdisp.c` and `cmddisp.c` compile under MSVC `/MT` on x86 and
  x64 using the named declaration/adaptation forms.
- Declined: importing the complete `nt_bop.c` aggregate, `ica.h`, or a second
  SoftPC/PIC executor merely to route `50:xx` or `54:xx`.
- Not claimed: a linked BOP ingress, any service result, or native guest
  execution.

## Next owner

The next active package is the finite MVDM external-boundary package selection
task.  It determines the source-shaped monitor/PIC and other direct-exit
dispositions before a further DEM/COMMAND link/runtime package is admitted.
