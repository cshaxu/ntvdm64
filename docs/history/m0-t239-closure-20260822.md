# M0 T239 Closure — Code-complete BOP-v1 retirement

## Outcome

T239 enforces the project rule that a BOP attaining the current OpenNT-derived
code-complete route leaves no parallel v1 provider behind.  The entire
`src/bx-vdm/bop-v1/` experimental provider/catalog/facade tree is deleted;
there are no retained unfinished-v1 consumers.

## Delivered cleanup

- Removed 50 legacy source/header inputs, 37 dedicated legacy fixtures and
  boundary/probe tests, and four stale build helpers that named deleted legacy
  catalog symbols.
- Moved the only still-useful fixed-width family/request wire record out of
  the retired catalog into the current, provider-neutral
  `bx_ntvdm_host_service_contract.h`.  The `_v1` spelling remains only as the
  frozen record-layout version; no provider selection or behavior changed.
- Updated architecture, source-layout and live BOP tracker authorities: the
  working tree has one current source-parity route, while retired experiments
  remain available only through Git history.

## Verification

- Fresh external formal-r3 Ninja build: **353/353** MSVC x64 `/MT`,
  CPU5/P-MMX edges; 51 targets linked.
- **31/31** eligible current-route DEM/COMMAND/top-level/native-session
  fixtures passed.  The separate `t236-s4-ntfdisk-readonly-fixture` was not
  eligible without its documented owner-authorized disposable VHDX drive
  (`NTDOS64_T236_S4_TEST_DRIVE`); it has no retired-v1 dependency.
- T230 DEM-route, T231 COMMAND-route, T236 DEM/COMMAND-retirement, T237
  XMS-retirement and T237 machine-semantic-boundary gates all passed.
- Documentation inventory/governance and `git diff --check` passed.

## Explicit transfers

The retirement does not make any unimplemented family complete.  DPMI,
Redirector, WOW, debugger/VDD, remaining top-level/machine entries and
machine/guest integration continue from the source-first tracker, without a
fallback to the retired project-authored v1 implementations.
