# M0 T289 closure — original DPMI owner package

T289 is closed after seven sequential source-first subtasks: protected-machine
foundation, original DPMI source composition, protected/real frame mechanics,
registration and stack composition, package-wide `53:00..18` local matrix,
and final mirror/exception/transfer audit.

The resulting DPMI package is not an enabled guest execution path.  Its
verified scope is the direct or same-shaped composition of every independently
available original source body, plus source-defined failure directions for all
unavailable ones.  The formal source fixtures and matrix pass on both MSVC
x86 `/MT` and x64 `/MT`; no BOP ingress, raw guest pointer persistence, host
LDT/IDT mutation, or second CPU executor was added.

The final S7 audit establishes that the 25 selected paths are identical in
both canonical OpenNT editions.  Nineteen mirror files are exact.  Six have
only their registered `MVDM-HOST-DIV-012` through `MVDM-HOST-DIV-019`
source-preserving bindings.  Durable host identities use only the session
host-resource mapping manager through the same-shaped SoftPC facade.

Nine source-proven unavailable selectors now have named future ownership:
mode transition, monitor fast-entry, DPMI-to-DEM/COMMAND transport and
protected interrupt/fault runtime transfer to queue candidate 8; CPU debug
registers transfer to queue candidate 6.  This is a transfer of uncomposed
runtime behavior, not a claim that those services work today.

Supporting evidence: [S6 matrix closure](../etc/evidence/m0-t289-s6-dpmi-family-matrix-closure-001.md),
[S7 closure](../etc/evidence/m0-t289-s7-dpmi-identity-transfer-closure-001.md),
and the [S7 transfer ledger](../etc/operations/m0-t289-s7-dpmi-identity-exception-transfer-ledger.tsv).
