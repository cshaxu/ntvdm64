# M0 T125 closure — 2026-08-12

## Result

T125 assembled an immutable evidence bundle from the T121 current-adapter
engine and the T122 retained ROMs. Its manifest binds:

- engine: `DB2BF25C2AA5BDA4B482B0968D668FCFD050F394FD7BC278E2642E725DC53E1E`;
- BIOS: `D974DE50C94D76445123FD1AC886612351BED7CC5E3C8D07C556814DA72BEBD6`;
- VGA ROM: `CD9FDD6A789DCD22B8A6B3B152788D43238DE49CCE674CFF57BDEB94580246C6`;
- deferred POST ROM: `E4CA4AE5190A15ED741D1DFED5A58533A2B6926A40302D839FB714F9FC673FFB`.

The T124 CLI shim validated that bundle, generated its private fixed
configuration and entered the retained native engine. Native stderr proves
Bochs parsed `bochsrc`, initialized the declared PC reset floor, mapped the
three private ROM files, and entered the guest path.

## Bounded observation

The runner/shim/native process tree was given a 15-second external watchdog.
It did not stop naturally, so only that known process tree was terminated.
The retained stderr contains 7,295 observed BOP entries, all `selector=50`,
`service=11`, at `CS:EIP=0070:0475`, followed by the normal #UD vector path.
This differs materially from T122's frozen direct observer, which reached
`50:36` and the deliberate FE controlled stop.

## Interpretation and transfer

T125 proves the CLI-to-shim-to-native startup boundary; it does **not** prove
continuous execution, a valid service result, or a complete host composition.
The repeated actual BOP is a new source-audit condition. T126 is admitted only
to classify its original DEM contract and current provider/native prerequisite
disposition before any code or configuration change.
