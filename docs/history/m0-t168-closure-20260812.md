# M0 T168 closure — 2026-08-12

The registered `BX-ABI-057` change makes only a validated generic adapter STOP
call native `SIM->quit_sim(0)`. Boundary test and fresh minimal derivative link
passed. It introduces no selector, BOP, guest, DOS, device, or CLI exit-code
policy. T169 must separately make one retained native observation; T166 is
not retried.
