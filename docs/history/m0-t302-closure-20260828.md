# M0 T302 closure — local Base VDM command protocol

## Result

`M0 T302 S1-S2` is closed for its admitted one-session DOS Base VDM protocol.
It does not claim CSRSS/BaseSrv product-shell recovery.

- The original public `GetNextVDMCommand(PVDMINFO)` shape now enters the named
  `adapter-mvdm-host-out/basesrv` family.
- A caller-local BaseClient facade and a copied BaseSrv DOS record retain the
  reached capacity, environment-only, no-command, reentry, teardown and
  result-field behavior.
- The app producer and monitor callback no longer own a parallel command
  protocol.
- The formal focused Ninja fixture passed for both x86 and x64.

## Evidence

- [S1 boundary audit](../etc/evidence/m0-t302-s1-base-vdm-protocol-boundary-audit-001.md)
- [P1 BaseSrv record migration](../etc/evidence/m0-t302-s2-p1-local-basesrv-record-migration-001.md)
- [P2 BaseClient facade and direct-composition probe](../etc/evidence/m0-t302-s2-p2-baseclient-local-facade-001.md)

## Explicit limits

The original full BaseClient translation unit remains mirror evidence. Its
private Base DLL, CSR capture and CSRSS path is not linked. First-VDM global
query, BaseSrv wait-pair transport, cross-process routing, WOW/PIF and child
lifecycle remain separate, source-owned future work.

## Next admission

`M0 T303 S1` performs the bounded first-vertical-slice audit for the
Bochs/SoftPC mechanical contract. It may inspect only reached original
machine-facing forms and existing typed adapter seams; it may not change
Bochs, enable a BOP or reintroduce the SoftPC CPU executor.
