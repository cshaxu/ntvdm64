# M0 T245 Closure — Source-built command bootstrap continuity

## Closed scope

T245 traced the source-built NTIO/NTDOS/COMMAND startup sequence only far
enough to recover its first independently owned gaps. It did not treat every
new trace selector as a leaf implementation task.

The closed path is:

```text
NTIO/NTDOS bootstrap
  → original CONFIG completion and Direct DEM initialization
  → cmdconf.c:SHELL=
  → sysconf.asm/sysinit2.asm:commnd
  → original DOS 8.3 canonicalizer
  → DEM demOpen/CreateFileOem
  → permanent source-built COMMAND.COM open
  → original COMMAND 54:02 / 54:0D / 54:0F
```

The last admission defect was a modern host path component with no usable
8.3 alias. S22 corrects it in the CLI boundary with a session-owned directory
link to the actual host bundle, without guest rewriting, copying, a virtual
boot volume, DEM/BOP changes, or Bochs change.

## Evidence

- [S20 permanent-command DEM-open observation](../etc/evidence/m0-t245-s20-permanent-command-dem-open-observation-001.md)
- [S21 OpenNT-shaped OEM CreateFile recovery](../etc/evidence/m0-t245-s21-opennt-oem-createfile-recovery-001.md)
- [S22 DOS-safe bootstrap alias closure](../etc/evidence/m0-t245-s22-dos-safe-bootstrap-alias-closure-001.md)

Fresh formal Ninja MSVC x64 `/MT` links the CLI, `bx-vdm`, mantle and core;
the S22 fixture exercises the actual bundle root, reads `COMMAND.COM` through
the temporary junction fallback, and proves teardown removes only the link.
The bounded native Direct run removes the prior `ERROR_PATH_NOT_FOUND` and
reaches original COMMAND `54:02`, `54:0D` and `54:0F`.

## Explicit transfer

The next reached selector is legacy SoftPC machine `BOP 06` (`illegal_op_int`),
whose current generic ingress declines repeatedly. It is not a canonical DEM
or COMMAND BOP, and T245 does not claim to repair it. `BOP-DEPENDENCY-114`
transfers the source/route reconciliation to M0 T246's selector-blind machine
package. No claim is made that a guest program has executed or that full
COMMAND/EXEC continuity is complete.
