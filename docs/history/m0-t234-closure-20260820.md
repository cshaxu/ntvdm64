# M0 T234 Closure — OpenNT COMMAND Environment And Handle ABI Correction

## Outcome

M0 T234 closes on 2026-08-20. It corrects the prior host-handle ABI narrowing
and removes the v2 COMMAND session's fixed 1024-byte environment limit while
retaining the original OpenNT COMMAND owners and their dispatch order.

## Requirement Audit

| Requirement | Closure evidence |
| --- | --- |
| Safe original-width host-handle representation | The registered `BX-VDM-001` seam preserves OpenNT's 32-bit DEM `AX:BP` and COMMAND `BX:CX` shapes while keeping native handles out of guest state; see [S1 result](../etc/evidence/t234-s1-32bit-opaque-host-handle-abi-result-001.md). |
| Dynamic COMMAND environment | The original `cmdenv.c` dynamic transform, merge, lookup, set and expansion bodies are compiled through the smallest session-owned seam; the fixed source and saved-VDM arrays are gone. See [S2 result](../etc/evidence/t234-s2-dynamic-command-environment-result-001.md). |
| Required behavior | The focused fixture covers an environment exceeding 1024 bytes, COMSPEC/AUTOEXEC merge, WINDIR filtering, repeat-call preservation, and malformed/allocation failures. |
| Formal affected target | Fresh NTFS-root Ninja `formal-r11` rebuilt 82 required actions and the dynamic-environment fixture exited zero. |
| Governance | Documentation governance and `git diff --check` passed at closure review. |

## Boundary And Transfers

- DEM's 73 slots and COMMAND's 17 slots remain routed through their retained
  original OpenNT dispatch tables and owners, with only named composition
  seams. The opaque handle manager is the registered host-width safety
  exception; it does not replace an OpenNT BOP owner.
- PIF parsing is retained in original `cmdpif.c` and `nt_pif.c`, but PIF-
  selected CONFIG/AUTOEXEC startup ordering is not claimed here. It transfers
  to the queued OpenNT COMMAND launch, PIF and child-lifecycle owner package.
- A fresh unconstrained formal `all` graph currently fails in unrelated
  bx-core C++ compilation because retained OpenNT SoftPC headers shadow the
  expected configuration header. This closure claims the packet's affected
  formal target only; it does not classify that independent graph failure as
  an OpenNT COMMAND environment defect.

## Closure Record

The active Status marker is retained only for the documentation validator.
It authorizes no additional T234 implementation. The next work must be
selected from the unnumbered Queue and admitted in a new Status packet.
