# M0 T174 closure — 2026-08-12

T174 closes the paired declared DOS target lifecycle provider. Existing `54:01` state now gates a one-time contained `54:11` result: copy DX as adapter lifecycle state, resume four bytes, clear CF, preserve AX and decline all unpaired/repeated/wrong-mode cases. The fresh pinned Bochs/adapter derivative links without Bochs archive or device expansion; an isolated x86 focused test passes. Modern MinGW frontend evidence is unavailable and the pre-existing legacy recognizer sweep remains blocked by an unrelated unregistered pathname-search file. No runtime continuation, CLI result exposure, host queue/redirection, or arbitrary-program claim is made.
