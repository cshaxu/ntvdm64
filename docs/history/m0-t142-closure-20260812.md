# M0 T142 closure — 2026-08-12

T142 source-correlates the observed `0BC1:0100` entry and `0BC1:FFFE` stack
with original NTDOS `$Exec`. The fault is later app-context state. The app
image bytes and PDB are not recorded, so no correction is admitted.

T143 is admitted to audit the coherent original DEM command-image load
lifecycle already visible in the trace.
