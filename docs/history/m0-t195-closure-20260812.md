# M0 T195 Closure: Bochs/Adapter Boundary Repair

T195 repaired the two semantic intrusion groups identified by its 58-entry
audit. It does not claim BOP runtime closure.

- S1 audited every register entry and separated live source/build targets from
  historical ledger rows.
- S2 fixed the copied-event, opaque-mechanics and generic-result migration
  contract.
- S3 moved BOP catalogue identity/observation from Bochs to adapter.
- S4 moved `02h`/`06h` historical handler selection and resume decision to the
  adapter machine-composition plane, retaining only checked native mechanics
  in Bochs.
- S5 reconciled migrated, removed and retained register rows and active build
  surfaces.
- S6 generated and MSVC-x86-linked a fresh closure, verified adapter/original
  map ownership, passed negative source scans and reconciled `58/58` ledger
  IDs.

The retained limits are explicit: no continuous guest execution claim, no
claim that all BOP providers run, and no device/PIC/CPU ownership transfer.
The next queued package first reconciles the full Bochs 2.6 intrusive diff;
only then may the holistic BOP runtime package be admitted.

Detailed closure evidence is in
`docs/etc/research/t195-s6-repaired-boundary-closure-001.md`.
