# m0-t213 historical record

Consolidated task record. Raw contemporaneous records are retained in the external documentation archive.

## m0-t213-closure-20260815.md

# M0 T213 Closure — one-shot terminal model and COMMAND handoff

T213 closed the normal-terminal investigation and its admitted package.

- DEM `50:3D` remains controlled historical VDM teardown.
- Exact top-level COMMAND `54:00` has a source-derived copied terminal fact,
  opaque composition handoff, cleanup-before-publication engine mapping, and
  focused x64 `/MT` package/engine evidence.
- A fresh native observation still rejects composition before CPU entry.

The next task is current-profile composition admission recovery, not another
BOP endpoint or trace-led implementation.

Evidence: [S4 native observation](../../artifacts/documentation-archive/20260910/etc/research/t213-s4-native-terminal-observation-001.md) and [S3 package closure](../../artifacts/documentation-archive/20260910/etc/research/t213-s3-command-terminal-package-closure-001.md).


## m0-t213-s1-closure-20260815.md

# M0 T213 S1 Closure — one-shot normal-terminal model map

S1 closes the whole-path source/ABI/failure map without changing execution.

- `54:0B` is a COMMAND-local final-return fact, not current engine or CLI
  completion.
- Original `50:3D` is historical VDM teardown; current typed controlled stop
  preserves that distinction.
- Current integrated evidence saw `50:3D` before any accepted final `54:0B`.
- `ExitProcess` is available but rejected for the in-process engine; historical
  `ExitVDM`/BaseSrv/CSR is not an admissible modern CLI dependency.

Evidence: [S1 normal-terminal model map](../../artifacts/documentation-archive/20260910/etc/research/t213-s1-one-shot-normal-terminal-model-map-001.md).

T213 S2 is limited to the complete COMMAND/DEM host-terminal state and copied
record design; it cannot add a trace-selected terminal handler.


## m0-t213-s2-closure-20260815.md

# M0 T213 S2 Closure — one-shot host-terminal package design

S2 closes the complete COMMAND/DEM terminal-state design without
implementation.

- `50:3D` is ambiguous across source-proven top-level and fatal paths, so it
  cannot become ordinary completion by selector, register, or trace heuristic.
- The existing final `54:0B` record is COMMAND-local and neither precedes nor
  authenticates every DEM teardown.
- A future copied terminal record requires a source-proven provenance contract;
  the current profile supplies none.
- Controlled termination remains the only honest current disposition.  No
  unavailable modern Win32 API blocks the work; the unavailable dependency is
  the historical `ExitVDM`/BaseSrv/CSR composition.

Evidence: [S2 host-terminal package design](../../artifacts/documentation-archive/20260910/etc/research/t213-s2-one-shot-host-terminal-package-design-001.md).


## m0-t213-s3-closure-20260815.md

# M0 T213 S3 Closure — COMMAND terminal package recovery

S3 closes the source-built COMMAND terminal handoff.

- Exact `54:00` produces a copied top-level COMMAND terminal fact with no
  invented DOS exit code; `54:03/0B` retain their prior package contracts.
- Composition exports only an opaque ordinary-terminal fact.
- Engine resets stage first, then maps only that opaque fact to its existing
  ordinary-completion enum; generic/DEM controlled stops remain controlled.
- MSVC x64 `/MT` package, engine, and composition-runtime compilation
  evidence passed.  The current guest boot profile has not yet reached this
  terminal and no normal guest-run claim follows.

Evidence: [S3 closure](../../artifacts/documentation-archive/20260910/etc/research/t213-s3-command-terminal-package-closure-001.md).

