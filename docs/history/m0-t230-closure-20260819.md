# M0 T230 Closure — OpenNT DEM parallel mirror

## Outcome

M0 T230 closes on 2026-08-19.  It completes the Direct-only OpenNT-shaped
DEM `50:00..48` package without transferring a callable DEM endpoint or the
required native observation to another task.

## Requirement audit

| Requirement | Current evidence |
| --- | --- |
| 73 original slot owner/ABI/failure coverage | [S11 audit](../etc/evidence/t230-s11-dem-73-slot-local-execution-audit-001.md) and [S12--S14 shared dispatcher matrix](../etc/evidence/t230-s12-s14-shared-demdispatch-local-matrix-001.md). |
| Original source-shaped implementation and local execution | Directly imported `src/bx-vdm/bop/opennt/dem/` translation units plus the 14-fixture formal matrix recorded in [P4 evidence](../etc/evidence/t230-s15-p4-v2-dem-composition-entry-001.md). |
| v1 DEM route retirement and cleanup | `dac8dea9` replaced the native startup caller; `cbc86873` deleted all 140 tracked handwritten `bop-v1/bx_ntvdm_dem_*` files; formal manifest and precedence gate exclude `bop-v1`. |
| Native checked-memory composition and bounded observation | [S16 native observation](../etc/evidence/t230-s16-native-direct-dem-observation-001.md) records real NTIO `50:11` accepted and resumed through v2. |
| No trace-selected DEM transfer | The sole trace is final integration evidence.  Its later `12:00` decline and non-DEM machine continuation are expressly assigned to future owner packages, not DEM work. |

## Delivered boundary

- The only formal DEM path is the original OpenNT mirror under
  `src/bx-vdm/bop/opennt/dem/` with narrow named shims under
  `src/bx-vdm/bop/shim/`.
- All 73 `demdisp.c` identities have the original implemented, ordinary/no-op,
  or original failure disposition locally exercised through `DemDispatch`.
- The native engine uses v2 startup/session composition; no formal product
  route selects a v1 DEM provider, dispatcher, result bridge, or composition
  runtime.
- The v1 handwritten DEM implementation set is deleted.  Surviving
  `bop-v1` material belongs to non-DEM historical owner packages and is not a
  formal runtime input.

## Limits

The bounded real run reaches `50:11` and then exposes a non-DEM `12:00` and
machine/BIOS continuation condition.  T230 does not implement those owners,
does not claim multi-program guest continuity, and does not reopen DEM based
on that trace.

## Verification

- Formal Ninja graph `build/M0-T230-S15/v2-startup-r2` source-built the full
  modern runtime and all 14 T230 fixtures.
- All 14 fixtures exited zero after v1 DEM deletion.
- The v2 route-precedence gate passed and the bounded native observation is
  recorded above.

Unrelated in-progress worktree files were excluded from this closure.
