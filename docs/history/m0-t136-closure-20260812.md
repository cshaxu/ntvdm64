# M0 T136 closure — 2026-08-12

T136 source-builds the registered, default-off BX-TRACE-056 diagnostic as a
single direct `cpu_trace.obj` override. The final map resolves `prefetch` from
that object ahead of retained `cpu/libcpu.a`; no archive rebuild is declared.

T137 is admitted for one unchanged-profile bounded observation only. It must
record the diagnostic outcome without adding a BOP/provider/device/reset
change.
