# M0 T171 closure — 2026-08-12

T171 maps the complete historical COMMAND dispatcher and rejects direct reuse
of its host process/redirection path for the CLI DOS profile. The successor is
the whole declared DOS guest-target lifecycle component, preserving the
COMMAND envelope/return contract without `CreateProcess`, host handles, VDD,
CCPU/SAS pointers or VDM re-entry. No runtime or source behavior changed.
