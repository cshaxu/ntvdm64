# M0 T214 Closure — current-profile composition admission

T214 closes the declared profile's copied-input/provider-installation path
before CPU entry.

- S1 recorded the ordered install-stage owner map.
- S2's retained `detail=40` result is partial-object diagnostic evidence, not
  current-source attribution.
- S3 ran a full current-source closure, attributed the actual rejection to
  `COMMAND_CONTEXT_CAPTURE` (`detail=45`), and restored the OpenNT
  `cmdenv.c` environment rules through a bounded source-derived adapter seam.
- The repair removes the arbitrary 4 KiB snapshot and per-variable 1 KiB
  limit; it retains OpenNT filtering, OEM conversion, `COMSPEC`/`WINDIR` and
  `PROMPT` behavior, and reports an explicit 16-bit guest transaction limit
  rather than truncating input.
- Focused x64 `/MT` evidence carries a 5,001-byte environment through
  `54:02` and `54:0F`. The full current-source fixture now passes admission,
  reaches CPU entry, and returns a controlled guest terminal. This is not a
  guest-run or COMMAND/BOP completion claim.

The next package must not select a service merely because this observation
ended at a terminal. It requires an owner-package admission from the global
BOP/provider ledger and its complete source/ABI/failure disposition.

Evidence: [S3 recovery record](../etc/research/t214-s3-opennt-command-environment-recovery-001.md), [S2 retained diagnostic](../etc/research/t214-s2-composition-admission-diagnostic-001.md), and commit `56733e9d`.
