# M0 T323 closure — DPMI/DPMI32 owner boundary

## Outcome

T323 closed its bounded source-first DPMI/DPMI32 owner recovery. It did not
claim a protected-mode guest runtime.

- S1 preserved the original 25-entry DPMI dispatcher as the complete selected
  provider boundary and separated it from the DOSX guest assembly carrier.
- S2 classified every reached `Sim32GetVDMPointer` form by lifetime and
  recovered the complete private `IntelBase`/`FlatAddress` host-address
  contract through registered mirror divergence `MVDM-HOST-DIV-140`.
- The selected original `original-mvdm-dpmi32.lib` and
  `original-softpc-process.exe` link on formal x86 and x64 Ninja rows.

## Evidence

- [S1 original owner/binding baseline](../etc/evidence/m0-t323-s1-original-dpmi-owner-binding-baseline-001.md)
- [S2 guest-span lifetime and recovery ledger](../etc/evidence/m0-t323-s2-dpmi-guest-span-lifetime-ledger-001.md)

## Explicit transfers

- `NtVdmControl(VdmSetInt21Handler)` remains a kernel-monitor operation with
  exact `STATUS_NOT_IMPLEMENTED` behavior until a separately admitted monitor
  owner package can recover it without recreating kernel VDM.
- `int21map.c` pointers that span original DOS/DEM mode-switch and re-entry
  remain with the later DPMI/DOS integration owner; this task did not create a
  DPMI-private mapper or extend a pointer lease across it.
- DOSX guest assembly remains immutable guest input and was not linked into
  the host provider.

## Acceptance limitation

Static source/link closure proves neither protected-mode instruction execution
nor a working DPMI application. Those require a future owner-admitted
machine/monitor and DOS continuation integration test.
