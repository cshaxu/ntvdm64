# M0 T241 Closure — PC IVT Watch For XMS `52:09`

## Outcome

T241 closes the smallest original XMS/keyboard coordination contract:
`52:09` now executes OpenNT `xmsNotifyHookI15`, whose active direct SoftPC
fragment compares the IVT `15h` pair, clears a stale expected pair, or stores
the incoming `CS:AX` pair before the original XMS body publishes CX.

## Boundary

- `bx-mantle` owns one configured, selector-blind IVT pair lifecycle and
  discards it on stage reset.
- `bx-vdm` owns the XMS service recognition, source fragment and named shim.
- No adopted Bochs source changed. No keyboard device, PPI/PS2, IRQ1, timer,
  mouse/video, generic INT15 execution, `BOP 5C`, or host input was enabled.

## Verification

The formal MSVC x64 `/MT` native-machine fixture passed matching and stale-IVT
branches plus original CX output. The full CLI target linked and the required
retirement/boundary gates passed. Its single bounded bundle run stopped before
any BOP, so this closure makes no guest-continuity claim. Detailed results are
in [S2 recovery evidence](../etc/evidence/m0-t241-s2-ivt-watch-source-recovery-001.md)
and [S3 regression evidence](../etc/evidence/m0-t241-s3-xms-ivt-watch-regression-001.md).

## Transfer

The next package is guest-owned NTDOS `EXEC` and parent return. Full keyboard
or INT15 execution remains a later independently admitted machine/device
package, not an extension of this completed XMS dependency closure.
