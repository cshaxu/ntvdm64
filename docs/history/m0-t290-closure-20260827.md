# M0 T290 closure — Original Redirector and brokered COMMAND extensions

## Scope closed

T290 recovered the complete original OpenNT `vdmredir` owner package through
source-first local composition, not through a replacement Redirector. Its six
subtasks closed lifecycle, synchronous pipe, local COMMAND, mailslot, async
safe-decline, and NetAPI/NetBIOS/DLC/window/mode dispositions before the final
whole-family reconciliation.

## Evidence

- The original dispatcher ledger contains all fifty `57:00..31` entries.
- [S6 final matrix](../etc/operations/m0-t290-s6-redirector-family-disposition-matrix.tsv)
  gives every entry and reached brokered dependency one source-first result.
- [S6 closure evidence](../etc/evidence/m0-t290-s6-redirector-family-closure-001.md)
  records the formal x86/x64 11-graph package matrix and disabled-ingress
  observation.
- The original source mirror contains 24 byte-exact selected `vdmredir` files
  and one registered identity-boundary divergence in `vrmslot.c`.

## Deliberate limits

No `57:xx` route is enabled. Private Lanman/Rxp/Xs transaction contracts,
NetBIOS/DLC drivers, physical IRQ completion and unbounded guest output forms
remain source-preserved unavailable outcomes. This is not a network, LAN,
NetBIOS, VDD or integrated guest-runtime success claim.

## Queue transition

The next ordered package is the original WOW owner group. It begins with a
whole-package source/ABI admission audit; no WOW host provider is enabled by
this closure.
