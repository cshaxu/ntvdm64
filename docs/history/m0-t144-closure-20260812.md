# M0 T144 closure — 2026-08-12

T144 identifies `0BC1` as original COM `Exec_DMA`: the command image is read
at `0BC1:0000` and enters at `0BC1:0100`. The late fetch is beyond the static
COMMAND image interval, but allocation/PDB size and later command flow remain
unrecorded.

The next candidate is a static COMMAND image/source-map audit; no BOP or
machine correction is selected.
