# M0 T146 closure — 2026-08-12

T146 replaced the invalid T136 direct CPU-object diagnostic override with one
complete, r5-pinned CPU archive in which only registered BX-TRACE-056 is
enabled. The final map resolves `prefetch` from `libcpu:cpu.o`, while T130's
separately linked machine-composition exception object and all retained
non-CPU inputs remain byte-identical.

The one allowed bounded execution used the same T130 profile but stopped
earlier at the original CPU-loop quit immediately after `50:36`; it did not
reach the late prefetch fault. T146 therefore establishes build closure, not
the fault predecessor. It rejects retry and semantic remediation. Any future
work must first statically audit the archive/toolchain divergence.

Evidence: `etc/research/t146-s1-single-diagnostic-cpu-archive-closure-001.md`
and `etc/research/t146-s2-bounded-prefetch-archive-observation-001.md`.
