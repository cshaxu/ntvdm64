# m0-t212 historical record

Consolidated task record. Raw contemporaneous records are retained in the external documentation archive.

## m0-t212-closure-20260815.md

# M0 T212 Closure — CLI lifecycle governance

T212 closes the lifecycle-governance boundary around the existing one-shot
engine.

- S1--S3 established copied CLI policy, complete terminal classification and
  native presentation without changing engine behavior.
- S4--S5 admitted and implemented the one selector-blind, mantle/engine
  cancellation producer with existing cleanup ownership.
- S6 composed that producer with a CLI-private console event, worker and
  copied-result join; no handle, pointer or callback crosses the engine ABI.
- S7 reconciled every V1 terminal and named normal completion, wall/resource
  policy, durable audit and DOS-visible cancellation as non-claims or explicit
  deferrals.

T212 does not prove a guest DOS run or ordinary guest completion.  The first
queued interactive package is therefore not automatically admitted.

Evidence: [S7 lifecycle ledger](../../artifacts/documentation-archive/20260910/etc/research/t212-s7-lifecycle-convergence-ledger-001.md),
[S6 orchestration closure](../../artifacts/documentation-archive/20260910/etc/research/t212-s6-cli-cancellation-orchestration-closure-001.md),
and prior S1--S5 closure records.


## m0-t212-s1-closure-20260815.md

# M0 T212 S1 Closure — CLI lifecycle governance map

S1 closes the current lifecycle source/ABI/failure mapping without changing
runtime behavior.

- The one-shot engine produces budget, controlled-terminal, composition and
  machine results, and owns its stage/composition cleanup.
- `ORDINARY_GUEST_COMPLETION` and `HOST_CANCELLATION` are valid enum values
  but have no current engine producer. COMMAND `54:0B` remains package-local.
- Existing Ctrl+C handling applies only to the retained child-process runner;
  it cannot be used as an in-process engine cancellation mechanism.
- The relevant ordinary user-mode Win32 APIs are available. The remaining
  boundary is ownership/composition, not a missing API.

Evidence: [T212 S1 lifecycle map](../../artifacts/documentation-archive/20260910/etc/research/t212-s1-cli-lifecycle-governance-source-abi-map-001.md).

T212 S2 is admitted to implement only a fixed-width CLI policy/result
classification and in-memory audit ABI. It does not alter the engine contract,
Bochs/mantle, BOP behavior or native execution.


## m0-t212-s2-closure-20260815.md

# M0 T212 S2 Closure — CLI lifecycle policy/result ABI

S2 closes the first copied CLI lifecycle boundary.

- `ntdos64_lifecycle_v1` owns policy validation and an in-memory audit/result
  record without changing the engine ABI.
- The source-built MSVC x64 `/MT` r2 fixture proves unique result
  classification, policy rejection and no cancellation-result fabrication.
- Normal completion and in-flight cancellation remain unproduced; no BOP,
  engine, mantle or Bochs behavior was changed.

Evidence: [S2 ABI and fixture record](../../artifacts/documentation-archive/20260910/etc/research/t212-s2-cli-lifecycle-policy-abi-001.md).

T212 S3 is admitted only to integrate the existing native CLI with the
already-closed copied ABI and a focused full-composition build. It cannot add
an engine cancellation seam or public host-mutation behavior.


## m0-t212-s3-closure-20260815.md

# M0 T212 S3 Closure — native CLI lifecycle integration

S3 closes native CLI consumption of the copied lifecycle ABI.

- The CLI derives its existing engine budget from a validated copied policy
  and reports a separately classified lifecycle result after engine return.
- A fresh MSVC x64 `/MT` CLI build/run returned the declared composition
  rejection (`3`) and printed its engine and lifecycle fields distinctly.
- The source-built closure repair added only existing current providers and
  the documented NTDLL user-mode import; no engine, mantle, Bochs or BOP
  behavior changed.

Evidence: [S3 integration record](../../artifacts/documentation-archive/20260910/etc/research/t212-s3-native-cli-lifecycle-integration-001.md).

T212 S4 is a source/ABI/failure map for a possible cooperative cancellation
seam. It is not implementation authority for the engine or Bochs.


## m0-t212-s4-closure-20260815.md

# M0 T212 S4 Closure — cooperative cancellation admission map

S4 closes the read-only admission map for in-process cancellation.

- Direct asynchronous CLI writes to Bochs state, raw handle/pointer transfer,
  host termination and BOP-based cancellation are all rejected.
- A selector-blind mantle-private atomic controller, polled by an existing
  CPU-thread timer callback, is the sole admitted mechanical candidate.
- Existing engine cleanup remains the owner; the pre-existing copied
  `HOST_CANCELLATION` enum is the only permitted eventual engine result.
- Wall-time and resource limits remain deferred; no unavailable Win32 API
  blocks future CLI orchestration.

Evidence: [S4 admission map](../../artifacts/documentation-archive/20260910/etc/research/t212-s4-cooperative-cancellation-admission-map-001.md).

T212 S5 is admitted to implement the mechanical controller only after its
mantle intrusion exception has been registered. It excludes CLI event/thread
orchestration and all BOP work.


## m0-t212-s5-closure-20260815.md

# M0 T212 S5 Closure — selector-blind mechanical cancellation

S5 closes the mechanical half of cooperative cancellation.

- The registered `BX-MANTLE-081` controller is mantle-private, atomic and
  selector-blind; no Bochs core source was changed.
- Actual stage evidence proves pre-entry and in-flight cancellation, typed
  STOP distinction, watchdog preservation and reset/reuse.
- Engine evidence proves cleanup precedes the existing copied
  `HOST_CANCELLATION` terminal and rejects a late request after cleanup.

Evidence: [S5 mechanical closure](../../artifacts/documentation-archive/20260910/etc/research/t212-s5-mechanical-cancellation-closure-001.md).

T212 S6 is admitted only for CLI-owned worker/event orchestration over the
closed typed engine request. It remains outside BOP, OpenNT and Bochs core.


## m0-t212-s6-closure-20260815.md

# M0 T212 S6 Closure — CLI cancellation orchestration

S6 closes the CLI-owned half of cooperative cancellation.

- Console control handling only signals a CLI event; it performs no engine,
  mantle, Bochs, BOP, or cleanup action.
- The CLI worker owns its copied input and is joined before any typed engine
  result is copied or lifecycle-presented.
- The sole engine request is the fixed `USER_REQUEST` reason; no host handle,
  pointer, callback, guest state, or selector crosses the engine boundary.
- Fresh MSVC x64 `/MT` worker and native-CLI fixtures pass.  The native probe
  retains its expected pre-CPU composition rejection and makes no guest-run or
  normal-completion claim.

Evidence: [S6 orchestration closure](../../artifacts/documentation-archive/20260910/etc/research/t212-s6-cli-cancellation-orchestration-closure-001.md).

T212 S7 may reconcile the complete lifecycle ledger and explicitly retain its
wall-time/resource/audit deferrals before task closure.

