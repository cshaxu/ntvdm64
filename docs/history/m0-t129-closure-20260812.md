# M0 T129 closure — 2026-08-12

T129 attempted the one permitted paired generic startup-transaction build.
Enabling the outer existing switch exposed seven unresolved symbols belonging to
the older, parallel startup bridge (`cpu_result_bridge_v1` and
`startup_transaction_bridge`). Linking those objects would create a second
adapter/session composition beside the current v4 runtime, so the attempted
image was rejected and no executable was produced.

The fixed CLI engine profile then enabled the already linked current adapter
bridge for its child process only. The unchanged T121 image completed the
source-derived `50:11` NTDOS write plus three existing multi-write
transactions, and subsequently reached `C4 C4 02`. That latter BOP is the
separately owned machine-composition path, not grounds to revive the rejected
parallel bridge or to add a host-service provider.

T130 is admitted to classify the existing machine-composition outcome with no
device, provider, or handler semantic expansion.
