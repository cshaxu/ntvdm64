# M0 T119 closure — 2026-08-12

T119 links one original machine handler invocation into the default-off,
selector-blind Bochs-to-machine-composition seam. The linked handler is the
unchanged OpenNT `unexpected_int`; the component alone recognizes `02h` and
owns its three PIC/RAM mechanics wrappers. Bochs performs only a generic
handled-result resume at fault RIP plus three.

The r3 map proves object and wrapper provenance. The image was not executed;
the retained CRT warning is explicitly unresolved as runtime assurance.

T120 is admitted for exactly one bounded observation of the frozen r3 image.
It may classify a reached `02h`, terminal fault, or budget stop, but may not
alter code or extend the machine/BOP surface.
