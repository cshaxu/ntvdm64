# M0 T213 Closure — one-shot terminal model and COMMAND handoff

T213 closed the normal-terminal investigation and its admitted package.

- DEM `50:3D` remains controlled historical VDM teardown.
- Exact top-level COMMAND `54:00` has a source-derived copied terminal fact,
  opaque composition handoff, cleanup-before-publication engine mapping, and
  focused x64 `/MT` package/engine evidence.
- A fresh native observation still rejects composition before CPU entry.

The next task is current-profile composition admission recovery, not another
BOP endpoint or trace-led implementation.

Evidence: [S4 native observation](../etc/research/t213-s4-native-terminal-observation-001.md) and [S3 package closure](../etc/research/t213-s3-command-terminal-package-closure-001.md).
