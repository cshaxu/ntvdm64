# M0 T366 — Declared DOS workload command-delivery closure

T366 closed the pre-EXEC source-shaped input package.

- S1 established that original COMMAND/BaseClient/BaseSrv already define the
  copied `VDMINFO` record contract and found one app omission:
  `BASE_VDM_COMMAND_DOS` was not set before the existing local broker.
- S2 set that existing discriminant at the sole app construction point. The
  focused x86 fixture proved a declared `T365TEST.COM` record is copied,
  delivered and consumed with existing malformed-input rejection.
- S3 proved the app, copied BaseVDM route, session/broker and original COMMAND
  archive are final CPU40/x86 process inputs.
- S4 made one fixed observation of existing immutable
  `COMMAND.COM /C EXIT`. It reached original COMMAND and successful original
  DEM opens of staged `COMMAND.COM`, then timed out at a later original
  no-next-record state before a `$Exec` completion/child-return result.

No guest byte, original MVDM/OpenNT body, BOP behavior, CPU/device behavior or
CSRSS recreation was introduced. The next package is DOS `.COM`/MZ execution
and parent return, beginning at the original `$Exec`/JFN/SFT/PSP cohort.
