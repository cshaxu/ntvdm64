# M0 T237 Closure — XMS source-mirror owner package

## Outcome

T237 closes the XMS source/ABI recovery package. All `52:00..0B` entries use
the imported OpenNT XMS owner route or an explicit source-derived unavailable
disposition; no XMS-v1 provider remains on a product route.

## S ledger

| S | Closed result |
| --- | --- |
| S1–S3 | OpenNT XMS source mirror, dispatcher, local ABI and focused failures/regressions. |
| S4 | Manual per-slot owner/route audit and bx machine-semantic boundary correction. |
| S5 | `52:06` checked AX:BX HIMEM-state binding plus original A20 writeback; `52:07/08` retain B1/B2 empty-map failures. |

## Verification

Fresh formal Ninja r7 rebuilt all 349 declared MSVC x64 `/MT`, CPU5/P-MMX
edges. `t237-s2-xms-source-mirror-fixture`,
`t237-s3-xms-a20-source-mirror-fixture`, the T237 v1-retirement gate, and the
machine-semantic boundary gate returned zero. Detailed evidence is in the
[S5 closure record](../etc/operations/m0-t237-s5-xms-umb-a20-closure-001.md).

## Explicit transfers

- A successful UMB physical span/reservation lifecycle transfers to the
  selector-blind bx machine/BIOS owner; bx-vdm retains XMS policy.
- `52:09` transfers to the keyboard/BIOS/IVT owner; its current explicit
  decline is retained rather than a silent no-op.
- Whole-guest continuity and all cross-family integration remain later work;
  T237 makes no such runtime claim.
