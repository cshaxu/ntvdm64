# M0 T189 closure — 2026-08-12

T189 closes the boot-file predicate question. Its initial `54:0C` pass-through
was not a missing OpenNT BOP or a Bochs feature gap: the derivative had rebuilt
the runtime while retaining stale COMMAND boot-file service and boot-namespace
provider objects. The approved four-object closure rebuilt only those two
adapter-owned objects plus the existing runtime and generic Bochs exception
object. The resulting manifest-bound, one-shot observation commits the
existing boot-file multi-write and reaches controlled stop with exit zero.

The optional diagnostic remains adapter-local and fixed-width. It has no
selector-specific Bochs logic, host capability, guest-memory probe, DOS
implementation, or machine/device change. The first invalid bundle attempt is
retained as manifest-composition evidence; it was not retried.

The completed run reaches the next source-owned boundary, COMMAND `54:02`
(`cmdComSpec`). Original source accepts the copied `DS:DX` COMSPEC text; the
current source-derived seam requires equality with a profile-made path. That
contract mismatch transfers to T190. Terminal-result transport remains queued
and is not admitted: no normal `54:11` return has been observed.
