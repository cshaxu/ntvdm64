# M0 T285 closure — duplicate Wave 2 withdrawal

M0 T285 is closed by owner-directed route correction before S3 began.

S1 and S2 remain valid, limited evidence: they selected the current original
host-control/DEM workset, recorded the `nt_bop.c` external contract groups,
and rebuilt selector-disabled x86/x64 static archives. They do not claim that
a BOP provider is newly enabled, nor do they justify repeating the
already-connected DEM/COMMAND product work.

The owner confirmed the intended sequence after T284 component renaming:

1. minimize Bochs-core divergence and private overlay surface;
2. recover XMS and then the remaining owner packages in dependency order;
3. perform whole-product composition and integration verification only after
   those packages close.

Accordingly, T285 S3--S8 are not executed. `QUEUE.md` removes the redundant
COMMAND candidate and promotes Bochs-core minimization, followed by XMS.
`src.old` remains excluded from source, generation, compilation, linking,
runtime, and acceptance inputs.
