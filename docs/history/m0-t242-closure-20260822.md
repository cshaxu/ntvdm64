# M0 T242 Closure — Source-Proven Guest EXEC Prerequisites

## Outcome

T242 recovered and verified the source-proven prerequisites on the local
source-built guest path without creating a host-side DOS process model:

- OpenNT/SoftPC `INT 15h AH=88h` extended-memory bootstrap now retains the
  original non-protected-mode `setAX(0)` result through the named fixed-width
  seam.
- The reached x86 `50:42` fast-read branch reuses imported OpenNT `demRead`
  with its historical SFT/position semantics; it does not enable `50:43` or
  fabricate a generic fast-I/O worker.
- A provenance-enabled source-built run identifies the later NTIO location as
  the original `sysinit2.asm:organize` CONFIG parser, and a longer bounded run
  reaches `spckbd.asm`'s `5F/AX=BEEF` handoff.

## Boundary and Transfer

`BOP 5F` is not a DEM, COMMAND, or guest EXEC service.  Its original route is
NTIO `spckbd` to SoftPC `MS_bop_F/kb_setup_vectors`, with keyboard-table, IVT,
PIC and timer lifecycle effects.  It transfers intact to the next,
selector-blind `bx-core`/`bx-mantle` machine-composition package.  `bx-vdm`
continues to own only BOP ingress and typed mechanical result transport.

## Verification

Focused formal Ninja fixtures and the full CLI link passed for each admitted
source group.  The bounded source-built observation accepted the recovered
`15/AH=88h` and `50:42` contracts and reached `5F/AX=BEEF`; it is source
attribution and owner-transfer evidence, not a claim of continuous guest
execution.  Detailed evidence is in
[P1](../etc/evidence/m0-t242-s3-int15-ems-source-mirror-001.md),
[P2](../etc/evidence/m0-t242-s3-fastread-source-recovery-001.md), and
[P3](../etc/evidence/m0-t242-s3-ntio-config-and-keyboard-transfer-001.md).
