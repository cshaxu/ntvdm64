# m0-t280 historical record

Consolidated task record. Raw contemporaneous records are retained in the external documentation archive.

## m0-t280-closure-20260826.md

# M0 T280 closure — original host-control and DEM source boundary recovery

M0 T280 is closed as the planned source/boundary recovery packet for the
selected original DEM and reached host-control workset. Its S1–S20 evidence
establishes the original source selection, declaration carriers, same-shaped
Win32 and SoftPC boundaries, host-resource identity handling, x86/x64 source
observations, and the final persistent guest-location recovery.

The final S20 result removes retained native guest pointers from reached DEM
state, including the non-scalar VDD SFT/JFT case. It does not claim that DEM,
WOW, host-control or any BOP provider is linked or executable. Those owner
packages remain queue-governed and must be admitted from the live package and
interface trackers.


## m0-t280-s10-closure-20260826.md

# M0 T280 S10 closure — reached NT I/O ABI decision

S10 completes the reached `ntioapi.h` ABI decision for the three original DEM
consumers (`demsrch.c`, `demerror.c`, and `demgset.c`).  The closure ledger
contains all eleven reached declaration/call families, their original source
forms, current modern availability, retained lifetime/failure rules and one
unique recovery disposition.

The exact OpenNT `ntioapi.h` remains a source carrier under
`opennt-platform-abi`; direct full inclusion next to modern `winternl.h` is
rejected by S9 evidence.  The next bounded step is one same-shaped
`adapter-win32` reached-subset facade, with direct binding only where exact
modern declarations are verified and explicit `NTSTATUS` unavailability
otherwise.

No host-local record, `HANDLE`, Unicode string, or temporary pointer is a
mapping-manager identity.  Only separately reached guest spans use the
session-owned guest-memory mapper through `adapter-softpc`.

Verification: `Verify-T280S10DemNtioapiAbi.ps1`, documentation governance,
and `git diff --check` passed before closure.  No DEM body, BOP route, host
provider or guest trace was enabled.


## m0-t280-s11-closure-20260826.md

# M0 T280 S11 closure — reached NT I/O facade

S11 provides one `adapter-win32` reached NT I/O facade for the original DEM
directory, volume, object and symbolic-link call shapes.  It preserves the
historical x86-sized `IO_STATUS_BLOCK` source contract on both architectures,
using a host-local modern status record only at the dynamic `ntdll` boundary.

The focused fixture parses original `dem.h`, compiles, links and runs on MSVC
x86 and x64.  Missing dynamic exports yield explicit `STATUS_NOT_IMPLEMENTED`.
No DEM body, BOP/provider route, guest pointer or session mapping is enabled.


## m0-t280-s12-closure-20260826.md

# M0 T280 S12 closure — original DEM object surface

`demsrch.c`, `demerror.c` and `demgset.c` were each observed through the S11
facade on MSVC x86 and x64. The failed observations identify shared historical
NT/RTL declaration dependencies, not service-local BOP work. The only x64
width-specific issue is persisted find-list host identity; it is assigned to
the existing session `host_resource` mapper, never a raw pointer cast.

No original body was linked or changed. See the S12 ledger and evidence.


## m0-t280-s13-closure-20260826.md

# M0 T280 S13 closure — DEM shared NT/RTL declaration surface

One adapter-win32 source-shaped declaration surface now supplies the reached
historical NT constants, object/device constants, RTL list macros and RTL
declarations. The original three DEM bodies pass `/Zs` on MSVC x86 and x64.
The remaining x64 casts are not suppressed: persisted find-list host identity
is transferred to the existing session host-resource mapping owner.


## m0-t280-s14-closure-20260826.md

# M0 T280 S14 closure — DEM find-list host-resource binding audit

The original `demsrch.c` find-list contract is now fully classified before
any mirror body is edited.  `FFindId` remains original numeric search data.
The persisted `PFFINDLIST` is an opaque host identity and must be represented
only by a surrogate from the existing session `host_resource` mapping-manager
instance.  DTA and FCB access remains a separate, bounded
`guest_memory`-lease operation.

The audit establishes publication, reverse lookup, stale-token behavior,
release-before-free ordering, and session-disposal tombstoning.  The present
source tree already has the required bounded imported-call session binding;
`adapter-win32`'s reached `NtVdmControl` facade uses it.  The missing boundary
is the equivalent `adapter-softpc` host-identity facade.  Editing the mirror
before that facade would couple it directly to project implementation,
contrary to the same-shaped recovery boundary.

The successor must add one adapter-softpc same-shaped host-identity facade
over the existing thread-scoped session binding. Only then may `demsrch.c`
receive the registered, source-local divergence. No provider, BOP route,
guest trace, raw pointer persistence, or new mapper is enabled by this
closure.


## m0-t280-s15-closure-20260826.md

# M0 T280 S15 closure — DEM host-identity facade

`adapter-softpc` now owns the sole source-facing host-identity facade for the
reached `demsrch.c` find-list pointer contract. It consumes the existing
thread-bound session and its `host_resource` mapping-manager instance; it does
not create a mapper, store a native pointer in guest state, or touch
guest-memory leases.

The focused x86/x64 Ninja fixture proves no-session rejection, identity
publication/resolution/release, isolation and disposal invalidation. The
original DEM mirror is intentionally still unedited. The successor may make
only the source-local, registered `demsrch.c` storage and validation
divergences, then must compile the original body and prove its failure paths.


## m0-t280-s16-closure-20260826.md

# M0 T280 S16 closure — `demsrch.c` host-identity divergence

The selected OpenNT DEM mirror now preserves the original DOS DTA/FCB 32-bit
layout on x86 and x64, and moves only persisted find-list identity through the
single session `host_resource` mapping manager. Native `PFFINDLIST` values
remain local to the host body; `FFindId` remains original numeric data.

Formal x86/x64 syntax composition and layout fixtures passed. This is not a
provider or runtime closure: the remaining unrelated `demsrch.c` x64 pointer
and HANDLE warnings are recorded for a whole-source S17 audit rather than
patched one warning at a time.


## m0-t280-s17-closure-20260826.md

# M0 T280 S17 closure — `demsrch.c` remaining x64 boundary audit

The complete original `demsrch.c` x64-sensitive surface is now classified in
the eight-row S17 boundary ledger. Four source-owner implementation groups
remain: native-width HANDLE sentinel, host-local pathname/device offset,
host-local directory-buffer cursor arithmetic, and the `VdmQueryDir` facade
relocation to its architectural owner `adapter-vdm-monitor`. None uses an
opaque mapping identity.

The only guest pointer boundary is the original synchronous `GetVDMAddr` call
family. It is explicitly reserved for the existing session `guest_memory`
lease through `adapter-softpc`; it is not part of host-local x64 arithmetic.
Non-width compiler diagnostics remain source-hygiene evidence rather than
permission for an unreviewed algorithm rewrite. No directory provider, BOP
route, guest trace or Bochs change was enabled.

The next bounded source-owner task is S18, which may recover the four
host-local groups together but must leave the guest lease and source-hygiene
groups out of scope.


## m0-t280-s18-closure-20260826.md

# M0 T280 S18 closure — `demsrch.c` host-local x64 binding

All S17 host-local x64 groups are closed together: handle sentinel, pathname
and device offsets, directory-buffer cursor arithmetic, same-shaped NT I/O
binding evidence, and `VdmQueryDir` owner relocation to
`adapter-vdm-monitor`. The selected original `demsrch.c` preserves its
directory-search control flow and failure branches with registered mirror
divergence `MVDM-HOST-DIV-003`.

Formal x86/x64 source and adapter fixtures pass. The remaining direct
`GetVDMAddr` calls deliberately remain a separate `adapter-softpc` bounded
guest-memory lease problem; neither S18 nor the moved monitor facade creates a
guest provider or mapping shortcut.


## m0-t280-s19-closure-20260826.md

# M0 T280 S19 closure — DEM guest lease contract audit

The original `demsrch.c` guest-address surface is fully classified. Transient
DTA/FCB accesses are bounded synchronous guest-memory lease candidates;
pathname inputs require a source-proven bounded-string policy. The audit also
found the essential cross-owner blocker: `demgset.c` stores `GetVDMAddr`
native pointers in long-lived DEM globals later consumed by `demsrch.c`.

Therefore no raw `GetVDMAddr` shim is admitted. The successor must first
replace these retained pointers with guest-address descriptors and fresh
per-dereference leases. This preserves original OpenNT source roles while
respecting the single session mapping-manager implementation and its
`guest_memory` instance.


## m0-t280-s20-closure-20260826.md

# M0 T280 S20 closure — DEM persistent guest-location recovery

S20 has removed every reached DEM global which retained a `GetVDMAddr` native
pointer beyond its source callback:

* DTA, extended-error and current-PDB locations retain only their original
  numerical 16:16 form and obtain fresh bounded guest-memory leases;
* the SFT-chain head retains DS:offset numeric data; and
* the original VDD SFT/JFT pointer ABI is recovered through a session-owned,
  synchronous host shadow with explicit source-visible commit/discard points.

The existing session `host_resource` mapping-manager instance owns only
host-local shadow and native-HANDLE identities. It never tokenises a DOS
numeric address. Formal x64/x86 fixtures prove the shadow lifecycle,
stale rejection, session isolation and teardown cleanup; formal DEM source
syntax passes on both architectures.

This is a source/boundary closure only. No DEM/WOW provider, BOP selector or
guest trace is enabled. Other transient `GetVDMAddr` calls outside the reached
retained-global family remain owned by their separately classified packages.

