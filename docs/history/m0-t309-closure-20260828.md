# M0 T309 closure — original DEM/COMMAND provider cohort boundary

## Result

The exact original DEM (16 translation units / 73 table entries) and COMMAND
(11 translation units / 17 table entries) cohorts compile and archive under
formal MSVC `/MT` x86 and x64 graphs with their unchanged original dispatcher
tables. The current Bochs mechanical archive also compiles as the named lower
machine boundary.

Every residual provider-origin external form has one physical caller,
original owner and final disposition in the P3 ledger. No service body,
dispatcher entry, BOP selector, or historical machine executor was substituted
to make the audit link succeed.

## Original source-candidate disposition

- `nt_pif.c` compiles unchanged on x86/x64; its finite PIF/session/Win32
  boundary is retained for later source-shaped composition.
- `nt_error.c` retains its whole original body. A modern Console facade and
  original SoftPC `CpuH` interface are named prerequisites; no dialog shim was
  substituted.
- `nt_event.c`, `nt_hosts.c` and `nt_lpt.c` remain a complete original
  Console/stream/LPT lifecycle group, not detached COMMAND helpers.
- `nt_msscs.c` remains the original host-composition/VDD root and transfers
  intact to the selectable original-SoftPC/Bochs backend task.

## Verification

- Formal Ninja cohort archive and deliberate `/force:unresolved` boundary
  audits ran under MSVC `/MT` for x86 and x64.
- The x86/x64 provider-origin caller set has the same 133 physical records;
  architecture differences are only toolchain spelling/decoration forms.
- Original `nt_pif.c` compiled unchanged under both architectures. The
  original error source exposes its declared modern Console and SoftPC
  prerequisites without an original-body change.
- Documentation governance and `git diff --check` passed for every delivery.

## Boundary and next work

T309 does not claim runnable complete DEM/COMMAND behavior or a continuous
guest execution path. Its remaining source composition is explicitly owned by
the queued original SoftPC backend, modern Console facade, VDD, Redirector,
WOW and later package tasks. The preferred next machine-composition work must
preserve original SoftPC source and make SoftPC or Bochs a per-session selected
backend; it must not crop those source owners to force the Bochs result.
