# m0-t225 historical record

Consolidated task record. Raw contemporaneous records are retained in the external documentation archive.

## m0-t225-closure-20260817.md

# M0 T225 closure — controlled replan to BOP owner-package code completion

## Outcome

T225 closes by owner-directed reprioritization. It established a substantial and reusable evidence baseline for Guest DOS multi-program execution, but it does **not** claim that a guest parent/child/return sequence completes. The remaining native `BOUND` continuity observation is not a BOP endpoint and must not continue to select leaf implementation work.

## Retained closure facts

- T225 S19 closes the callable DEM `50:00..48` package at its declared Direct/Readonly boundary, including a 73-slot source/ABI/failure map and whole-family regressions.
- T225 S20 closes the callable COMMAND `54:00..10` package at its declared Direct/Readonly boundary, including a 17-slot source/route map and whole-family regressions. Its CLI process-broker exclusion remains an explicit limitation, not a replacement implementation.
- T225 S56 closes the grouped real-mode segment-limit compatibility profile; it does not select an opcode-level continuation repair.
- T225 S57 closes the current owner/package/profile-disposition reconciliation for all 203 retained original BOP endpoints. A disposition or typed stop is not automatically a complete historical provider.
- S58 was admitted only to diagnose the post-BOP continuity observation. No repair, provider, guest image change, or Bochs semantic change was accepted from that trace.

## Handoff

M0 T226 is admitted to turn the 203-row ledger into package-by-package local code-complete and conformance-tested behavior. It works by original owner package, uses the mandatory source-recovery ladder, and defers native guest traces until a completed package has its local regression. The later Queue candidate, *Guest DOS native multi-program integration and execution continuity*, owns the parent/child/return and `BOUND` integration questions.


## m0-t225-s19-closure-20260817.md

# M0 T225 S19 Closure — DEM whole-package recovery and route convergence

S19 closes the complete callable DEM `50:00..48` package at its declared
boundary.  The 73-slot source/ABI/failure map assigns every identity a
reusable-original, source-derived, original no-op/failure, or explicit
deferred disposition.  The current CLI reaches DEM only through the one
composition package session; historic adapter-runtime entry gates are not a
live bypass.

The current MSVC x64 `/MT` whole-package fixture passes all 73 identities in
both Direct and Readonly modes.  The OpenNT PDB ordering correction preserves
`50:3C -> DOS_ABORT -> 50:02`, and `50:25` now follows the original
`demClientError` failure split.  A fresh 302-edge Ninja-built native CLI then
ran one bounded Direct/Readonly validation pair.  Both modes produced the
same 33 BOP and 33 generic-UD records and ended by the internal tick budget.

This closure proves DEM package routing and its stated profile behavior.  It
does not claim DOS program completion, whole COMMAND recovery, Overlay
implementation, machine/BIOS recovery, or general DOS compatibility.  Trace
identities remain validation evidence only and do not create a new leaf
service backlog.

Evidence:

- [73-slot route reconciliation](../../artifacts/documentation-archive/20260910/etc/research/t225-s19-dem-full-package-route-reconciliation-001.md)
- [Direct/Readonly family regression](../../artifacts/documentation-archive/20260910/etc/evidence/t225-s19-dem-direct-readonly-package-regression-result-001.md)
- [legacy route inventory](../../artifacts/documentation-archive/20260910/etc/research/t225-s19-legacy-runtime-route-inventory-001.md)
- [PDB lifecycle ordering result](../../artifacts/documentation-archive/20260910/etc/evidence/t225-s19-dem-pdb-lifecycle-ordering-result-001.md)
- [DPB failure result](../../artifacts/documentation-archive/20260910/etc/evidence/t225-s19-dem-dpb-failure-regression-result-001.md)
- [bounded native package validation](../../artifacts/documentation-archive/20260910/etc/evidence/t225-s19-dem-native-package-validation-result-001.md)


## m0-t225-s20-closure-20260817.md

# M0 T225 S20 Closure — COMMAND Whole-Package Recovery

## Scope

S20 reconciled the complete original OpenNT COMMAND dispatcher package
`54:00..10` through the one bound bx-vdm package session.  It did not claim
interactive COMMAND, historical BaseSrv/CSR re-entry, guest program
completion, or a generic host-process broker.

## Closure Evidence

1. [17-slot source/route reconciliation](../../artifacts/documentation-archive/20260910/etc/research/t225-s20-command-full-package-route-reconciliation-001.md)
   assigns every callable `cmddisp.c` entry to an original owner, source
   recovery rung, and Direct/Readonly disposition.
2. [Sensitive subfamily map](../../artifacts/documentation-archive/20260910/etc/research/t225-s20-command-sensitive-subfamily-source-map-001.md)
   preserves host CWD, opaque handle, child-terminal, console/keyboard and
   dynamic-environment limits without inventing a virtual root or 4023-byte
   environment cap.
3. [Direct/Readonly package regression](../../artifacts/documentation-archive/20260910/etc/evidence/t225-s20-command-direct-readonly-package-regression-result-001.md)
   passes all callable services through the one package session using the
   formal MSVC x64 `/MT` Ninja graph.
4. [Live-route inventory](../../artifacts/documentation-archive/20260910/etc/research/t225-s20-command-live-route-inventory-001.md)
   proves the formal CLI graph does not link legacy exact COMMAND branches
   from `adapter_runtime`.
5. [Bounded native validation](../../artifacts/documentation-archive/20260910/etc/evidence/t225-s20-command-native-package-validation-result-001.md)
   builds all 302 native graph edges and preserves an identical 33-record
   Direct/Readonly observation sequence with the normal budget terminal.

## Exit-Criteria Review

| S20 exit criterion | Evidence | Result |
| --- | --- | --- |
| Every callable COMMAND slot has source-proven disposition | 17-slot reconciliation | met |
| Retained/replaced Direct/Readonly routes have one family regression | package regression | met |
| No stale shim bypasses package session | live-route inventory | met |
| One bounded native package validation occurs after regression | native validation | met |

## Limitations And Handoff

The original `cmdExec32` BaseSrv/CSR re-entry branch remains deliberately
unadmitted: the CLI launch plan has no compatible external command producer,
and manufacturing a queue would duplicate unavailable historical composition.
The bounded Direct child transaction is terminal-only; Readonly remains
explicitly unavailable.  The next T225 subtask starts from the guest DOS
`EXEC`/PSP/parent-return owner package, not from any trace value.

## m0-t225-s21-closure-20260817.md

# M0 T225 S21 Closure — Guest DOS EXEC/PSP/Parent-Return Admission

S21 closes the source and current-route admission for the Guest DOS
multi-program package. It establishes that a declared multi-program sequence
must remain an original NTDOS guest process tree in one VDM. `$Exec`, DOS
arena/PSP/JFN/environment construction, child entry, termination and parent
return remain guest-owned; bx-vdm must not create a DOS process or replace it
with a nested host NTVDM/child process.

The [source/ABI/current-route map](../../artifacts/documentation-archive/20260910/etc/research/t225-s21-guest-dos-exec-psp-return-source-map-001.md)
records the fixed EXEC parameter and PSP layouts, the `50:11` NTDOS
identity-checked guest-RAM staging seam, and the constrained `50:36`/`50:3C`
DEM notifications. It confirms that the latter do not own PSP state, ordinary
file close, or parent return.

S21 does not claim that an original NTDOS `$Exec` path has executed. The
smallest next closure is continuous native reach from source-built NTIO through
original NTDOS initialization and the original COMMAND handoff. Any observed
missing mechanism is a separately-owned startup, DEM, or bx-core/bx-mantle
question; an adapter loader or host-process substitute remains rejected.

## m0-t225-s22-closure-20260817.md

# M0 T225 S22 Closure — Original NTIO-to-NTDOS-to-COMMAND Continuity Admission

S22 closes the source-built startup-continuity admission for T225. It does not
claim guest program execution, parent/child return, complete COMMAND behavior,
or a runnable NTVDM.

## Established facts

- A fresh formal Ninja MSVC x64 `/MT` build ran the source-built primary guest
  input in Direct and Readonly. Both crossed original NTIO `50:11`, NTDOS
  initialization, `50:36`, and COMMAND `54:02/0D/0F` with identical output.
  The exact observation is retained in
  [P1](../../artifacts/documentation-archive/20260910/etc/evidence/t225-s22-native-startup-continuity-observation-result-001.md).
- P2 corrected the source-defined COMMAND `54:0F` success return: BX remains
  the required environment paragraph count after publication. The focused
  formal family regression passes; the native boundary remained unchanged.
- P3 resolved `C4 C4 06` as the original SoftPC INT 06 machine path
  (`illegal_op_int`), distinct from debugger `56h`. It is not a DEM, COMMAND,
  debugger, or adapter host-service selector.
- P4 proves the historical `illegalp.c`/`unexp_nt.c` pair is an x86 clang-cl
  source island, while the formal product is MSVC x64 `/MT`; the old objects
  cannot be linked across that boundary. It also proves empty default
  port-space is not an original PIC substitute.

## Transfer

The next active subtask is S23, the whole bx-mantle INT 06 machine-composition
recovery. It must establish original-handler source/ABI equivalence, native PIC
port lifecycle, typed x64 mantle mechanics and the formal Ninja closure before
one bounded native Direct/Readonly observation. It may not turn the observed
selector into an adapter BOP provider, move selector meaning into Bochs, or use
an x86/prebuilt object in the product.

## m0-t225-s23-closure-20260817.md

# M0 T225 S23 Closure — bx-mantle INT 06 Machine Composition

S23 closes the admitted whole INT 06 machine-composition prerequisite for
T225.  It does not claim a complete machine/BIOS BOP package, guest program
execution, parent/child return, or general BOP recovery.

## Closure Evidence

1. The [source/package map](../../artifacts/documentation-archive/20260910/etc/research/t225-s23-int06-native-pic-source-map-001.md)
   identifies the original `illegal_op_int`/`unexpected_int` pair and assigns
   CPU/memory/PIC mechanics to the Bochs layers.
2. The [PIC lifecycle audit](../../artifacts/documentation-archive/20260910/etc/research/t225-s23-native-pic-lifecycle-link-closure-001.md)
   and [native lifecycle result](../../artifacts/documentation-archive/20260910/etc/evidence/t225-s23-native-pic-lifecycle-result-001.md)
   prove the admitted original 8259 port lifecycle under the formal MSVC x64
   `/MT` Ninja graph.
3. The [x64 rehost decision](../../artifacts/documentation-archive/20260910/etc/research/t225-s23-int06-x64-rehost-admission-001.md)
   records why the historical x86 callback island cannot enter the modern
   process, and the [provider record](../../artifacts/documentation-archive/20260910/etc/research/t225-s23-opennt-host-int06-provider-admission-001.md)
   proves the fixed-width source-derived transcript preserves original frame,
   PIC, EOI and BDA ordering.
4. The [typed native port action](../../artifacts/documentation-archive/20260910/etc/evidence/t225-s23-byte-port-action-result-001.md)
   and [RAM preflight](../../artifacts/documentation-archive/20260910/etc/evidence/t225-s23-ram-preflight-result-001.md)
   retain selector-blind mantle mechanics and reject inactive or invalid work.
5. The [native transaction fixture](../../artifacts/documentation-archive/20260910/etc/evidence/t225-s23-int06-native-transaction-result-001.md)
   executes the complete source-derived operation sequence against a real
   minimal machine and checked resume.
6. The [bounded Direct/Readonly observation](../../artifacts/documentation-archive/20260910/etc/evidence/t225-s23-direct-readonly-native-int06-observation-001.md)
   proves both profiles execute accepted `C4 C4 06` on identical immutable
   NTIO/NTDOS/COMMAND input and retain an identical observed sequence.

## Exit-Criteria Review

| S23 exit criterion | Evidence | Result |
| --- | --- | --- |
| Original handler source/ABI/failure contract is mapped | source map and x64 rehost decision | met |
| Minimum native PIC and ordinary-RAM lifecycle is source-built | lifecycle audit, native PIC, port and RAM results | met |
| Typed x64 mechanics and source-derived provider compose through formal graph | provider and native transaction results | met |
| One bounded Direct/Readonly native observation follows composition | P9 observation | met |

## Boundary And Handoff

`src/bx-core` and `src/bx-mantle` remain selector-, BOP-, OpenNT- and DOS-blind.
`src/bx-vdm` alone recognizes the admitted three-byte machine event and routes
copied operations to `opennt-host`; no host-capability or general BOP provider
was added.  The observed `02h` is therefore only a machine/BIOS owner-package
audit fact.  S24 must inventory and source-classify the complete reachable
machine/BIOS selector family before admitting another implementation.  It may
not convert trace order into singleton repair work.


## m0-t225-s24-closure-20260817.md

# M0 T225 S24 Closure — Machine/BIOS Selector Owner Audit

S24 closes the required whole-owner audit before another startup-machine
implementation is admitted. It does not claim a full PC BIOS, a new device,
or execution of a guest program.

## Established Facts

- The historical SoftPC `BIOS[]` universe remains fully classified by the
  retained T131 evidence: 41 named definitions and 349 conditional forms,
  split into 227 unavailable, 25 OpenNT top-level collisions and 97
  Bochs-native candidates.
- The current source-built startup reaches nine relevant machine/top-level
  identities: `02`, `06`, `12`, `15:AH=88`, `17`, `5E`, `5F`, `66`, and `C8`.
  Their original owners, current bound routes, recovery rungs and
  Direct/Readonly disposition are recorded in the
  [S24 ledger](../../artifacts/documentation-archive/20260910/etc/research/t225-s24-machine-bios-current-route-ledger-001.md).
- `06` is the sole typed native `opennt-host` transaction. `02` is reachable
  but currently declines through the formal bridge; the old callback/TLS
  `machine_composition_v2` island is compiled evidence, not a valid live
  successor.
- `5E` stays with the OpenNT top-level package. The other startup identities
  already split between legacy checked transactions, bounded profile values
  and explicit no-device continuations.

## Verification Limitation

The retained `Test-SoftpcBiosOwnerMatrix.ps1` cannot run in this checkout
because its pinned historic JSON export is absent. S24 does not recreate that
input or turn a direct source scan into a new immutable inventory. The prior
T131 result remains the complete-table evidence; S24's current-route facts
are direct source review.

## Transfer

S25 owns one startup-machine composition normalization package: a unified
source-derived `unexpected_int` family for `02` and `06`, plus audited
migration/regression of the retained `12`, `15:88`, `17`, `5F`, `66` and `C8`
routes. It must leave `5E` with its top-level owner, keep bx-core/bx-mantle
selector-blind, add no device or historical BIOS table, and run a family
regression before exactly one native Direct/Readonly validation.


## m0-t225-s25-closure-20260817.md

# M0 T225 S25 Closure — Startup-Machine Composition Normalization

## Delivered

S25 closes the complete admitted startup-machine package: `02`, `06`, `12`,
`15:AH=88`, `17`, `5F`, `66`, and `C8`. The implementation establishes one
live bx-vdm composition surface. It retains `5E` as a distinct OpenNT
 top-level route.

`02` enters the source-derived OpenNT `unexpected_int` common suffix without
a synthetic frame; `06` retains `illegal_op_int`'s saved-frame and carry
ordering before entering that same suffix. All host-side operations remain
fixed-width typed mantle RAM/port requests. No callback/TLS legacy island is
selected.

## Evidence

- [P1 source/ABI/failure map](../../artifacts/documentation-archive/20260910/etc/research/t225-s25-startup-machine-source-abi-failure-map-001.md)
  records original owners, direct-reuse exclusions, retained ordering and
  failure behavior for every member.
- [P2 shared interrupt result](../../artifacts/documentation-archive/20260910/etc/evidence/t225-s25-startup-machine-interrupt-result-001.md)
  source-builds the 02h/06h shared transcript and proves its focused
  minimal-machine cases.
- [P3 family/native result](../../artifacts/documentation-archive/20260910/etc/evidence/t225-s25-startup-machine-family-native-validation-result-001.md)
  records the whole-family positive/negative fixture and byte-identical
  Direct/Readonly native validation.

The formal MSVC x64 `/MT` Ninja graph built the family fixture and fresh
native CLI from `build/M0-T225-S25/startup-machine-r2`. Direct and Readonly
both returned internal-budget exit 4 with identical 33-record BOP/#UD logs.

## Non-claims and transfer

This closure neither implements a general BIOS table nor claims guest DOS
EXEC, PSP creation, child entry, parent return, WOW16, DPMI, Redirector, or
additional device support. No BOP observed in native validation becomes a
new implementation task.

T225 continues with its declared guest multi-program execution outcome. The
next S begins from original NTDOS EXEC/PSP/return source and current composed
route evidence, not from trace order.

## m0-t225-s26-closure-20260817.md

# M0 T225 S26 Closure — Guest EXEC Current-Route Reconciliation

## Delivered

S26 closes its owner-scoped source/current-route audit for a guest DOS EXEC
child's lifecycle. It establishes that NTDOS remains the exclusive owner of
EXEC, PSP/arena/JFN/environment setup, guest entry transfer, child teardown,
and saved parent continuation. The completed DEM file/lifecycle packages and
startup-machine package are prerequisites, not substitutes for that guest
behavior.

The audit identifies the only remaining composition boundary: OpenNT COMMAND
parent-return handling at `54:0B` / `cmdReturnExitCode`. The current route can
perform only a limited register resume. Its terminal contract deliberately
accepts only a top-level exit and rejects a DOS exit code, so it cannot publish
an ordinary guest child result or declared re-entry after engine cleanup.

## Evidence

- [S26 source/current-route map](../../artifacts/documentation-archive/20260910/etc/research/t225-s26-guest-exec-current-route-reconciliation-001.md)
  records the original guest/host ownership, current Direct/Readonly
  disposition, recovery ladder, and the source-defined gap.
- Original references are `msdisp.asm`, `msproc.asm`, `misc.asm`,
  `msctrlc.asm`, `pdb.inc`, and OpenNT COMMAND `cmdexec.c` / `cmddisp.c`.

## Transfer

S27 owns one complete COMMAND parent-return lifecycle package: first its
source/ABI/failure map, then only the smallest contract-preserving seam if
original COMMAND code cannot compose, followed by family regression and one
bounded native observation. `54:00` remains a distinct top-level VDM exit.

This closure neither claims that a COM or MZ child has executed nor authorizes
a synthetic DOS loader, adapter-owned DOS return queue, host child process, or
trace-selected singleton work.

## m0-t225-s28-closure-20260817.md

# M0 T225 S28 Closure — Native First-Fault Diagnostic

## Closed Scope

S28 establishes a default-off, selector-blind mechanical diagnostic for the
native repeated segment-limit failure reported by S27.  It does not repair a
guest image, segment descriptor, BIOS, device, BOP, provider, or host
capability.

## Delivered Evidence

- [Source/ABI/failure map](../../artifacts/documentation-archive/20260910/etc/research/t225-s28-native-segment-fault-diagnostic-source-abi-map-001.md)
  rejects reuse of the #UD/BOP bridge and records the limited BX-TRACE-084
  exception.
- [Focused fixture result](../../artifacts/documentation-archive/20260910/etc/evidence/t225-s28-first-fault-observation-fixture-result-001.md)
  proves default-off decline plus exactly one test-owned non-UD capture and
  finite stop under formal MSVC x64 `/MT`, CPU5/Pentium-MMX.
- [Native Direct/Readonly result](../../artifacts/documentation-archive/20260910/etc/evidence/t225-s28-native-first-fault-direct-readonly-result-001.md)
  records identical first facts: `#GP(13)`, error `0`, pre-delivery
  `CS:EIP=0C41:2357`, `EAX=0`, `EFLAGS=0x93`, real-mode value `1`, and the
  distinct machine-failure first-fault terminal.

The implementation is local commit `5571f03b`
(`feat(t225): complete first-fault diagnostic`).  It is pending remote delivery
because the execution environment requires an explicit current confirmation of
the configured remote's ownership before it will transmit repository contents.

## Result And Transfer

The two supported mutation modes have byte-identical stdout and stderr hashes,
so host mutation policy does not explain the failure.  The typed observation
also does not distinguish incorrect source-built guest/startup layout from
Bochs segment-access or exception-delivery mechanics.  Consequently S28 does
not admit a repair.

S29 owns one read-only attribution map: correlate the observed physical
address (`0x0C41 << 4 + 0x2357 = 0xE767`) with the immutable staged-image
layout, original OpenNT source/control-flow evidence, and the exact adopted
Bochs segment-access path.  It must either name the next owner or retain the
ambiguity; no trace-led BOP implementation follows from S28.


## m0-t225-s29-closure-20260817.md

# M0 T225 S29 Closure — Native First-Fault Source/Layout Attribution

## Closed scope

S29 was a read-only source/layout audit of S28's first native `#GP(13)` at
`0C41:2357`. It made no runtime, adapter, guest, or Bochs change.

## Delivered evidence

- [The source/layout attribution](../../artifacts/documentation-archive/20260910/etc/research/t225-s29-native-first-fault-source-layout-attribution-001.md)
  recomputes the physical address, revalidates immutable source-built inputs,
  maps initial staging and the formal 50:11 NTDOS load contract, and identifies
  the exact adopted Bochs virtual-word access failure path.

## Result and transfer

The fault is outside the initial NTIO image and is compatible with executing
the formally loaded source-built NTDOS image, but neither an exact NTDOS
source routine nor the actual failing access tuple is recorded. Direct and
Readonly parity rejects host-mutation profile policy as the cause. No BOP,
provider, guest, or machine repair is admitted.

S30 owns only a default-off, selector-blind bx-core segment-access fact
diagnostic, first mapped and registered before implementation. It may expose
the original rejected access tuple through the existing finite mantle stop;
it may not interpret it or repair behavior.

## m0-t225-s30-closure-20260817.md

# M0 T225 S30 Closure — Native Segment-Access Tuple Diagnosis

## Closed scope

S30 added the smallest registered default-off selector-blind observation at the
original `read_virtual_word_32` failure branches. It made no emulation repair.

## Delivered evidence

- [The source/ABI/failure map](../../artifacts/documentation-archive/20260910/etc/research/t225-s30-segment-access-tuple-source-abi-failure-map-001.md)
  retains the original Bochs access predicates and records BX-TRACE-085 before
  code changes.
- [The focused fixture result](../../artifacts/documentation-archive/20260910/etc/evidence/t225-s30-segment-access-observation-fixture-result-001.md)
  proves disabled and duplicate decline plus a test-owned direct-limit tuple.
- [The immutable native result](../../artifacts/documentation-archive/20260910/etc/evidence/t225-s30-native-segment-access-direct-readonly-result-001.md)
  records byte-identical Direct/Readonly `DS=0C41`, offset/limit `FFFF`,
  two-byte direct-limit facts.

## Result and transfer

The fault is not profile mutation, BOP routing, or a generic unknown #GP. It
is the original direct-limit branch for a DS-segment word read at offset FFFF.
The tuple does not prove the NTDOS source instruction or the lifecycle that
formed its pointer. S31 therefore owns only a read-only original-source
control-flow/data-pointer attribution; no CPU wrap, descriptor, guest, or
provider repair is admitted.

## m0-t225-s31-closure-20260817.md

# M0 T225 S31 Closure — DS:FFFF Source/Lifecycle Attribution

## Closed scope

S31 was a read-only source/build/control-flow attribution of S30's native
DS-segment end-of-limit word-read. It made no runtime or source implementation
change.

## Delivered evidence

- [The attribution map](../../artifacts/documentation-archive/20260910/etc/research/t225-s31-ds-ffff-source-lifecycle-attribution-001.md)
  establishes that `DEVIOCALL2`'s original `MOV AX,[SI.SDEVSTRAT]` at the
  source-built NTDOS file offset `0x2AF7` is the only identified exact
  source/layout candidate for DS offset `FFFF` with a two-byte read.

## Result and transfer

The first native record proves DS `0C41`, not the expected initial NTIO `0070`
device-header segment. The candidate's dynamic execution is intentionally not
claimed: the retained first-fault EIP is not an instruction-boundary witness.

S32 owns the complete original guest bootstrap/device-header lifecycle map,
from formal 50:11 loading through sysinit/DosInit/charinit and DEVIOCALL2
input provenance. It may not repair CPU, guest, device or provider behavior.

## m0-t225-s32-closure-20260817.md

# M0 T225 S32 Closure — Original Bootstrap/Device-Header Lifecycle Map

## Closed scope

S32 is a read-only, source-first audit of the original NTIO-to-NTDOS bootstrap
and first device-header lifecycle. It made no runtime, guest, BOP/provider,
Bochs, or host-capability change.

## Delivered evidence

- [The lifecycle source/ABI/failure map](../../artifacts/documentation-archive/20260910/etc/research/t225-s32-bootstrap-device-header-lifecycle-source-abi-failure-map-001.md)
  covers original resident NTIO initialization, `50:11` NTDOS loading,
  temporary `sysinit` relocation, `DosInit`/DOSDATA transition, device-chain
  initialization, and `charinit`/`DEVIOCALL2` inputs.

## Result and transfer

The current `50:11` implementation is proven as a narrow original-shaped
mechanical seam: it writes the authenticated NTDOS image to the guest-selected
real-mode `DI<<4` address and resumes after the four-byte BOP. It neither
selects a DOS load address nor constructs DOSDATA, a device header, a request
packet, or a device chain.

The retained native DS end-of-limit event is post-load but pre-attribution.
It remains compatible with the source-built `DEVIOCALL2` strategy-field read,
but establishes neither dynamic instruction identity nor `sysinit`/`DosInit`
completion. S33 owns only the source/ABI admission of a bounded,
non-invasive guest bootstrap continuity observation. It may not implement a
BOP/provider, preseed original state, or repair CPU/machine behavior.

## m0-t225-s33-closure-20260817.md

# M0 T225 S33 Closure — Bootstrap Continuity Observation Admission

## Closed scope

S33 audited whether existing, non-invasive evidence can distinguish original
guest bootstrap stages. It made no runtime, BOP/provider, guest, Bochs, or
host-capability change.

## Delivered evidence

- [The observation admission map](../../artifacts/documentation-archive/20260910/etc/research/t225-s33-bootstrap-continuity-observation-admission-map-001.md)
  correlates the retained 33-record source-built BOP sequence to original
  `sysinit`, `DosInit`, and post-`charinit`/device-chain checkpoints.

## Result and transfer

The existing generic-UD sequence copier is sufficient and already has the
right selector-blind, fixed-width boundary. The record at `010D:0516` proves
`sysinit`; `9346:7439 50:0F` proves `DosInit`; and `9346:7588 50:46` proves
that the console/device initialization loop reached `continit`. No generic
instruction trace or new external-code intrusion is justified.

S34 owns only source/build attribution of the separately retained first
non-UD state `0C41:2357` after these now-proven guest stages. It may not add a
new trace, mutate guest state, implement a BOP/provider, or repair Bochs.

## m0-t225-s34-closure-20260817.md

# M0 T225 S34 Closure — Post-Bootstrap First-Fault Attribution

## Closed scope

S34 reconciled source-built NTDOS layout, relocation assumptions, the S30
first-fault tuple, and the separately retained S19 BOP transcript. It made no
runtime or source implementation change.

## Result and transfer

The exact source routine at `0C41:2357` remains unproven. S19 proves bootstrap
reach only in its own invocation and must not be merged with S30's first-fault
history. The existing fixed-width BOP and first-fault observers are sufficient
to bind both facts in one next run; no new Bochs intrusion or general trace is
justified.

S35 owns one paired Direct/Readonly immutable-input observation using both
existing options, with no code, guest, profile, provider, or machine change.

## m0-t225-s35-closure-20260817.md

# M0 T225 S35 Closure — Paired Bootstrap/First-Fault Observation

S35 ran the admitted Direct and Readonly immutable-input observations exactly
once each, with the existing BOP-sequence and first-fault observers together.
Both outputs are byte-identical. The same run reaches the existing `06h`
startup-machine BOP before the `0C41:2357` DS:FFFF first fault.

[The reviewed result](../../artifacts/documentation-archive/20260910/etc/evidence/t225-s35-paired-bootstrap-first-fault-observation-result-001.md)
records inputs, hashes, full boundary conclusion and the disposable raw-log
location. It establishes ordering only. It does not assign causation to `06h`
or admit a BOP, adapter, guest, CPU, or machine repair.

## m0-t225-s36-closure-20260817.md

# M0 T225 S36 Closure — Startup-Machine Post-Return Component Audit

## Closed scope

S36 audited the complete startup-machine package's original/current selector
`06h` frame, return, PIC/BDA and sibling contracts against S35's paired
observation.  It made no code, provider, guest, Bochs, profile, or trace
change.

## Result and transfer

[The audit](../../artifacts/documentation-archive/20260910/etc/research/t225-s36-startup-machine-postreturn-component-audit-001.md)
proves that selector `06h` only affects its saved return frame and original
PIC/BDA transcript; the accepted event is ordered before, but is not proven to
cause, the later DS:FFFF fault.  It also corrects the prior provisional layout
reading: the same live `0C41` segment issues COMMAND BOP calls, so it cannot be
treated as a demonstrated NTDOS segment.

S37 owns one complete COMMAND guest-image/lifecycle provenance audit.  It may
use existing source/build and bounded-observation evidence to identify the
load/segment and lifecycle boundary, but it admits neither a new trace nor a
singleton BOP/provider repair without a separate packet.


## m0-t225-s37-closure-20260817.md

# M0 T225 S37 Closure — COMMAND Guest-Image/Lifecycle Provenance Audit

## Closed scope

S37 reconciled the original NTDOS `$Exec` guest-loader contract, the
source-built COMMAND artifact, the existing whole COMMAND host-service
package, and S35's same-run COMMAND BOP locations.  It made no source,
provider, guest, Bochs, or trace change.

## Result and transfer

[The audit](../../artifacts/documentation-archive/20260910/etc/research/t225-s37-command-guest-image-lifecycle-provenance-audit-001.md)
separates artifact identity, guest image lifecycle, and host-service routing.
It proves that `$Exec`, not a `54h` provider, owns COMMAND segment/PSP/entry
state; `0C41` remains only a COMMAND-plane caller-segment candidate.  Exact
segment/routine attribution remains deliberately unresolved.

S38 owns the smallest complete COMMAND guest-image continuity observation
design: source/ABI/failure mapping and a default-off selector-blind loader
transfer observation.  It admits no BOP implementation, guest mutation or
Bochs semantic repair.


## m0-t225-s38-closure-20260817.md

# M0 T225 S38 Closure — COMMAND Guest-Image Continuity Observation Design

S38 establishes that the original `$Exec` transfer registers are already
available at the generic BOP bridge, while the public sequence record omits
them.  [The design](../../artifacts/documentation-archive/20260910/etc/research/t225-s38-command-image-continuity-observation-design-001.md)
admits only a versioned, default-off, selector-blind bx-vdm/CLI snapshot
record.  It rejects new Bochs hooks, `50:36` special behavior, guest reads and
all BOP/provider repairs.

S39 owns that bounded implementation and its focused/boundary evidence; a
native Direct/Readonly observation is admissible only after the static proof.


## m0-t225-s39-transfer-20260817.md

# M0 T225 S39 Transfer Record — Selector-Blind Snapshot Implementation

S39 delivered and pushed the version-2 default-off selector-blind BOP
snapshot recorder at `706d2995`. Its fixed-width pre-dispatch register and
segment snapshot, capacity/overflow behavior, focused fixture and boundary
scan are complete.

The required native paired observation did not reach a normal result because
the existing mantle instruction watchdog did not wake the normal CPU loop
when it set its kill latch. [The attempt record](../../artifacts/documentation-archive/20260910/etc/evidence/t225-s39-native-observation-attempt-001.md)
preserves that limitation. It makes no BOP, COMMAND, guest, provider, profile
or Bochs semantic claim.

S40 is the separately admitted, mantle-only mechanical prerequisite. It owns
the existing watchdog delivery path and must complete before S39's paired
observer result can be retried. This transfer is not a claim that S39's
native Direct/Readonly acceptance criterion has passed.

## m0-t225-s40-closure-20260817.md

# M0 T225 S40 Closure — Mantle Watchdog Asynchronous Stop

S40 closes the smallest mechanical prerequisite exposed by S39. The project
mantle one-shot watchdog and cancellation poll already set the native Bochs
kill latch, but normal CPU execution only services that latch after an
asynchronous event. S40 makes those callbacks set the existing stop-trace
async marker as well.

[The source/ABI map](../../artifacts/documentation-archive/20260910/etc/research/t225-s40-mantle-watchdog-async-stop-source-abi-map-001.md)
and [result](../../artifacts/documentation-archive/20260910/etc/evidence/t225-s40-mantle-watchdog-async-stop-result-001.md)
prove the mantle-only route, normal-loop budget terminal and paired
Direct/Readonly CLI budget terminal. No bx-core, BOP, guest, provider,
profile, OpenNT or CLI semantic change was made.

S41 owns one resumed paired native observation of the already-implemented S39
selector-blind recorder. It may report facts only; it does not admit repair.

## m0-t225-s42-closure-20260817.md

# M0 T225 S42 Closure — Exception-Loop Watchdog Delivery

S42 completes the mantle-only correction that lets the existing kill latch
survive the native CPU trace-clear path. Focused and paired native evidence
prove the existing budget terminal in Direct and Readonly. The paired run
also completes the previously unavailable S39 v2 snapshot observation,
including an observed `50:36` copied-state fact.

It admits no BOP repair. A later owner must use original source to interpret
that fact before proposing any guest/COMMAND continuity work.

## m0-t225-s43-closure-20260817.md

# M0 T225 S43 Closure — 50:36 Source Attribution

S43 closes the read-only attribution of the paired selector-blind `50:36`
observation. Original OpenNT source fixes it as
`SVC_DEMENTRYDOSAPP`, issued by NTDOS `$Exec` immediately before child transfer.
The copied `DS:SI`, `AX:DI`, `DX`, and `BX` low-word values agree with the
original child-entry, initial-stack, PDB, and initial-AX contract.

This closes neither DEM/COMMAND as a whole nor a child program’s execution or
parent return. No runtime source, BOP/provider route, guest image, profile, or
native retry changed. The [source-attribution record](../../artifacts/documentation-archive/20260910/etc/research/t225-s43-observed-50-36-source-attribution-001.md)
contains the paths, field map, and evidence limits.


## m0-t225-s44-closure-20260817.md

# M0 T225 S44 Closure — Guest EXEC And Parent-Return Route Reconciliation

S44 reconciles the complete original guest `EXEC`/PSP/entry-transfer/termination/
parent-return lifecycle with the live formal CLI composition. It confirms that
`50:36` is currently a no-VDD void-resume lifecycle seam and that the existing
COMMAND session already exports an opaque ordinary parent-completion fact for
`54:0B`; neither is a remaining singleton BOP implementation.

The first remaining closure is a whole guest-native source-built parent/child/
return fixture. It must prove original guest execution after `$Exec`'s `retf`,
termination, parent restoration and the bound COMMAND outcome without a host
child substitute. The [whole-path reconciliation record](../../artifacts/documentation-archive/20260910/etc/research/t225-s44-guest-exec-transfer-parent-return-current-route-reconciliation-001.md)
contains the source/current route, recovery ladder and evidence limits.


## m0-t225-s45-closure-20260817.md

# M0 T225 S45 Closure — Source-Built Guest Parent/Child/Return Fixture Asset

S45 selects the existing source-built OpenNT SHARE.EXE as the immutable
ordinary-termination child: its original source invokes `INT 21h/AH=4Ch` with
AL zero. It rejects QUIT.COM because that uses top-level `BOP_UNSIMULATE` and
would not prove a child return.

The fixture must use a newly staged four-image primary source-built input root;
it may not reuse S10's purpose-limited staging manifest. The complete source,
provenance, ABI, Direct/Readonly and failure map is retained in
[the S45 record](../../artifacts/documentation-archive/20260910/etc/research/t225-s45-source-built-guest-parent-child-return-fixture-asset-abi-map-001.md).
No guest image, runtime behavior or native trace changed.


## m0-t225-s46-closure-20260817.md

# M0 T225 S46 Closure — Source-Built Guest EXEC Lifecycle Fixture

S46 creates a fresh, purpose-limited four-image fixture from the original
source-built NTIO, NTDOS, COMMAND and SHARE stages.  SHARE remains an
identity-only `TARGET.EXE` alias; no guest byte was created or altered.

It adds a default-off, selector-routing-neutral copied observation of the
existing `50:36 -> 50:3C -> 54:0B` guest lifecycle.  The focused regression
proves disabled, ordered, out-of-order and rejected-outcome behavior.  A
fresh formal MSVC x64 `/MT` Ninja build links the CLI and fixture; the
manifest's pre-existing S6 static-library closure was corrected to include
its existing `opennt-host` provider dependency.

One bounded native Direct/Readonly pair on the same source-built inputs
produced identical logs and the same existing budget terminal.  Each observed only one `50:36`; the copied `DS:SI=0c41:0100` is COM-style and cannot be attributed to the selected MZ SHARE target. Neither observed child termination nor COMMAND parent return. Thus S46 does not claim that SHARE executes, terminates, or returns,
and it does not admit a trace-selected repair.  The exact result and the
owner-qualified limitation are in [the S46 evidence record](../../artifacts/documentation-archive/20260910/etc/evidence/t225-s46-source-built-guest-exec-lifecycle-result-001.md).

S47 is limited to a complete source/ABI/failure reconciliation of the
post-`50:36` guest execution-continuity component.  It must classify original
guest, bx-core and bx-mantle owners before any implementation is proposed.

## m0-t225-s47-closure-20260817.md

# M0 T225 S47 Closure — Post-Entry Guest Execution Continuity Reconciliation

S47 maps the complete original `$Exec` pre-entry, child transfer, child
termination, parent restoration and COMMAND return lifecycle against the live
selector-blind core/mantle and typed bx-vdm seams. It proves that the current
`50:36` provider preserves only the original no-VDD void-resume contract and
has no authority over the subsequent child stack, segment, far-transfer,
termination, or COMMAND-return state.

It also corrects S46's attribution: the only observed `50:36` has
`DS:SI=0c41:0100`, a COM-style entry, while selected SHARE is MZ with a distinct
header CS:IP geometry. The record is compatible with initial COMMAND entry;
it is not proof that SHARE started. The [S47 map](../../artifacts/documentation-archive/20260910/etc/research/t225-s47-post-entry-guest-execution-continuity-source-abi-failure-map-001.md)
records the original source and immutable artifact evidence.

S48 is admitted only to implement a fixed-capacity, default-off, copied
whole-lifecycle observation ledger. It may record repeated existing
`50:36`, `50:3C` and `54:0B` accepted events, but may not parse guest memory,
name a target from a selector, change a route/outcome/CPU state, or repair any
observed result.

## m0-t225-s48-closure-20260817.md

# M0 T225 S48 Closure — Source-Built Guest Whole-Lifecycle Ledger

S48 adds a default-off, fixed-capacity copied lifecycle ledger in bx-vdm.
It records only already accepted/resumed 50:36, 50:3C, and 54:0B events in
their raw arrival order. It neither changes a route/outcome/CPU state nor
reads guest memory or infers a target.

The formal MSVC x64 /MT Ninja graph and all declared targets built in the
fresh lifecycle-r3 root. During that verification, the previously declared S20
COMMAND family fixture exposed an incomplete static-library list; its
manifest-only closure now declares its existing bx-mantle, bx-core, and
opennt-host owners after bx-vdm. No service or machine behavior changed.

The focused ledger fixture validates default-off, repeated ordered entry /
termination / parent-return records, non-resume rejection, and bounded
overflow. The fresh source-built Direct/Readonly pair is byte-identical and
records one raw entry only. As required by S47, that entry is not attributed
to selected SHARE and does not claim guest child execution.

The detailed result is
[the S48 evidence record](../../artifacts/documentation-archive/20260910/etc/evidence/t225-s48-source-built-guest-exec-lifecycle-ledger-result-001.md).

## m0-t225-s49-closure-20260817.md

# M0 T225 S49 Closure — Post-entry real-mode mechanical continuity audit

S49 reconciles the complete original NTDOS `$Exec` transfer after `50:36`
with the current generic bx-core mechanics and the bx-mantle/bx-vdm boundary.
It proves that NTDOS—not the adapter—owns `mov SS`, `mov SP`, the two-word
far-return frame, the PDB segment setup and `retf`; bx-core owns the generic
16-bit segment, stack, far-transfer and IP-width mechanics; and bx-mantle
owns only machine lifecycle and ordinary backing memory.

No source/current evidence identifies a BOP, provider, device or new-Bochs
intrusion defect. The retained limitation is narrower: no existing evidence
executes the complete source-shaped transfer on the immutable native guest
state as one combined proof. S50 therefore owns only a generic mechanical
fixture and cannot add BOP semantics, guest inspection, synthetic child
execution or a trace-selected repair.

The detailed map is [the S49 continuity record](../../artifacts/documentation-archive/20260910/etc/research/t225-s49-post-entry-realmode-mechanical-continuity-map-001.md).


## m0-t225-s51-closure-20260817.md

# M0 T225 S51 Closure — Paired post-handoff integration observation

S51 re-staged all four immutable original-toolchain guest inputs and ran the
existing default-off lifecycle ledger once in Direct and once in Readonly.
Both logs are byte-identical and retain exactly one accepted `50:36` record
before the established budget terminal. S50 had already proved the source-
shaped generic real-mode transfer; therefore S51 admits no BOP, provider,
device or Bochs repair. The next evidence must reconcile the current generic
limit diagnostic with source and historical owner records, rather than infer
VGA, PIC, FPU or adapter work from a log string.

Detailed result: [S51 observation record](../../artifacts/documentation-archive/20260910/etc/evidence/t225-s51-post-handoff-integration-observation-result-001.md).


## m0-t225-s53-closure-20260817.md

# M0 T225 S53 closure — first-fault observation

S53 reused the existing default-off, selector-blind first-fault observer and
ran the declared source-built Direct/Readonly pair. Both profiles recorded
the same first delivered exception: `BOUND_GdMa` / vector 5 at `0C41:1F97`.
The option intentionally turns that first delivery into a controlled
diagnostic stop; ordinary bounded runs continue instead to their
instruction-budget terminal.

Consequently this is neither a normal guest fault terminal nor evidence for a
Bochs, BOP, provider, firmware, or guest-layout repair. Its full procedure,
inputs and limitation are retained in
`docs/etc/evidence/t225-s53-first-fault-bound-observation-result-001.md` and
the corrective interpretation is committed in `e5bad8d8`.

No runtime source changed. S53 is closed and transfers no trace-selected
repair. T225 continues only through a complete owner package for the guest
startup/EXEC/parent-return lifecycle.

## m0-t225-s55-closure-20260817.md

# M0 T225 S55 Closure — startup, COMMAND initialization and EXEC stage ledger

## Closed scope

S55 reconciled the complete available source/current route from NTIO startup
through NTDOS COM loading, initial COMMAND initialization, the recognized
startup-machine interruption, and the unobserved child/parent-return suffix.
It did not implement a BOP, DOS-kernel, adapter EXEC, or CPU compatibility
change.

## Delivered evidence

- The bootstrap-provider reconciliation proves no missing startup BOP,
  configuration or device-header provider.
- The declared COMMAND image fixture proves normal DEM open/read/checked-RAM
  write/close preserves the complete source-built 50,384-byte image in Direct
  and Readonly.
- The stage ledger distinguishes the COMMAND INIT message bytes from an
  instruction attribution and preserves the unobserved child/return suffix.
- The SoftPC/Bochs source comparison identifies one separately admissible
  mechanical candidate: the historical CCPU default does not raise a real-mode
  or V86 segment-limit fault, whereas current Bochs rejects the observed
  real-mode DS:FFFF word access.

## Transfer

S56 owns a complete SoftPC-compatibility boundary map before any CPU source
change. It must classify every relevant access width/direction/mode and prove
that a default-off, selector-blind compatibility gate can retain native Bochs
behavior when disabled. The observation does not authorize a special address,
COMMAND, BOP, adapter, DOS or host-service repair.

## m0-t225-s56-closure-20260817.md

# M0 T225 S56 Closure — grouped SoftPC real/V86 segment-limit compatibility

## Closed scope

S56 implemented the complete admitted default-off real/V86 segment-limit
compatibility boundary in `bx-core` and `bx-mantle`.  It was one CPU/mechanics
package, not a BOP, DOS, COMMAND, adapter, device, or host-capability change.

## Delivered evidence

- OpenNT SoftPC CCPU's default real/V86 rule was retained as the source
  reference; native protected-mode behavior remains gated off.
- One formal CPU5/P-MMX MSVC x64 `/MT` fixture covers scalar access,
  RMW/string, stack, and fetch/control groups, including default-off #GP
  negatives and enabled cross-boundary continuation.
- Existing S30 default-off access observation remains passing.
- The required paired source-built Direct/Readonly native run installs the
  complete current composition, reaches the finite budget, and is
  byte-identical in both modes.  Its later BOUND diagnostic is observation
  only.

## Transfer

S57 owns a complete current BOP owner/package and profile-disposition
reconciliation.  It must use the existing OpenNT selector/service inventory,
actual bound routes and Direct/Readonly/Overlay disposition to select whole
owner packages.  It must not create a service patch from the S56 trace marker
or alter Bochs CPU behavior.

## m0-t225-s57-closure-20260817.md

# M0 T225 S57 closure — 2026-08-17

S57 closes the whole BOP owner/package and profile-disposition reconciliation.
The reproducible ledger covers all 203 retained OpenNT endpoints with one
owner, present route or explicit absence, source-recovery rung, Direct/
Readonly/Overlay disposition and workaround action.  It does not claim that
any mapped, resumed or stopped endpoint is a complete historical provider.

The next admitted package is the whole DEM process-resource ownership and
cleanup contract.  It starts from original PDB/JFT/SFT ownership and `50:3C`
termination order, not from trace order or the current BOUND observation.

