# M0 T231 Closure — OpenNT COMMAND owner-package completion

## Outcome

M0 T231 closes on 2026-08-20 as the COMMAND `54:00..10` code-complete and
v2 product-route package.  It does not claim that a real guest has reached a
COMMAND BOP or has continuous execution.

## Requirement audit

| Requirement | Closure evidence |
| --- | --- |
| Original owner/package structure | The eight OpenNT COMMAND mirror translation units and original 17-entry `cmddisp.c` table are active under `src/bx-vdm/bop/opennt/command/`; see [S10 route admission](../etc/evidence/t231-s10-command-v2-route-admission-001.md). |
| All 17 callable COMMAND services | Eleven focused fixtures exercise the complete owner package through the original dispatcher bodies, including bootstrap, console/keyboard, configuration, environment, launch, lifecycle and exit. |
| v2-only product route | The bounded `54:00..10` route uses the v2 session/checked-RAM bridge.  COMMAND-v1 implementation, CMDINFO codec, composition and formal fixture routes are physically removed. |
| Formal modern source closure | `formal-r11` completed 316 MSVC x64 `/MT` Ninja actions and subsequently reported `no work to do`. |
| Shared DEM/COMMAND link closure | The same formal graph links both direct owner packages; all eleven COMMAND and ten DEM focused fixtures returned zero. |

## Delivered boundary

- `bx-vdm` owns bounded BOP recognition and routes only to original OpenNT
  COMMAND owner bodies plus narrowly documented host/ABI seams.
- `bx-mantle` remains selector- and COMMAND-blind.
- Public modern Win32/NTDLL capabilities remain behind the original owner
  logic; no v1 provider is restored as a fallback.

## Explicit transfer and limits

The bounded native execution still reaches an earlier `12:00` / BIOS-machine
path before any `54:xx` COMMAND observation.  That condition belongs to the
queued **bx machine, BIOS and selector owner-package completion** package.
It is not a missing COMMAND service or an ABI/link-closure defect.  T231
therefore makes no claim of native COMMAND reachability, child-process guest
continuity, or unrestricted multi-program execution.

## Verification

- Formal Ninja `build/M0-T231-S10/formal-r11`: 316 actions, then no work.
- T231 focused fixtures: all eleven zero exit.
- T230 focused DEM fixtures in the same current graph: all ten zero exit.
- `git diff --check` passed for the committed closure revision `1487bd59`.

Unrelated S58 research worktree material was excluded from this closure.
