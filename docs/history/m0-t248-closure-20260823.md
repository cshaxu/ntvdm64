# M0 T248 Closure — selector-blind post-EXEC real-mode continuity

## Closed scope

T248 determines whether the post-`50:36` continuity frontier is a missing
selector-blind CPU/interrupt/return mechanic or another owner. It does not
claim an ordinary DOS child return.

## Result

- S1 maps `50:36`, `50:1E`, original guest EXEC/return ownership and the
  SoftPC `02/06` machine paths without assigning DOS or BOP semantics to
  bx-core/bx-mantle.
- S2 proves the existing passive software-interrupt/IRET observation has no
  missing interrupt-return event on this route.
- S3 repairs only two independently found mantle lifecycle/negative-contract
  issues and supplies a selector-blind finite terminal-position record.
- S4 proves the final transfer is an ordinary guest far indirect call from a
  runtime-modified address inside the initial NTIO aperture:
  `0000:0A87 -> 5859:5F04`. The target is zero-filled ordinary RAM and then
  reaches the declared execution budget. The initial source image byte at
  `0A87` is different, so neither Bochs nor a BOP provider owns that mutation.

## Explicit transfer

`BOP-DEPENDENCY-115` remains partial for original `$Exit/$Abort` parent return.
The next required owner is a source-built guest low-memory runtime
mutation/indirect-transfer provenance package. It must first audit an
appropriate selector-blind physical-write observation seam; it may not patch
`50:1E`, synthesize PSP/PDB state, or add machine/firmware behavior based on
this trace.

Evidence: [S1 owner map](../etc/evidence/m0-t248-s1-post-exec-realmode-continuity-owner-map-001.md),
[S2 interrupt replay](../etc/evidence/m0-t248-s2-post-exec-interrupt-replay-001.md),
[S3 recovery](../etc/evidence/m0-t248-s3-terminal-position-lifecycle-recovery-001.md),
and [S4 attribution](../etc/evidence/m0-t248-s4-post-exec-final-position-attribution-001.md).
