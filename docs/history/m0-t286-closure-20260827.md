# M0 T286 closure — Bochs-core diff and overlay minimization

T286 is closed after six sequential subtasks:

1. complete divergence/overlay ledger;
2. product-shell lifecycle privatisation;
3. device lifecycle disposition;
4. retirement of unconsumed observers;
5. private RAM boundary disposition and fixture repair;
6. final metric reconciliation and focused x86/x64 Ninja validation.

The final retained surface is 27 non-identical upstream-relative Bochs mirror
files (393 additions / 356 deletions) and ten private overlay source/header
bodies (1,067 lines).  No `bochs-core` production source is project-authored;
all retained changes are source-root bindings, true-subset crops or minimal
call/declaration seams to registered private overlay mechanics.

The focused `/MT` x86 and x64 Ninja graphs each compile the selected core,
private overlay and `adapter-bochs` into static libraries, link the typed
memory fixture and execute it successfully.  This proves only the minimized
mechanical Bochs boundary; it does not claim an MVDM host closure, an enabled
BOP route or continuous guest execution.

The obsolete broad T260 product manifest remains historical evidence and is
not used by this closure.  The next queue package is the original XMS owner
package, which must use its owner/source/interface trackers rather than this
mechanical fixture as implementation authority.

Supporting evidence: [S1 ledger](../etc/operations/m0-t286-s1-bochs-core-divergence-disposition-ledger-001.md), [S2](../etc/evidence/m0-t286-s2-product-shell-overlay-001.md), [S3](../etc/evidence/m0-t286-s3-device-lifecycle-review-001.md), [S4](../etc/evidence/m0-t286-s4-observer-retirement-001.md), [S5](../etc/evidence/m0-t286-s5-memory-boundary-review-001.md), and [S6](../etc/evidence/m0-t286-s6-final-minimization-validation-001.md).
