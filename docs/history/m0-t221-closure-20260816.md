# M0 T221 Closure: DEM Direct/Readonly whole-provider

## Scope and result

T221 recovered the bounded DEM file/handle/FCB/search provider package for the
Direct and Readonly host-mutation profiles.  Its scope was the selected 29
identities: namespace/path, handle I/O, pathname/FCB search, FCB file state
and the explicit pipe boundary.  The result is one `dem_whole_provider` view
owner with typed, generation-checked tokens and checked guest transfers;
Direct uses selected ordinary host roots and Readonly admits reads while
refusing mutations.

## Requirement-to-evidence audit

| Requirement | Evidence | Result |
| --- | --- | --- |
| Source-first owner, ABI and failure analysis | [S1 recovery ledger](../etc/research/t221-s1-dem-recovery-ledger-001.md), [S2 disposition](../etc/research/t221-s2-file-provider-disposition-001.md) | Historical DEM owners, unavailable CCPU/SAS composition dependencies and source-derived seams are recorded. |
| One provider view and typed opaque handles | [P1--P3](../etc/research/t221-s3-file-view-boundary-001.md), [P2 token record](../etc/research/t221-s3-typed-direct-token-001.md) | No raw host handle or pointer enters guest ABI; token kind and generation are checked. |
| Direct/Readonly namespace, FCB and search family | P57--P64 Status records and the [29-identity audit](../etc/research/t221-s3-direct-readonly-29-identity-closure-audit-001.md) | Direct uses a self-owned temporary host root; Readonly mutation paths refuse before a host mutation. |
| Handle-family profile contract | [P66 stale-token](../etc/research/t221-s3-direct-readonly-stale-token-001.md), [P67 handle matrix](../etc/research/t221-s3-direct-readonly-handle-family-matrix-001.md) | Valid Direct/Readonly operations and `CF/AX=5`/`6` terminals are covered. |
| Full selected-package regression | [P68 closure audit](../etc/research/t221-s3-direct-readonly-29-identity-closure-audit-001.md) | Focused MSVC x64 `/MT` normal package-session fixture passes. |

## Accepted deliveries

The final package evidence is at commits `e3f498f9`, `eb6ddcac` and
`85ffb79b`; earlier T221 P records remain indexed in Status and `docs/etc/`.
All accepted commits were pushed to `origin/main`.

## Verification

The final focused fixture command was:

```powershell
powershell.exe -ExecutionPolicy Bypass -File tools/probe/Invoke-T198S16BootNamespaceVdmX64Probe.ps1 `
  -RepositoryRoot O:\repos.hobby\ntdos64 `
  -BuildRoot O:\repos.hobby\ntdos64\build\M0-T221-S3\171-direct-readonly-handle-matrix `
  -Fixture dem-whole-provider-session -HostArchitecture x64
```

It built with MSVC x64 `/MT` and returned exit `0`; the disposable build root
was then removed.  Documentation governance verification and `git diff --check`
passed for the final evidence delivery.

## Retained limitations and transfer

Overlay and Virtual implementations/evidence remain retained but frozen;
`TODO.md` controls their later re-admission.  Pipe `50:47/48` stays
Redirector-deferred.  T221 neither recovers raw-device/DASD, DEM drive-view
and CWD/DPB state, full session lifecycle, Redirector, DPMI, WOW nor native
execution trace behavior.

The successor **M0 T222** is admitted as the next DEM owner package: direct
host drive-view, CWD, clock, DTA, DPB and machine-identity services in Direct
and Readonly profiles.  It begins with a complete source/ABI/failure ledger;
trace remains integration evidence only.
