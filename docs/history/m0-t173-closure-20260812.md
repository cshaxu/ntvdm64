# M0 T173 closure — 2026-08-12

T173 establishes the source-derived no-next-target `54:11` ABI. Original COMMAND treats `DX` as guest DOS termination status but returns a separate historical host-process `dwExitCode32` in `AL`; the CLI profile must not invent `DX → AL`. The contained branch resumes over the BOP with CF clear, preserves AX/stack/segments and retains copied DX only as adapter lifecycle state. Historical re-entry, redirection, directory and NT event/process work remain unavailable. T174 owns the one-time paired provider implementation; no runtime or source behavior changed in T173.
