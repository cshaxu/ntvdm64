# M0 T223 Closure: COMMAND Bootstrap/Environment/Config Package

## Scope And Result

T223 recovered the selected original COMMAND bootstrap/environment/config
owner package for a contained CLI session:
`54:01/02/04/05/0C/0D/0F`. It does not claim closure of the larger COMMAND
plane.

## Requirement-To-Evidence Audit

| Requirement | Evidence | Result |
| --- | --- | --- |
| Original owner and ABI/failure map | [S1 dispatcher inventory](../etc/research/t223-s1-command-dispatcher-current-route-inventory-001.md), [S1 selection](../etc/research/t223-s1-command-abi-component-selection-001.md) | `cmddisp.c`, `cmdmisc.c`, `cmdenv.c`, `cmdconf.c`, and `cmdsvc.h` determine package ownership, copied ABI, lifecycle and exclusions. |
| Correct current-directory failure ABI | [P1 repair](../etc/research/t223-s2-command-current-directory-abi-repair-001.md) | Invalid/unavailable drive preserves `CF=1, AX=1`; no successful empty-directory fallback remains. |
| Session-broker separation | [P2 routing correction](../etc/research/t223-s2-command-session-broker-routing-001.md) | `54:10` remains a deferred session-broker package, rather than a bootstrap success claim. |
| Bounded contained bootstrap state | [P3 state map](../etc/research/t223-s2-command-bootstrap-state-machine-map-001.md) | The CLI session preserves the admissible first/retry/return contract and explicitly excludes later BASE/console/launch behavior. |
| Direct and Readonly full-package regression | [profile-session regression](../etc/research/t223-s2-command-profile-session-regression-001.md) | Each mode traverses the same bound session sequence with its selected generic policy and no cross-profile fallback. |

## Verification

The dedicated MSVC x64 `/MT` profile-session fixture compiled, linked, and
exited zero. It routes `54:02`, `54:0F` (capacity failure then success),
`54:05`, `54:01`, `54:04` (success and unavailable-drive failure), `54:0C`,
and `54:0D` under both Direct and Readonly profiles. The separate
`command-bootstrap` original-owner fixture also exits zero. Disposable roots
were removed after their concise evidence records were written. Documentation
governance verification and `git diff --check` pass for the closure delivery.

## Retained Limitations And Transfer

T223 does not close COMMAND standard handles, pipes, child execution,
console/keyboard, lifecycle, session broker, Redirector, XMS, DPMI, WOW,
Bochs/mantle, or a native trace. The next selected package is the Queue's
standard-handle and redirection capability: its first S must map the complete
original owner/ABI/failure surface before any endpoint implementation.
