# M0 T244 Closure — Headless native 8042 lifecycle

## Closed scope

T244 closes the selector-blind PC-device prerequisite selected after the T243
`BOP 5F` source recovery: a native Bochs 2.6 8042 controller with ports
`60h/64h`, controller timing, ordinary IRQ1 delivery and clean minimal-machine
ownership.

The adopted upstream controller is owned by `bx-mantle`; no BOP selector,
OpenNT, DOS, SoftPC or host-service meaning entered `bx-core` or
`bx-mantle`. Registered exception `BX-MANTLE-091` severs only product-shell
attachments: plugin entry, SIM runtime parameters, GUI LEDs, CMOS publication,
default mouse/host input and clipboard paste.

## Evidence

- [S1 keyboard/IVT machine audit](../etc/evidence/m0-t244-s1-pc-keyboard-ivt-machine-audit-001.md)
  freezes the source/ABI ownership map.
- [S2 lifecycle closure](../etc/evidence/m0-t244-s2-headless-8042-lifecycle-closure-001.md)
  records the formal MSVC x64 `/MT` Ninja witness: inactive ports, live
  controller ports, source `F2h` acknowledgment, IRQ1→IAC `09h`→EOI,
  no auxiliary-device fabrication, cleanup and two clean lifecycles.

## Explicit transfer

Host keyboard input, mouse, GUI, CMOS, paste, PIT/IRQ0, video, firmware,
monitor IRET/ICA behavior, console input and interactive guest continuity are
not claimed. They remain separate source-owner packages.
