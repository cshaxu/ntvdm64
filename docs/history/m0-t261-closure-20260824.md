# M0 T261 closure — OpenNT host original-layout recovery

## Result

M0 T261 is closed after S1--S12. `src/opennt-host` now uses re-rooted OpenNT
paths and original filenames for its admitted source surface. Project-authored
`nt_error_compat.h`, `vrputil_compat.h`, and `vrnmpipe_compat.h` no longer
exist beneath that component.

## Final S12 evidence

- Exact historical `error.h`, `vrputil.h`, `vrnmpipe.h`, and `oemuni.h` are
  imported under their original relative paths; their SHA-256 values are in
  [the S12 evidence](../etc/evidence/m0-t261-s12-opennt-host-original-header-replacement-001.md).
- Non-original dialog state is owned by `adapter-win32`, while the static
  VDMREDIR selection remains a registered source-level divergence in the
  actual host/BOP consumers.
- A fresh formal Ninja root compiled, archived, and linked 312 focused edges.
  The BOP59 and Redirector fixtures both return `0`.
- All uses of the removed compatibility headers are absent from production and
  tests. Documentation governance passes.

## Boundary

This closure does not claim recovery of unadmitted NT4 product shells such as
CSR, VDD, ICA, or multi-VDM brokers. Those are separate owner packages, not
missing header implementations. Historical files retained as reachable subsets
remain subject to their statement-local `DIVERGENCE(<ID>)` comments and the
component README register.
