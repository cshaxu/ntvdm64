/* DIVERGENCE(BOP-DIV-109): `printer_.c` is retained only as the original-name
 * linkage boundary for the admitted printer open/close fragment.  The bounded
 * headless body is private below rather than a mirror-root implementation. */
#include "opennt-bop-overlay/softpc/printer_.c"
