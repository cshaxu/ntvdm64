/* DIVERGENCE(BOP-DIV-108): `nt_mouse.c` is retained only as the original-name
 * linkage boundary for the admitted headless `host_mouse_install1` fragment.
 * Its source-derived body exceeds the mirror threshold and is private below. */
#include "opennt-bop-overlay/softpc/nt_mouse.c"
