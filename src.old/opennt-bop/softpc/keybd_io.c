/* DIVERGENCE(BOP-DIV-110): `keybd_io.c` is retained only as the original-name
 * linkage boundary for the reached `UpdateKbdInt15` fragment.  Its machine
 * composition is private below and does not create keyboard-device behavior. */
#include "opennt-bop-overlay/softpc/keybd_io.c"
