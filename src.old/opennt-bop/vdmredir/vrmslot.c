/* DIVERGENCE(BOP-DIV-091): the retained VDMREDIR mailslot implementation is
 * source-derived and exceeds the mirror threshold.  Keep its entire shared
 * record/list linkage private while preserving every original entry name and
 * one translation-unit instance for the matching OpenNT provider. */
#include "opennt-bop-overlay/vdmredir/vrmslot.c"
