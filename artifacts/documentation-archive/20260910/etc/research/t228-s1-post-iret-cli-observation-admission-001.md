# T228 S1 P24 — Bounded Post-IRET CLI Observation Admission

P23 closes a selector-blind, default-off machine profile for successful
real/V86 IRET completion.  P22 already rules out the next adapter DEM file
family.  This admission permits exactly one existing diagnostic-CLI lifecycle
integration and one hash-locked paired Direct/Readonly observation, so the
chronological pre-interrupt and post-IRET records can be compared against the
complete original COMMAND/NTDOS transient/MCB owner map.

The CLI may only configure the P23 copied mantle ring before the existing
worker, print copied records after the worker ends, and reset the ring on all
setup, worker-failure and normal completion paths.  The option must be
compiled only under `BX_NTVDM_ENABLE_MANTLE_INTERRUPT_RETURN_OBSERVATION` and
be rejected by the default CLI.  It may not pair records by vector, inspect
guest memory, decode an interrupt, add a bx-vdm route, alter an engine result,
change a host-mutation profile, modify images, or choose an owner repair.

Required evidence is: focused P23 fixture retention; diagnostic x64 `/MT`
CLI build; default CLI negative option check; two independently launched,
cleared-environment Direct/Readonly runs against the retained hash-locked
four-image input and one-million-tick budget; byte/output comparison; and
source-map interpretation limited to the already named COMMAND/NTDOS owner
domain.  A run may select at most one complete original owner package; it
must not generate a BOP, INT-function or opcode leaf task.