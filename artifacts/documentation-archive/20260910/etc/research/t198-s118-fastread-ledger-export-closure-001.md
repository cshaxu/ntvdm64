# M0 T198 S118: Ledger Export Closure

The fixture now iterates the fixed ledger and emits every copied attempt to
`run.log`, including ordinal, accepted bit, copied registers and typed result
metadata. The export source compiles in
`artifacts/build/t198-s118-ledger-export-compile-r1` under the locked CPU5/x64
configuration. No guest was executed.

S117 remains a retained but attribution-insufficient one-run record; a future
task must separately admit any replacement observation.
