# M0 T198 S134: Paired COMMAND Endpoint Native Result

S134 executed its one authorized native observation at `artifacts/build/t198-s134-paired-command-endpoints-native-r1`. The retained probe record declares `runs: 1`, the S93 exact COMMAND/SHARE input contract, and bounded observer exit code `2`.

`run.log` SHA-256 is `68B875E271490E4796CEDA28C3C5143B431375ECCFCCEEC66D2A11BC2CDA191A`.

The result stops at `54:01` / `95AB:03C3`. It does not reach `54:04`, so it does not provide the planned native integration proof for the restricted current-directory provider. It also does not refute the earlier distinct evidence that `54:0E` continued to `54:04`; the observations have different reached boundaries and cannot be combined into a component claim.

S134 has exhausted its one-run allowance. No retry, next-BOP implementation, or further endpoint patch is admitted. The pair remains partial evidence only: `54:0E` has fixed DX-zero/four-byte-resume plus one continuation witness; `54:04` has adapter-only restricted-root and direct-failure proof. COMMAND console/launch remains incomplete.
