# M0 T301 S1 P6 — Declaration-Shape Partition

Every current first-degree call now has one declaration-shape result in
`mvdm-first-degree-rebaselined-declaration-shape-ledger.tsv` before any
original body is selected:

- 473 function-declaration candidates may proceed to compatible physical-body
  resolution.
- 727 macro-only rows remain conditional-form evidence, not functions.
- 365 mixed or unclassified rows remain variant/form resolution work.
- 10,861 rows have no original declaration token and must be separately
  classified as public leaf, hard boundary, finite shallow owner, or source
  unavailable.

The partition is conservative: a call token visible both as a macro and as a
function form is not automatically assigned a function body. No source body,
second-degree callee, provider, import, or build route was selected.
