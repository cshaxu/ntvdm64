# M0 T360 S3 — original SystemRoot package-layout conformance

The focused x86 graph generated by
`tools/build/New-T310PackageLayoutNinja.ps1` completed all five actions and
ran `package-layout-fixture.exe` successfully.

Passing the fixed executable spelling
`O:\\ntvdm64\\original-softpc-process.exe` to that fixture selected
`O:\\ntvdm64` as SystemRoot. The unchanged original `cmdconf.c` value
`<root>\\system32\\COMMAND.COM /p <root>\\system32` measures 54 visible ASCII
bytes, within the guest's 63-visible-byte capacity.

A fresh non-runtime stage at
`build/M0-T360/S2/layout-stage` contains all sixteen required immutable media
assets at the root, `system32` and `softpc` destinations listed in S1. This is
only a staging inspection; it is not guest execution evidence.
