# M0 T280 S12 — original DEM object-surface observation

Compile the three original reached DEM consumers (`demsrch.c`, `demerror.c`,
and `demgset.c`) as independent x86/x64 syntax/object observations through
the closed NT I/O facade. Do not link them into a provider or change their
bodies. Classify any remaining unresolved source surface by unique adapter
owner before attempting a repair.

The only permitted output is reproducible source/build evidence and a complete
next-boundary ledger. Guest addresses, if reached by source parsing, remain
the separate adapter-softpc/session guest-memory concern.
