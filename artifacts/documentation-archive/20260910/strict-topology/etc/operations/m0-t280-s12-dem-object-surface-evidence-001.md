# M0 T280 S12 — original DEM object-surface evidence

All three reached original DEM consumers were observed with MSVC `/Zs` on x64
and x86 through the closed S11 NT I/O facade. None was linked.

The same shared blockers occur on both architectures: historical NT status,
object/device and DOS wildcard constants; reached RTL path/string/list helper
declarations; and source-shaped object-manager definitions. `demsrch.c` also
has the expected x64 warning class where its original 32-bit persisted
find-list values would narrow native pointers. That is not permission to cast:
the future binding must use the session `host_resource` mapping instance, while
only a guest DTA copy uses the separate guest-memory lease.

The complete per-source outcome and unique owner are in the adjacent ledger.
This is a failed syntax observation used as evidence, not a passing build and
not a provider enablement.
