# M0 T280 S13 — DEM shared NT/RTL surface evidence

The reached OpenNT NT status/object/device constants, DOS wildcard constants,
RTL list macros and function declarations were recovered as one
`adapter-win32` source-shaped declaration surface. Original DEM bodies were
not edited.

Using the S12 Ninja graph, all three bodies (`demsrch.c`, `demerror.c`, and
`demgset.c`) pass `/Zs` on MSVC x64 and x86. Warnings are retained evidence:
x64 exposes original pointer-to-32-bit casts in `demsrch.c`; they are not
fixed by this declaration packet. The future binding must use session
`host_resource` mapping for the persisted host identity and separate
`guest_memory` leases for guest spans.

No provider, BOP route, object link, raw guest pointer, or mapping-manager
operation is introduced by this closure.
