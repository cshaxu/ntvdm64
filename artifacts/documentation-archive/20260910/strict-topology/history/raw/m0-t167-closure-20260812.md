# M0 T167 closure — 2026-08-12

Source audit proves that T166's exit 1 comes from the historical textconfig
product shell after a generic accepted STOP returns from the CPU loop. The CLI
only propagates that status. A contained correction may be made only in the
generic Bochs STOP path, with no selector/BOP inspection; it requires a new
intrusion-register entry and a separate task. No runtime was rerun.
