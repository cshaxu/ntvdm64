# M0 T126 closure — 2026-08-12

T126 classifies the T125 `50:11` loop as a retained build-composition gap, not
an unimplemented BOP. Original `demLoadDos`, the source-derived transaction,
the host session and the linked object closure all agree on the `DI:0000` and
RIP+4 contract. T121's only relevant exclusion is its explicit
`BX_NTVDM_ENABLE_CPU_RESULT_BRIDGE=0` compile flag. T127 owns a strictly
selector-blind bridge-enabled derivative; no BOP semantic change is approved.
