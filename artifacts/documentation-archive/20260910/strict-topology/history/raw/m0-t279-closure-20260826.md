# M0 T279 closure — original MVDM support libraries

## Completed result

T279 recovers the bounded, low-dependency original support-library closure:

- the three original selected translation units remain byte-exact;
- `adapter-win32` supplies only the reached historical RTL/PEB/TEB source
  surface with public Win32 implementations and private native pointers;
- x86 builds original `oemuni.lib` and `suballoc.lib` bodies with MSVC `/MT`;
- x64 builds the same libraries, with exactly four source-derived OEM exports
  supplied by `opennt-mvdm-support-overlay` to avoid pointer narrowing; and
- the x64 local wrapper fixture passes.

## Package boundary

All 112 exact `opennt-mvdm-support` paths remain in the source mirror. The
original `dirs`, `makefil0`, `inc` and `sources` forms are build/declaration
evidence; the T279 Ninja generator is the modern formal executor and selects
only the original `oemuni` and `suballoc` library bodies. `toemuni.c`, `tsa.c`,
`debug.c`, and `vdmutils` remain tool/test or separate-tool inputs.

`suballoc` is static-only: its three original callbacks are intentionally not
bound here. A later owner can bind them only through `adapter-softpc` to the
unique session guest-memory mapping-manager instance.

No BOP route, MVDM host provider, guest load, mapping operation or runtime
machine capability is enabled by this closure.

## Verification

- T278 exact physical mirror verification: pass (1,689 frozen paths);
- T279/S1 interface ledger: pass (71 reached interface rows);
- T279/S2 binding verification: pass (three source hashes, four x64 overlay
  requirements recorded);
- T279/S3 artifact verification: pass (x86/x64 static libraries and x64
  fixture); and
- documentation governance and `git diff --check`: pass.

The next queue package is original host control plane and DEM; its admission
must use the tracker and this support-library ABI as input, not treat T279 as
an enabled DEM/BOP provider.
