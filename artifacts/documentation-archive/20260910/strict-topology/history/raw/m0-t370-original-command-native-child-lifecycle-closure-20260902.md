# M0 T370 closure — Original COMMAND native-child lifecycle

T370 closes the bounded original COMMAND local native-child cohort.

- S1 re-established the original `54:08`, `54:0A` and `54:0B` topology:
  `cmdExec`/`cmdExecComspec32` enter `cmdExec32`, its original detached worker
  owns source process/wait/re-entry order, and `54:0B` remains the separate
  DOS child-return route.
- S2 retained that flow while replacing only the unsafe cross-worker guest
  aliases with a session-owned bounded snapshot and restoring the historical
  BaseSrv hold through the thread-create/re-entry interval.
- S3 rebuilt the focused x86 fixture and the formal CPU40/x86 final product.
  The final product query proves the original COMMAND archive and its selected
  SoftPC/Base VDM/session bindings are all final-link inputs.
- S4 made exactly one frozen `LOADFIX.COM` observation.  It timed out at an
  earlier original COMMAND/Base VDM `54:01` continuation before any native
  child service was reached.

T370 is consequently code-, focused-proof- and formal-link-complete for its
local source owner.  It does not claim a native child, DOS EXEC/PSP return,
WOW, Redirector or CSR/CSRSS broker implementation.  The earlier runtime
terminal is retained for the cross-family execution/control-broker candidate;
the ordered queue otherwise continues with XMS/DPMI/DOS-extender execution.
