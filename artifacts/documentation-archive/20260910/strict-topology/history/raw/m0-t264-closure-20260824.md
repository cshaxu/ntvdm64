# M0 T264 Closure — Project Code Filename Identity Normalization

T264 is closed on 2026-08-24.

The task moved 42 `app`/`session` paths, 77 project-owned adapter paths, and
70 standalone `_v1`/`_v2` paths with `git mv`. It also removed non-semantic
private product/backend/source-provenance identifiers while retaining names
that describe guest semantics, real Bochs mechanics, OpenNT compatibility
contracts, external configuration, or imported-source identity.

The final review found one rename-induced build regression: the generic
`src/app/config.h` shadowed Bochs's generated `<config.h>`, causing a Windows
SDK `FLOAT128` collision in Bochs SoftFloat. It was repaired without changing
Bochs by renaming the app-private header to `bundle_config.h` and updating its
four direct consumers.

Verification used the external MSVC x64 `/MT` Ninja graph at
`build/M0-T264-S5/r001`: all remaining compile/archive/link edges completed,
81 executable targets were produced, and final `ninja -n` returned `no work to
do`. Documentation governance and `git diff --check` passed. Detailed
inventory and correction evidence is retained in
`docs/etc/evidence/m0-t264-s1-project-code-filename-inventory-001.md`.
