# M0 T165 closure — 2026-08-12

T165 added a CLI-only retained-terminal observer. Its real full-CLI fake
fixture (`ntdos64-run -> engine -> fake Bochs`) preserved immutable input
identity and both streams, propagated exit 47, and refused evidence-directory
reuse. Two earlier fixture mistakes were retained and classified: direct engine
launch omits runner-owned adapter environment, and the v3 profile name is a
fixed identity rather than an arbitrary label. No native Bochs, guest, adapter,
BOP, or runtime source changed. T166 is separately admitted for one native
observation.
