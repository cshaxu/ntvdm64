# M0 T278 closure: MVDM runtime/support/tool/firmware physical re-root

## Result

The former `opennt-mvdm-host` temporary union is now physically split into
the approved mirror components by Git renames only:

- `opennt-mvdm-host`: 1,491 retained host-carrier paths;
- `opennt-mvdm-support`: 112 exact `dirs`, `makefil0`, `inc`, `oemuni` and
  `suballoc` paths;
- `opennt-mvdm-tools`: 28 exact `vdmutils` paths; and
- `opennt-mvdm-firmware`: 58 exact `softpc.new` BIOS/ROM/data paths.

All 1,689 frozen SHA-256 identities pass. No original source body, README
mirror exception, adapter ABI, build edge, provider or guest behavior changed.

## Verification

`Verify-T278S2PhysicalMirrorMove.ps1` validates every path and hash.
`Verify-T278S3ComponentIsolation.ps1` proves the component file sets match
the ledger and scans 447 active project configuration files for stale moved
path references; the result is zero.

## Handoff

The next dependency-ordered candidate is **Original MVDM support libraries**.
It may recover `dirs`/`makefil0` build control and the `inc`, `oemuni` and
`suballoc` support closure, but it must not assume the physical move enabled
any provider or modern build edge.
