# M0 T100 closure — 2026-08-12

T100 composed the entire eight-slot OpenNT DEM `demNotYetImplemented` family
through the common BOP ingress, provider registry and DEM plane. It did not
create a `50:42` special case. Each selected member resumes after the
four-byte BOP and clears CF, exactly matching the original no-op contract;
normal DEMREAD remains outside the provider.

Focused source tests and a narrow MSVC relink pass with unchanged retained
Bochs inputs. The single bounded guest trace reaches `50:42`, resumes from
`6351` to `6355` with CF clear, reaches normal close (`50:02`), and only then
ends at the known Bochs physical prefetch-map veto. The full evidence is
`etc/research/t100-s1-original-unavailable-dem-plane-runtime-001.md`.

T101 takes only the next read-only source/trace mechanics audit. It is not
authorized to repair or enable a Bochs machine feature without a separate
admission.
