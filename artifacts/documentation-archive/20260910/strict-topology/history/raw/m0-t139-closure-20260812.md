# M0 T139 closure — 2026-08-12

T139 preserves T130 as the only comparable post-handoff runtime baseline and
excludes the whole-`cpu.cc` direct override from control-flow comparison.
Future CPU observation requires a new reproducibility admission; no workaround
is selected.

T140 is admitted to correlate the existing `0BC1:FFFF` physical address with
the NTDOS image and original OpenNT source/link layout, without a runtime run.
