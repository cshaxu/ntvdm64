# M0 T116 closure — 2026-08-12

T116 closes the static machine-composition boundary for reached BOP `02h`.
The original unchanged `unexpected_int` object, its exact three compatibility
imports, and a narrowly scoped native mechanics context are proven in one x86
link projection. A focused default-disabled contract test proves declines for
host-service/unavailable/non-`02h` requests and retains the original PIC mask
rule.

No imported Bochs/OpenNT source changed; the handler was not called; no #UD,
device, guest, or continuous-execution result is claimed.

T117 is admitted to register and implement the selector-blind, default-off
Bochs-to-machine-composition seam. It must retain the three-operation context,
leave declined exceptions unchanged, and not give the adapter access to PIC or
RAM mechanics.
