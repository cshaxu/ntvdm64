# M0 T193 closure — reached machine-selector composition audit

T193 closes the complete source/provenance audit of non-host-family selectors
reached by the valid T190 command-bootstrap observation.

- `06` is the single source-proven original SoftPC machine handler island;
  it remains a narrow, Bochs-mechanics composition and never an adapter
  service.
- The frozen T190 engine already links `unexpected_int`, `illegal_op_int`,
  and the selector-blind machine seam under
  `BX_NTVDM_ENABLE_MACHINE_COMPOSITION=1`.
- `12/15/17` remain native/firmware candidates; `5E/5F` remain collisions;
  `66/C8` are unavailable/deferred; and `FE` is engine control.

No new selector handler, BIOS table, device, provider, Bochs patch, or
runtime claim is admitted.  The follow-on work is a single whole
execution-continuation package, not another BOP-selector patch series.
