# M0 T140 closure — 2026-08-12

T140 proves `0BC1:FFFF` is physical `0x1BC0F`, outside the source-built NTDOS
image loaded at `[0x8B00,0xF7D2)`. It is not an NTDOS byte or symbol and does
not admit a guest image patch.

The preceding `50:36` is original `SVC_DEMENTRYDOSAPP`; T141 is admitted to
audit that original host lifecycle contract before any behavior change.
