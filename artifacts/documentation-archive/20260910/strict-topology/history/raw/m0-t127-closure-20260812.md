# M0 T127 closure — 2026-08-12

T127 copied the verified T121 generated root and changed exactly one retained
makefile token: `BX_NTVDM_ENABLE_CPU_RESULT_BRIDGE=0` to `=1`. Current source
hashes for `exception.cc` and adapter runtime matched the T121 manifest before
the build. MSVC x86 rebuilt only `cpu/exception.o` and linked the declared
target; no archive rebuilt. The new executable hash is
`A534A36D26C84056B3F1DF1C8D9E4481CDB174D583D7281014D767B016F3D44E`.

The map retains the existing host-session pending-write and runtime v4 symbols.
This is build evidence only. T128 owns a separately bounded shim execution.
