# M0 T132 closure — 2026-08-12

T132 establishes that the sole original `unexpected_int` island is
research-only-with-risk: its clang-cl object requests `msvcrt.lib`, while the
Bochs and mechanics objects request `LIBCMT`, and T130 retains `LNK4098`.
The final PE's lack of an `msvcrt.dll` import does not prove static-library CRT
coherence. No new original handler island may inherit this recipe.

T133 is admitted to audit the preceding firmware/INT10 reset condition, which
is independent of the BOP handler and adapter host-service planes.
