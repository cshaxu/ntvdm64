# M0 T158 closure — 2026-08-12

T158 replaces only the existing Bochs exception object to enable registered
generic real-mode vector logging.  One bounded log proves the post-FASTREAD
path is original INT29 then INT10 pseudo-ROM, followed by execution from
`0000:0000` and the known INT1/stack-fault sequence.

The culprit is the retained direct-before-POST execution-plan composition,
which leaves the saved machine INT10 vector zero.  It is not a BOP, adapter or
Bochs stack-mechanics error.  T159 must audit the pre-existing native POST +
deferred-handoff configuration against current adapter objects before any
runtime change.
