# M0 T152 closure — 2026-08-12

T152 closes the original fast-read execution-path audit. The T149 sequence
conforms exactly to original x86 control flow: i386 clears `MIPS_BIT_MASK`,
`FastOrSlow` selects `50:42`, and the visible original DEM dispatcher returns
CF clear without an I/O operation. The normal `50:16` path is consequently
not selected.

This provides the first source/trace explanation for the out-of-image command
execution: the current source-correct unavailable result cannot populate the
COM load buffer. It is a missing historical x86 fast-I/O composition, not a
Bochs, adapter-routing, BOP02, PIC or guest-state-flag defect. Any remediation
must be a holistic source-derived provider contract, not a DEMREAD alias or
MIPS-bit workaround.
