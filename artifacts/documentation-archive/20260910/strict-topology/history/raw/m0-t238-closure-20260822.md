# M0 T238 Closure — No-external OpenNT BOP slice verification

## Outcome

T238 closes the queue-head no-external-dependency package as a validation
closure, not as 105 new implementations. The frozen inventory remains 75 BOP
rows plus 30 OpenNT dependency rows. Their declared local source/ABI/lifecycle
contracts now have a fresh no-v1/no-bypass formal verification result.

## Evidence

- Formal r3 rebuilt all 353 MSVC x64 `/MT`, CPU5/P-MMX graph edges.
- The frozen owner-group regression sweep passed **31/31** fixtures.
- The audit found and repaired an existing BOP 59 public-dialog load defect:
  Common Controls v6 activation is scoped to the source-derived
  `host_direct_access_error` seam, preserving Terminate/Ignore without a
  private NT4 UI shell.
- Two apparent COMMAND failures were stale test interpretations, not a reason
  to replace imported OpenNT: one scanned a multisz as a single string; the
  other expected a synchronous result rather than the imported detached
  `cmdExec32` pending/re-entry order.

The full procedure, intermediate r1/r2 failure evidence, source disposition
and final result are in the [T238 audit record](../etc/operations/m0-t238-s1-no-external-no-bypass-audit-001.md).

## Explicit transfers

T238 does not close any branch needing selector-blind bx machine/BIOS
mechanics, legacy/private NT4 composition, Redirector IPC, WOW16, VDD/debugger
or DOS EXEC/PSP parent-return. Those remain in the first unadmitted external
compatibility candidate, whose admission is deliberately left to the owner.
