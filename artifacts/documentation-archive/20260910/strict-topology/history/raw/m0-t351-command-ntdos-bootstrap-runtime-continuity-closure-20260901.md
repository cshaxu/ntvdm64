# M0 T351 — COMMAND/NTDOS bootstrap runtime continuity closure

## Delivered boundary

T351 closed the complete source-defined scalar lifecycle reached by original
COMMAND `SVC_CMDSETINFO`: `MS_bop_4`, `CmdDispatch(05)`, `cmdSetInfo`, the
numeric scalar-location/short-lease compatibility binding, later selected
COMMAND/DEM/SoftPC scalar consumers, and the original CPU40/SAS owner route.
The selected original source bodies remain in their mirrors.  The only
compatibility replacement is the already registered mapping-manager-based
short lease; no raw pointer global, extra BOP provider, guest mutation, or
alternate executor was introduced.

## Evidence

- S1 established the complete original owner and consumer ledger.
- S2 passed the current scalar lifecycle fixture and a fresh 425-action
  CPU40/x86 formal product link.
- S3 re-ran the current fixture from exact sources and recorded the stale
  historical register-fixture failure without recreating its retired ABI.
- S4 made exactly one immutable non-debug fixed-container observation.  It
  reached `50:11`, `50:3B`, `50:0F`, `50:1B`, and `54:05`, then the fixed
  eight-second watchdog ended the child with `0x53504354`.

## Explicit limitation

The observation does not establish `cmdSetInfo` return, post-BOP CPU40
execution, NTDOS `msinit.asm` stack transition, COMMAND execution, or a DOS
child/parent lifecycle.  It is not a failure attributed to any particular
BOP or graphics component.  A later, separately admitted source-owner
continuation package must first attribute the post-`54:05` boundary; graphics
integration cannot treat this timeout as authorization for controller repair.
