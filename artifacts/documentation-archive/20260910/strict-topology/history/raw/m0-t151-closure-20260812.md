# M0 T151 closure — 2026-08-12

T151 closes the static command-image tail provenance audit. The exact
source-built COMMAND file is the staged T149 input, but it covers only
`0BC1:0100..C5CF`. `0BC1:FFFF` is 0x3A2F bytes beyond its exclusive end, so
there is no file byte or COMMAND source label to recover there.

A co-located map sidecar is now retained and corroborates the same end range,
but it is not required for the decisive raw-image calculation and cannot map
an out-of-image address. No source/runtime/BOP/device/provider change follows.
T152 is limited to a source/trace audit of the reached fast-read contract.
