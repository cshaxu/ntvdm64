# M0 T155 closure — 2026-08-12

T155 rebuilt only the current adapter/CLI objects in the x86 Bochs derivative
and proved the T154 `50:42` provider on the real guest path.  The bounded log
shows a full `0xc4d0`-byte COMMAND.COM transfer and a later successful EOF
read; `50:43` is not selected.

The guest then reaches a separate real-mode stack-prefetch/triple-fault stop.
The observer's completion JSON is absent because watchdog child cleanup was
denied, but the preserved log and no-residual-process check remain valid
bounded reachability evidence.  No continuous-execution claim is made.

T156 is a source/state audit of that reached stack/exception stop.  It must
not modify Bochs, guest, devices, firmware or the adapter until the fault
predecessor and ownership are proven.
