# M0 T217 Closure — source-first COMMAND prerequisite and loader family

T217 completed the bounded continuation work after declared guest image
visibility without treating a terminal trace BOP as an implementation order.

- Recovered the OpenNT `cmdenv.c::cmdGetInitEnvironment` contract through the
  smallest x64 CLI seam; a live process-environment regression proves that the
  former 4,023-byte adapter ceiling is gone and the original paragraph-retry
  model is preserved.
- Mapped SYSINIT's command-name realization, configuration override and
  loader/EXEC owner chain from original sources. `50:36` was correctly
  classified as a no-VDD entry notification, not a launcher.
- Closed the declared-image loader handle family with source-built x64 `/MT`
  evidence for `50:12 → 50:00 → 50:42 → 50:16 → 50:02`, including split
  token ownership, seek/ZF ABI, continuity and forged-token refusal.

No conclusion is made about guest `$Exec` completion, target execution,
COMMAND return/lifecycle, a native Bochs trace, or a general BOP completion.
The current whole-DEM identity fixture's independent `50:1A` overlay-CWD
expectation remains a recorded owner-package finding, not a hidden T217 fix.

The next package, T218, begins with the guest `$Exec` structural prerequisite
map and profile disposition; it must not choose individual BOP work from a
trace.
