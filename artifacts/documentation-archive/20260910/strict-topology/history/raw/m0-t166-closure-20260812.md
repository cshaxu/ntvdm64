# M0 T166 closure — 2026-08-12

The one admitted full CLI/native run retained complete input, stdout, stderr,
and outcome evidence. It reached the exact accepted `BOP_UNSIMULATE` stop and
normal Bochs CPU-loop shutdown without timeout, then returned exit code 1 from
native Bochs's outer shutdown path. This is neither generic BOP success nor a
guest failure claim. No second run is allowed. T167 owns the source-only
exit-contract audit.
