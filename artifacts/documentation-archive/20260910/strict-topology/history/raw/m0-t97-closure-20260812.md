# M0 T97 Closure — Immutable DOS Namespace And Pathname Search

T97 closed the profile-owned namespace/search component rather than a general
host filesystem. It established v4 identity-bound DOS metadata for COMMAND,
the selected target, CONFIG.SYS and AUTOEXEC.BAT; withdrew contradictory
file-token experiments; and restored only source-derived pathname FindFirst /
FindNext (`50:09/0B`) through the shared DEM plane. The provider uses copied
guest inputs and DTA transactions, never a host path, handle, stat, directory
enumeration or clock. FCB forms remain deferred.

The focused runtime fixture passes. A bounded Bochs probe then established the
next dependency: the retained engine rejects the v4 execution plan before its
CPU loop, with zero BOPs and transactions. This does not disprove the provider;
it identifies the engine composition as the next closure work. Evidence is in
`etc/research/t97-s5-profile-and-engine-checkpoint-001.md`.
