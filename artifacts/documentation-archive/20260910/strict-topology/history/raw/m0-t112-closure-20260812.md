# M0 T112 closure — 2026-08-12

T112 reduces the original unexpected-interrupt handler to a finite PIC/RAM
mechanics manifest. It rejects a premature generic adapter-to-Bochs port API,
because the original operation is synchronous and multi-step while the current
bridge is a result-returning boundary.

T113 is admitted to prove the unmodified original handler's compile closure
without a shim, link, Bochs change or runtime execution.
