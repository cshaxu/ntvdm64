# M0 T159 closure — 2026-08-12

T159 statically closes the current-adapter native POST/deferred-handoff
composition. The approved form rebuilds exactly `main.o` and
`cpu\\exception.o`, disables direct execution-plan entry, preserves native
POST/IVT/INT10 lifecycle, and consumes one opaque `CA80:0003` UD2 rendezvous
through existing adapter checks. It retains all Bochs archives and the current
T154 adapter set without a source, firmware, device, or BOP change.

The result is configuration closure only. T160 is separately admitted for one
fresh MSVC/x86 link and one bounded deferred observation; any archive/device
rebuild or semantic workaround stops that task.
