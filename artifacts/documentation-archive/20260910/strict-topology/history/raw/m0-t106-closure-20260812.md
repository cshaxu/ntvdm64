# M0 T106 closure — 2026-08-12

T106 proved the generic two-phase gather/read/write/resume bridge is active
and source-owned.  It is not missing from the T105 runtime.  The captured
predecessor is original `MS_bop_2`, whose VdmRedir-failed outcome is fixed:
CF set and `ERROR_INVALID_FUNCTION` in AX.

T107 is the bounded source-derived rehost of that original unavailable
disposition through the common provider route, with no redirector backend.

