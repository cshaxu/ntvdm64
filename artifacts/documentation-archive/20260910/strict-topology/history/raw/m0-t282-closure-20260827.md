# M0 T282 closure — original COMMAND local lifecycle source-composition phase

## Result

T282 closed its admissible original COMMAND source-composition phase. All
eleven original COMMAND translation units compose in the formal x86/x64
static graph. Local `GetNextVDMCommand` transport, original initialization,
OEM helper composition, source-shaped register access, bounded guest-memory
primitives and the original `cmdGetCurrentDir` bounded binding have focused
evidence.

## Explicit non-claims

This closure does not claim BOP ingress, runnable COMMAND, a guest trace,
child execution, PIF/console/keyboard completion, a broker, or historical
CSRSS/BaseSrv behavior. `cmdComSpec` and `cmdGetInitEnvironment` remain
disabled because their original invalid-address cases have no observable
service result.

## Complete disposition

The S16 32-row ledger assigns each COMMAND service/helper one successor.
Platform ABI canonicalization and historical-interface adapter convergence are
the immediate ordered prerequisites; Redirector, WOW and guest parent-return
remain later specialist owners. No direct original local recovery remains
eligible without bypassing that sequence.

## Verification

- Formal MSVC `/MT` x86/x64 original COMMAND archive composition passed.
- Formal x86/x64 bounded guest-location fixture passed.
- S16 confirms 32 unique source-owner entries with no ownerless row.
- Documentation governance and `git diff --check` are required for delivery.
