# M0 T104 closure — 2026-08-12

T104 added a deferred mode to the current-adapter derivative.  It rebuilds
only `main.o` and `cpu/exception.o` with direct entry disabled and deferred
handoff enabled; all other engine archives remain inherited.  The fresh
MSVC/x86 build and one bounded native POST trace both succeeded.

The trace reaches the real NTIO/NTDOS BOP path without the former VGA-aperture
panic.  It is a lifecycle proof, not CLI completion.  T105 is admitted for a
single longer observation that will classify the next execution boundary
before any provider change.

